# Repository scorecard — Claude, 2026-08-27 (1.1.0 re-score)

**Assessor:** Claude (Fable 5), via three firewalled evidence-gathering agents (A: research
integrity / ambition / documentation honesty; B: runtime correctness / maintainability;
C: test quantity / test-CI truthfulness / release governance), graded per axis and assembled.
**Scope:** tag `v1.1.0` @ `df881b87`, checked out as its own worktree; live external state
(PyPI, GitHub tags/releases, CI run history) as of **2026-08-27**.
**Rubric:** A exemplary / B solid, minor gaps / C functional with significant known gaps /
D serious deficiency actively costing the project / F absent-broken. +/- allowed.
Grades reflect the state AT THE TAG, not trajectory. A grade moves only when normal tests,
CI, or release procedure now ENFORCE the improvement — never because a plan, ledger row, or
roadmap item exists. Every grade change cites its enforcing artifact. Hard grading by design.

**Independence disclosure.** The assembler read both 2026-08-19 cards in full (they are the
rubric and baseline) and was blinded from: the "Scorecard → roadmap reconciliation
(2026-08-27)" section of `docs/plans/roadmap_1_1_to_1_3.md`, the 2026-08-27 "re-score owed"
lines in `docs/plans/README.md` / `docs/limits/README.md`, and any memory file about that
reconciliation or #564. The blind held for the section and the pointer lines. One leak: the
session's auto-loaded memory INDEX carries a one-line pointer naming "#564 … scorecards →
roadmap item 16 (mechanization) + 1.2 gate 8; D5 RE-OPENED, D27 re-filed, D28
pre-ingestion". That line was seen before grading; the file behind it was not opened, and
the three evidence agents were given neither it nor the 08-19 grades. Weight the
independence of the *evidence* fully. Agent C's only firewall contact was one grep-output
line from the 08-19 Claude card restating a baseline condition already in its brief; Agents
A and B reported no contact. The agents' proposed grades were adjusted by the assembler on
four axes, each with the reason stated in that axis's section; no evidence was altered.
Ordering note: the first `git commit` of this card failed on a GPG pinentry timeout in
the same shell step that performed the unblind read; the retry committed the file
unchanged (its mtime precedes the read, and the committed text is the pre-unblind text).
Nothing below the Summary was edited after unblinding; this paragraph and the next were.

**Divergence from the 2026-08-27 reconciliation** (read after the grades were committed).
The reconciliation's mechanization list (roadmap 1.1.x item 16: bump↔CHANGELOG step,
fix→tests lint, `os.replace` lint, function-length baseline, delete `docs/CHANGELOG.md`;
1.2 gate 8(d): Tier-3 dispositions) was derived independently of this card and matches
its upgrade conditions almost one-for-one — convergence, and a reason to trust those
conditions. Four points diverge. (1) The reconciliation lists "the nightly
`requires_model_cache` job" among the enforcement now in place; that lane has failed
every scheduled and dispatched run since it was created, because its "every model" warm
list omits the model the marked tests load. (2) It states "PyPI serves 1.1.0 and every
sync line says so" and asks for the re-score "at the `v1.1.0` commit"; at that commit
four sync lines say the release is pending. (3) It anticipates "a real move" upward and
names nothing on the research-integrity or documentation-honesty axes; this card moves
both DOWN, on evidence absent from the reconciliation — the Exp 53/53b dirty-tree,
unreachable-commit provenance gap behind an EARNED ledger row, the tag placed on a
"PUBLISH PENDING" commit, the nonexistent "verb-count drift check" claimed in item 14
(left uncorrected: 18 claimed, 21 in code), and two new silent public-API defects (N1,
N2). Its item 16 has no counterpart for this card's integrity upgrade conditions
(in-process harnesses refusing dirty-tree gated records; a prereg-precedes-data check) or
for the N2 create→shutdown→load round-trip test. (4) Gate 8(a) counts "seven" `scripts/`
harnesses overwriting S4 evidence; at the tag the count is five. None of these change a
grade; they are recorded as signal.

## Summary

| Area | 08-19 (Claude / Codex) | 1.1.0 | Δ (vs 08-19 Claude) |
|---|---|---|---|
| Research integrity | A− / A | **B−** | ▼▼ |
| Ambition and originality | B+ / A | **B+** | = |
| Documentation honesty | B+ / A− | **C+** | ▼▼ |
| Runtime correctness | D+ / C− | **C** | ▲▲ |
| Maintainability | C+ / D+ | **C** | ▼ (▲ vs Codex) |
| Test quantity | A− / A | **A−** | = |
| Test/CI truthfulness | C+ / C | **B−** | ▲ |
| Release governance | D+ / D | **C+** | ▲▲ |

**Signature finding:** every enforcement mechanism the 08-19 cards asked for at the
*engineering* perimeter got built and is real — CI runs the whole `tests/` tree behind
branch protection, the suite is hermetic, the architecture audit has a baseline gate, six
of the severe ledger defects have revert-verified guards, and PyPI actually serves 1.1.0
from a GitHub Release with SHA-identical assets. The two new falsehoods this cycle both
sit exactly one hop outside that perimeter: the 1.1.0 tag was placed on a commit whose own
CLAUDE.md, plans README, docs index and publication guide say **"PUBLISH PENDING — PyPI
serves 1.1.0rc1"**; and the release's headline hardware result (Exp 53b, ledger row
EARNED) executed from a **dirty working tree at a commit unreachable from the tag**, with
its pre-registration landing on `main` in the same squash as its data — the Exp 42b lesson
("a result whose code-under-test cannot be established is not a validation") recurring in
an in-process harness the provenance lint's regex does not reach. Engineering enforcement
moved the low grades up; research integrity moved down because its enforcement is scoped
to subprocess-spawning harnesses and the flagship result ran through a different door.

## Why each grade was earned

### Research integrity — B−
- Exp 52 (Nurture) is the model: pre-registration `b9398ded` → harness + amendments
  `e367f526` → Phase A data `60195a29` → Phase B `732386ef`, all on `main`, all 36 Phase B
  rows stamping `executed_git_hash 60195a29`; amendment 1 disclosed with the Exp 48
  precedent, margin unchanged; amendment 2 *added* a stricter cap; claim scope in
  `docs/experiments/52_nurture.md` §"What this shows — and what it does not" matches the
  data (one session, n=12/arm, sign-only relief, no LLM path). Guarded by five CI-run tests
  (`tests/unit/test_cradle_mother.py`, `test_cradle_mother_credit_mode.py`,
  `test_infant_operant_satiated_body.py`, `test_exp52_phase_a_harness.py`,
  `test_analyze_cradle_mother_guard.py`).
- Exp 53 honestly recorded as APPARATUS (27/36 = 0.75 < 0.80) and not relabelled; L8
  records that the Exp 37 Qwen32B PASS "cannot currently be re-derived" and keeps L184 at
  PARTIAL; L9 corrects its own earlier draft. Exp 42b retraction and Exp 48 CONTESTED →
  SUPERSEDED trail intact. Provenance lint still in CI ("Harness provenance lint" step),
  its regex matches exactly the six harnesses CLAUDE.md names, no new exempt markers.
- **The flagship result is not commit-reproducible.** All 5 start records in
  `docs/experiments/data/53_cross_context_readout.jsonl` and `53b_cross_context_readout.jsonl`
  stamp `executed_git_hash 68f9026e` + `working_tree_dirty_src_scripts: true`;
  `git merge-base --is-ancestor 68f9026e v1.1.0` → NOT an ancestor. Neither the write-up
  nor either pre-registration contains the word "dirty"; the write-up cites the hash as if
  it identified the code. The harness told the truth; the write-up dropped it; the ledger
  row L193 says EARNED.
- **Freeze-before-data is not git-verifiable for 53/53b.** Exp 53 first data record
  15:04 UTC; its pre-registration reached `main` at 17:05 UTC (`6e7530c6`). The 53b
  pre-registration's only commit on `main` is the squash `617b1625` that also lands its
  data and RESULT; the "one declared change" (δ 0.55→0.30) was chosen after seeing Exp 53's
  misses and is justified against "the azimuths Exp 53 actually delivered". Amendment 2 has
  no commit object anywhere. Pre-data status rests on self-attestation.
- Two Tier-1 EARNED rows (`behavioral_graduation_candidates.md` L185 EC pattern completion,
  L186 SEM pain → NAc) carry neither a Regression-guard line, committed data, nor a
  data-lost annotation — the roadmap's own step-4 cut-line rule unmet. Tier-3 table:
  16 of 20 still Pending, **zero dispositions since 2026-05-27** (the only diff since 08-19
  is row #9's data note). The Exp 45 row's fired trigger was "waived" in narrative
  (§"Walk continuation — 2026-08-18/19": "step 3 did NOT run — waived") while the doc
  asserts "No row is Stale or Broken, which is the release rule".
- Pre-registration count 39 protocol files (was 36), of which 5 are actual pre-registrations
  (44b, 52, 53, 53b, h1); Exp 44b's is still "TEMPLATE".
- **Held from B:** the EARNED hardware transfer row rests on a dirty-tree run at an
  unreachable commit with its pre-registration squashed in with its data; two EARNED rows
  have no guard or data; Tier 3 untouched for three months.
- **Upgrade to B:** (1) every in-process harness under `scripts/orient_*/` refuses to write
  a gated record when `working_tree_dirty_src_scripts` is true unless `--allow-dirty` is
  passed and the flag is echoed in the result doc's header — enforced by extending
  `scripts/lint_harness_provenance.py` scope to in-process harnesses plus a unit test on
  the refusal; (2) a CI script asserting, for every `docs/experiments/data/*.jsonl` whose
  pre-registration is named in its result doc, that `git log --format=%ci -- <prereg>` on
  `main` precedes the file's first `ts`; (3) L185/L186 gain a data citation or a dated
  data-lost annotation; (4) Tier-3 rows 1–8, 10–15, 18–19 get dispositions.

### Ambition and originality — B+
- One new mechanism since 08-19 earned behavioral weight the same week: relief-sourced
  operant credit (`simulation/cradle_mother.py`, `generative_runner.py`,
  `embodiment/tool_bridge.py`, commit `e367f526`). Exp 52 Phase A: taught 0.892 vs
  satiated/yoked/no_feed all 0.496 — yoked (same reward volume, no contingency) at chance
  is a contingency-not-volume, need-gated operant result with zero gradient updates. Phase B
  embodied 0.878 / 0.441 / 0.413.
- Cross-context transfer on hardware (Exp 53b): SHA-verified `aut_nac.json` + `aut_ec.json`
  from the sim nursery drive the physical Reachy toward a speech source 36/36, with
  zero-bias controls at 0.00 and 0.50; 120/120 live percepts pattern-complete into
  sim-built clusters. (Provenance caveats charged on the integrity axis, not here.)
- Bio-mapping taxonomy re-sampled on six modules (`time/oscillator.py` MECHANISM,
  `time/scn.py` FUNCTIONAL, `math/angular_gyrus.py` FUNCTIONAL, `decisions/nac.py`
  FUNCTIONAL/MECHANISM-at-link, `default_network/network.py` NAME-ONLY, `memory/atl.py`
  FUNCTIONAL) — all accurate to the code. The 08-19 "~60% mnemonic" reading stands.
- Counter-interest publication continues (roadmap "Enthusiasm-to-evidence flags" #4
  concedes the magnitude line rests on n=1 sessions atop a 3×-contested DoA gain).
- Assembler adjustment: Agent A proposed B, citing 44b-not-run, tabular policies, and the
  53b provenance gap. The first two are the unchanged 08-19 held-from conditions and the
  third is an integrity finding already charged above; this axis measures what was
  attempted and whether it is novel, which improved. Held at B+.
- **Held from A−:** Exp 44b — the pre-registered at-power test that a bio-mechanism is
  indispensable under an LLM — is still a template (`44b_pilot.md` 1 seed/arm; the
  2026-08-24 S4 non-stationarity analysis added a blocker, not progress); the earned
  learning remains three cluster-keyed biases over a 3-bin azimuth representation.
- **Upgrade to A−:** Exp 44b run as pre-registered (prereg merged to `main` before the first
  sub-sim; ≥10 seeds/arm; `exp44/campaign.py` provenance assertion in the log; primary sign
  test p < 0.05; the Exp 51 name-copying control) with raw captures under
  `docs/experiments/data/44b/`.

### Documentation honesty — C+
- The 08-19 stale confessions were fixed and are accurate at the tag: `os.replace` sites
  = **17** (matches CLAUDE.md's "17"); bare swallows = **431** (matches "≈430");
  `similarity/ec.py::EntorhinalCortex` docstring now says "exact same-modality centroid
  scan, O(Nd)" and the code is that scan. Every CI step CLAUDE.md names exists in
  `.github/workflows/test.yml`; `scripts/lint_claude_md_invariants.py` passes (7 docs,
  all links resolve); every `Regression guard:` link in CLAUDE.md, `docs/agents/*.md` and
  the graduation ledger resolves.
- **The tagged tree contradicts itself about its own release.** `CLAUDE.md` §Active
  initiatives ("**PUBLISH PENDING** … PyPI serves 1.1.0rc1 … plain install resolves to
  1.0.9"), `docs/plans/README.md`, `docs/index.md` and `docs/publication_guide.md` all say
  pending, while `CHANGELOG.md` `## [1.1.0] - 2026-08-26` says "`pip install pymaxim`
  resolves to it" and `docs/announcements/release_1_1_0.md` says the artifacts were
  "uploaded to PyPI". The publication guide's own sync-table rule ("version + true PyPI
  state … in the same commit, not as a follow-up") is unsatisfiable by the documented
  publish→tag→release order, and was not satisfied: the flip is a separate commit 2h10m
  after the tag. `docs/publication_guide.md` §5 additionally says "1.1.0rc1 for this
  release" while the tree is at 1.1.0.
- **A claimed check does not exist.** `docs/plans/roadmap_1_1_to_1_3.md` §1.1.x item 14:
  "18 API verbs … Keep dependency and verb-count drift checks executable" — `README.md`
  says 17, `src/maxim/__init__.py::_API_VERBS` holds 21 unique names (22 literals;
  `"list_registered_tools"` twice), and `grep -rn _API_VERBS tests/` → nothing.
- Roadmap §"1.1 cut line" row 16 is "OPEN — 1.1 DOCS GATE" while listed under "Blocking
  for the 1.1 cut" and CLAUDE.md says "website audit DONE 2026-08-26". `ARCHITECTURE.md`
  still says "`ec.py` … LSH-based approximate nearest neighbor" — the D21 fix stopped at
  the module boundary. Ledger row L189 calls `45_doa_sweep_baseline.jsonl` "durable data"
  while L9 (same tag) documents its first sweep as `dry_run: true`. The
  `.github/workflows/test.yml` warm-step caption "Every model any marked test loads" is
  false (see Test/CI). CLAUDE.md's "9,168 tests in 12:09" vs 9,490 collected, 5:22 wall.
- No quantified admission is CI-emitted; the correct 17 and ≈430 are hand-maintained.
- **Held from B−:** six false or self-contradicting claims in the tagged tree, two of them in
  the always-loaded CLAUDE.md and the CI workflow, one of them ("drift checks executable")
  asserting an enforcement artifact that does not exist — the 08-19 failure class
  ("the honesty layer itself rots") recurred in a new place.
- **Upgrade to B−:** (1) a `lint` job step that reads the three sync-table prose lines +
  `docs/index.md` and fails unless all agree on one PyPI-state string that matches
  `pyproject.toml` and the CHANGELOG top header; (2) `tests/unit/test_api_surface.py`
  asserting `len(_API_VERBS)` equals the number in README.md; (3) the swallow lint and the
  `os.replace` grep print totals in CI and CLAUDE.md cites the CI output; (4)
  `ARCHITECTURE.md` EC rows corrected.

### Runtime correctness — C
- Both 08-19 upgrade conditions are met and enforced. D13/D14: `#523` extracted
  `_planning_attempt_is_active`, `_proposal_without_action_reason`, `spinner_truth_message`;
  guarded by `tests/unit/test_planning_liveness.py` (60) + `test_worker_pool.py` (42) in the
  CI fast suite; forcing `_planning_attempt_is_active → False` fails 3. D15: `api.run()`
  now delivers `goal` via `state.data["pending_cli_input"]` (consumer in
  `runtime/agent_loop.py::run_agentic_loop`) and rejects loudly — black-box verified
  `run(goal="   ")` → `ConfigurationError`, `run(robot="reachy_mini")` headless →
  `ConfigurationError`, unknown kwarg → `TypeError` (`tests/unit/test_api_core.py`, 30).
- Four more severe rows FIXED with revert-verified CI guards: D2
  (`tests/unit/test_memory_pair_invalidation.py`, 5 fail on revert), D17
  (`tests/unit/test_load_agent_restore_contract.py`, 4 fail; black-box: corrupt
  `hippocampus.json` → `MemoryCorruptionError`, `on_corrupt="fresh"` loads), D18
  (`tests/unit/test_register_tool_persistence.py`, 6 fail), D22
  (`tests/unit/test_sim_types.py`, 3 fail). D12 guard exists but fails **by hang** (>60 s,
  no `pytest-timeout`) rather than by assertion. All 12 cited guard files: 288 passed.
  Fix:feat 12:2 over the last 40 commits; 11/12 fix commits touching `src/` also touch
  `tests/`.
- Still open and silent in normal use: D6 (Hebbian binding never grows on llm-primary
  percepts — `memory_hub.py` passes a 1-tuple, `episode.py::apply_hebbian_on_close` returns
  on `len < 2`), D9 (drive emitter in `embodiment/body.py` constructs `TemporalEvent` with
  nonexistent kwargs → `TypeError` into `log.debug` on every emission, reproduced), D31
  (mirror-image DoA credited with the wrong sign, no fold guard), D28, D27 (labelled
  "1.1 gate", shipped open), D15/D16 partial (`home_dir` CWD-relative in 2+1 sites;
  `imagine()`/`campaign()` setup windows). 499 single-statement silent broad handlers in
  127 files by independent AST count.
- **Two new silent stable-API defects, reproduced black-box.** N1: `api.campaign()`
  documents `npc_model`, `interactive`, `prompt_handler` and never references any of them
  (same class as D15, sibling verb). N2: `AgentInstance.shutdown()` calls
  `memory_hub.on_session_end()`, which returns `{}` when no session was started — neither
  `create.agent()` nor `load.agent()` calls `on_session_start()`, so the public
  create→shutdown→load cycle writes only `hippocampus.json` + `nac.json`, drops EC/SCN/ATL,
  and every subsequent `load.agent()` logs "Half-present NAc/EC pair" on the API's own
  output — the orphaned-bias state D2/D17 were fixed to detect, produced by the API itself.
- Assembler adjustment: Agent B proposed C+. Held at C because (a) 11/60 D13 guards are
  `inspect.getsource(run_agentic_loop)` substring pins and no test in the file calls
  `run_agentic_loop` — the fix is guarded at helper level, the wiring by text; (b) N2 is a
  new silent defect in the persistence contract of the same class as the ones just closed.
  The 08-19 condition said "Upgrade to C"; it is met, and no further.
- **Held from C+:** N1/N2 falsify "every public argument has an observable effect" and
  "the API's own shutdown produces loadable state"; D6/D9/D31 unchanged since baseline.
- **Upgrade to C+:** (1) a black-box test in `tests/unit/test_api_core.py` doing
  `create.agent → mutate SCN/EC → shutdown → load.agent` through the public API and
  asserting `ec.json`/`scn.json`/`atl.json` exist, state round-trips, and no
  "Half-present" WARNING is logged; (2) `api.campaign()` threads or rejects all three
  parameters with a test. **To B−:** additionally D6 and D9 closed with guards (D9: a test
  constructing the drive `TemporalEvent` through `body.py`'s emitter), and a D12 guard that
  fails by assertion under `pytest-timeout`.

### Maintainability — C
- Perimeter gained two real ratchets: the architecture audit now has a shipped baseline
  (`src/maxim/utils/architecture_baseline.json`, 30 reviewed entries, 33 findings, 0
  unreviewed) gated by `tests/unit/test_architecture_audit.py::TestRealCodebase` in the
  CI fast suite — injecting one forbidden import fails 1 test (the 08-19 upgrade condition
  #2, met and enforced); `scripts/lint_no_silent_swallows.py` in CI is zero-total over the
  16 measurement-path files (0 at the tag) + PR-diff-scoped repo-wide. mypy on 5 of 508
  files passes; ruff clean over 950 files; version sync green; 6 briefs, 76 lessons.
- **The center got worse.** AST-measured (`end_lineno − lineno + 1`):
  `runtime/agent_loop.py::run_agentic_loop` **3,546** (was ≈3,331; 49 `try`, 38 broad
  handlers, 262 locals), `simulation/orchestrator.py::start_simulation_mode` **3,342**
  (was ≈3,226), `cli.py::_main_impl` **1,752** (was ≈1,737). All three grew; the D13/D14
  fixes landed *inside* the monolith and their wiring guards are `getsource` string pins
  because the function cannot be unit-driven. `docs/plans/god_function_decomposition.md`
  is `Status: DRAFT (2026-08-10)` with zero follow-ups; no module added under `runtime/`
  or `simulation/` since 2026-08-01. 16 functions > 300 lines, 6 > 500, 40 modules
  > 1,000 lines. 17 hand-rolled `os.replace` sites, no lint.
- On the 08-19 disagreement (Claude C+ vs Codex D+): the evidence supports the C-range
  reading — the perimeter ratchets held (no regression escaped in this window; 11/12 fixes
  carried tests) and the fail-loud front-gate the decomposition plan sequenced first was
  actually completed first. The cost of the monolith is real (three of four wedge defects
  lived in it; fixes grew it; guards degraded to text pins) but bounded, not cyclic. C, not
  C+, because the split is untouched and nothing stops the functions growing.
- **Held from C+:** `run_agentic_loop` grew 215 lines since 08-19 with the split plan
  untouched, and CI does not bound any function's length.
- **Upgrade to C+:** an AST-based CI lint pinning `run_agentic_loop`,
  `start_simulation_mode`, `_main_impl` at ≤ their v1.1.0 lengths (3,546 / 3,342 / 1,752),
  AND one merged extraction moving ≥1 numbered section of `run_agentic_loop` into a
  module-level function exercised by a test that calls it directly. **To B−:**
  `run_agentic_loop` < 3,000 lines; an `os.replace` CI grep with the 17-site grandfather
  ratchet; mypy scope > 5 files with a CI-tracked list.

### Test quantity — A−
- 9,095 test functions in 411 files (08-19: 8,850 / 430); the documented fast-suite
  command collects 9,490 (41 deselected `slow`); per-dir: unit 8,613, integration 334,
  substrate 344, behavioral 166, learning 35, experiments 18, performance 5. Local run:
  **9,452 passed, 38 skipped, 0 failed, 0 errored, 5:22 wall**.
- Guard-test-per-fix held over the whole window: 9/9 `fix` commits 2026-08-19 → tag that
  touch `src/` also touch `tests/` (the #519 slip did not recur). Six ledger fixes shipped
  with revert-verified guards (see Runtime). Two-process PYTHONHASHSEED guard, fake-SDK
  hardware-safety suites, and 12 live "verified to fail" citations intact (the drop from 21
  is the CLAUDE.md diet relocating text to lessons, not fewer verified guards).
- Assembler adjustment: Agent C proposed B−, citing 41 `slow` tests that execute in no CI
  lane and no behavioral test file for Exp 52/53b. The first is charged on Test/CI
  truthfulness (it is a CI-coverage claim, not a quantity); Exp 52 has five CI-run unit
  guards. The 08-19 held-from conditions persist unchanged and the upgrade condition is
  unmet, so the grade holds.
- **Held from A:** guard-test-per-fix is still habit — `scripts/lint_*.py` are exactly four
  and none couples `src/` to `tests/`; the 90-day clean window is 8 days old; the
  behavioral suite is still 5 files against an experiment surface that grew by three
  experiments; the flagship D13 wiring is guarded by 11 source-text pins.
- **Upgrade to A:** a diff-scoped CI step (`scripts/lint_fix_touches_tests.py` or
  equivalent) failing any commit whose subject matches `^fix` and touches `src/` without
  `tests/`, plus 90 days clean under it.

### Test/CI truthfulness — B−
- **The 08-19 hold is closed.** `.github/workflows/test.yml` job `unit-tests` step "Run
  required fast suite" runs `python -m pytest tests/ -x -q -m "not slow" --tb=short
  --ignore=tests/integration/test_memory_hub.py` plus a "MemoryHub integration gate" step,
  on every PR and push (switched in `150fd6a1`, #527); branch protection on `main`
  requires `unit-tests` + `lint`. Integration, substrate, behavioral, learning and
  experiment suites are now merge gates.
- **Hermetic, verified.** `tests/conftest.py` module scope creates a
  `maxim-tests-*` temp root and sets `HOME`, `XDG_CONFIG_HOME`, `XDG_CACHE_HOME`,
  `HF_HOME`, `TORCH_HOME`, `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1` before any `maxim`
  import; `pytest_collection_modifyitems` skips `requires_model_cache` unless
  `MAXIM_RUN_MODEL_TESTS=1`. After the full local run: **0 files newer than the marker
  under `~/.maxim` or `~/.config/maxim`**, with sentence-transformers 5.4.0 installed. 45
  autouse fixtures in `tests/conftest.py`. The architecture-audit test now asserts against
  the shipped baseline instead of "returns a list" (D19 gate).
- **The nightly lane the workflow says exists has never passed.** `model-cache-tests`
  (schedule + dispatch only) is **7/7 scheduled failures 2026-08-21 → 2026-08-27** plus
  2/2 dispatches, always `tests/substrate/baselines/test_baselines.py::TestEmbeddingBaseline`
  with `OSError: We couldn't connect to 'https://huggingface.co'`. Root cause is in the
  workflow: the warm step's caption "Every model any marked test loads" lists four models
  and omits `all-MiniLM-L6-v2` (`embedding_baseline.py::run_baseline` default). The lane
  built "so a regression fires this test instead of silently passing CI" has been red for
  the entire release window with nobody acting on it; its `executed ≥ 12` floor is met by
  13, one skip from vacuity.
- 41 `slow`-marked tests (16 integration, 15 substrate, 9 benchmarks, 1 unit) execute in
  no lane, nightly included. Branch protection is `strict: false`, `enforce_admins: false`.
  No `MAXIM_DATA_HOME` isolation (HOME redirect only); one temp root leaked.
- **Held from B:** the one lane meant to prove the substrate guards can fire is 9/9 red
  under a false caption; 41 tests run nowhere.
- **Upgrade to B:** `model-cache-tests` green on 7 consecutive scheduled runs with the warm
  list derived from source (not a hand-kept comment) and the `executed` floor raised to the
  true marked count minus explicitly-listed dataset skips; a scheduled `-m slow` lane with
  an `executed > 0` assert; `strict: true` on the required checks.

### Release governance — C+
- **The 08-19 falsehood is gone: PyPI serves 1.1.0** (JSON `info.version` 1.1.0; wheel
  2026-08-26T18:47:11Z sha256 `a63b3eed…`, sdist `0f7a3632…`), preceded by 1.0.9
  (08-23) and 1.1.0rc1 (08-25). GitHub Release `v1.1.0` published 18:47:39Z with the wheel
  + sdist, **byte-identical to the PyPI files**. Tag `v1.1.0` → `df881b87`; sdist `src/`
  equals the tag's `src/` except the release-only `console/ui_dist/`. Documented order
  (publish → tag → release) was followed. `CHANGELOG.md` has `## [1.1.0] - 2026-08-26` with
  a date matching the upload; `[Unreleased]` is empty at the tag;
  `scripts/audit_release_tags.py` passes. Version policy followed inside the release
  window (src-touching windows bumped: 4/10 → rc1, 2/13 → 1.1.0).
- **The tag sits on a commit that says the release is pending.** `df881b87`'s CLAUDE.md,
  `docs/plans/README.md`, `docs/index.md`, `docs/publication_guide.md` all say "PUBLISH
  PENDING / PyPI serves 1.1.0rc1 / plain install → 1.0.9" — permanently false for anyone
  checking out `v1.1.0` or reading the Release's source snapshot; the guide's sync-table
  rule was violated by construction (flip commit `c4204121` 2h10m post-tag).
- The 08-19 "Upgrade to C" condition is only partly met: `docs/CHANGELOG.md` (the dead
  duplicate, frozen at 0.3.0) **still exists** at the tag. The "to B" condition (CI check
  that a version bump carries a CHANGELOG entry) does not exist (`grep -i changelog
  .github/` → empty). The `v1.0.9` GitHub Release has **zero assets**; 1.0.9 has three
  release dates (CHANGELOG 08-19, tag message 08-22, PyPI 08-23). The v1.1.0 Release body
  uses seven repo-relative links that resolve to HTTP 404 from the release page — the
  exact bug class `fix(readme)` #529 fixed for the PyPI page one release earlier. One day
  post-tag, `feat(exp54)` (#561) changed `runtime/agent_loop.py` + `motor_backend.py` with
  no bump and an empty `[Unreleased]`.
- Assembler adjustment: Agent C proposed B−. Held at C+ because the 08-19 C condition is
  incomplete (duplicate changelog), the B condition is absent, nothing new is mechanized
  (the credited artifacts are external state and a manual script), and the tag/commit
  contradiction is the same "remediation wrote a falsehood" class as 08-19.
- **Held from B−:** the tagged source asserts the release is pending; the duplicate changelog
  survives; no CI ties a bump to a CHANGELOG entry; seven dead links in the Release notes.
- **Upgrade to B−:** (1) a `lint` step asserting that when `pyproject.toml`'s version
  changes in a diff, `CHANGELOG.md` gains a matching `## [X.Y.Z] - <date>` header and the
  CLAUDE.md / plans README / docs index version lines carry no "pending"/"rc"/"serves" text
  contradicting `pyproject`; (2) `docs/CHANGELOG.md` deleted; (3) `scripts/audit_release_tags.py`
  in CI, extended to assert every tag with a PyPI release has a GitHub Release with ≥2
  assets whose sha256 match PyPI and whose notes use absolute links; (4) a post-tag `src/`
  commit without a bump fails a diff-scoped check or must add an `[Unreleased]` line.

## Cadence

Re-score at the 1.2 cut, or at any point an axis's upgrade condition above is claimed
complete — the claim triggers the re-score, the re-score decides. This card is the Claude
half of the dual-assessor cadence; the paired Codex card for 1.1.0 is owed separately and
should be produced blind to this one. Divergence between the two cards is signal —
investigate before averaging.
