# Behavioral Graduation Candidates — Living Behavioral-Regression Discipline

**Status:** Living artifact. Pairs alongside benchmarking as one of two 1.0 gates AND persists post-1.0 as the behavioral-regression discipline — the behavioral analog to CLAUDE.md's "Architectural invariants (do not break without discussion)" section.
**Created:** 2026-05-27.
**Sibling doc:** [behavioral_convergence_practice.md](deferred/behavioral_convergence_practice.md) — "does the agent get better" (system-level, ongoing). This doc — "do specific bio-mechanisms carry the load they claim" (mechanism-level, experiment-driven).
**Triggered by:** CLAUDE.md tagging audit (PR #272, 2026-05-26) revealed 67 `[engineering]` vs 4 `[behavioral]` — a 5.6% behavioral ratio. Honest read: many bio-mechanisms in CLAUDE.md claim behavioral weight but haven't yet earned a cited experiment. The Roy harness was the prerequisite for earning them; it is just now mature enough to start producing graduations at scale.

## Front-gate scope pressure (retroactive)

Added 2026-05-27 per CLAUDE.md Principle 3.

**Question:** does this discipline need to be its own artifact, or can it ride on existing infrastructure?

**Existing infrastructure surveyed:**

| Candidate | Why insufficient (or sufficient) |
|---|---|
| CLAUDE.md "Architectural invariants (do not break without discussion)" section | **Wrong type of artifact** — that section enumerates code rules that break loudly. The behavioral analog (mechanism claims that can silently regress) needs experiment citations + status lifecycle (`Pending` / `Earned` / `Stale` / `Broken`), neither of which the engineering-invariants section models |
| CLAUDE.md two-tier invariant tagging (`[engineering]` vs `[behavioral]`) | **Provides the type signal** — and revealed the 5.6% behavioral ratio that triggered this discipline — but lacks lifecycle tracking. An invariant can be tagged `[behavioral]` and still silently regress without re-validation |
| Regression-guard convention (Principle 5, paths/grep/structural-enforcement citations) | **Provides the format** — this doc's Earned entries use the same `Regression guard:` field so one grep audits both surfaces. But applies to engineering only; doesn't address experiment re-runs |
| [behavioral_convergence_practice.md](deferred/behavioral_convergence_practice.md) | System-level "does the agent get better" living doc. Different scope: this one is mechanism-level "does this specific bio-claim hold" |
| [memory_consolidation_practice.md](deferred/memory_consolidation_practice.md) | Same shape as the above — single-mechanism living doc. Not a meta-list across all behavioral claims |
| Trigger-event-driven CI re-run of Roy iterations | Plausible 1.2+ structural enforcement, but adds infrastructure + cost the discipline can survive without. Cheaper to track manually as a living doc; CI hook is a future-proofing add |

**Verdict:** yes-needs-own (small but real). The discipline is the **list itself** + the **lifecycle status states** + the **trigger-event re-run convention** — none of which fit inside CLAUDE.md's existing sections.

**Specific reason:** CLAUDE.md's engineering-invariants section explicitly lists code rules; this list is meta — it tracks which claims need experiment validation, what their current status is, and when they need re-running. The discipline is one new artifact, not a new code mechanism.

## Why this is a living gate (not a one-shot 1.0 milestone)

This doc serves **two phases of the same discipline**:

**Pre-1.0 (the graduation push).** 1.0 has two orthogonal gates, not one:

- **Benchmarking** — agent performance on tasks (does it solve the thing).
- **Behavioral graduation** — mechanism validation (does the bio-pipeline carry the load it claims).

Both gate 1.0. They are siblings, not nested. A passing benchmark with unbacked bio-claims still leaves the bio-inspired-LLM-harness framing exposed. A failing benchmark with strong mechanism validation is not 1.0-shippable either.

**Post-1.0 (the regression discipline).** Earned behavioral invariants can silently regress under major architectural changes — and CLAUDE.md's existing regression-guard discipline only covers engineering (CI grep allowlists, unit tests pinning constants). Behavioral claims have no equivalent forcing function today. The Roy-3c bisect is the canonical lesson: a non-code encoder drift broke a previously-settled mechanism without anyone noticing for weeks (the *cycle convergence vs divergence* working principle in [CLAUDE.md](../../CLAUDE.md)). A trigger-event-driven re-validation loop would have caught it.

The behavioral analog frame: **CLAUDE.md's "Architectural invariants" lists code rules that can't break. This doc lists behavioral claims that can't silently regress.** Same discipline, different evidence type.

**Format alignment with CLAUDE.md Principle 5.** Per [Principle 5 (regression-guard / experiment citation)](../../CLAUDE.md#working-principles-for-new-mechanisms), CLAUDE.md invariants declare `Regression guard:` (engineering) or `Roy experiment:` (behavioral). This doc's Earned entries use the same `Regression guard:` field format, so a single grep audits both surfaces. When a Tier 3 entry graduates, the `Regression guard:` line on the entry should match (or extend) what the underlying CLAUDE.md invariant declares.

Each entry below is an engineering invariant (or family) from CLAUDE.md whose bio-flavored framing makes an implicit behavioral claim. The entry's lifecycle:

1. **Pre-1.0 path:** `Pending` → graduate via cited Roy/equivalent experiment → `Earned <date>`. By 1.0, Tier 1 must be 100% Earned; Tier 3 entries that can't be earned get explicit dispositions (downgrade or [Dormant per Principle 2](../../CLAUDE.md#working-principles-for-new-mechanisms)).
2. **Post-1.0 path:** `Earned` → re-validated on triggering events → `Maintained <last_validated>`. If a trigger fires and the entry isn't re-run, it goes `Stale` (blocks next release until validated). If a re-run fails, `Broken (regression detected <date>)` (must fix or downgrade before release). When a mechanism is removed from the codebase, the entry goes `Retired <date>` but stays in the table as historical record.

## Three-tier scope

Not all 67 `[engineering]` invariants from CLAUDE.md need to graduate. Don't burn experiments on engineering scaffolding.

## Heartbeat walks

**Commands live in the standing runbook:** [docs/experiments/protocols/heartbeat_rerun_runbook.md](../experiments/protocols/heartbeat_rerun_runbook.md) — per-row harness invocations, the universal provenance preflight (worktree pinned at the release candidate, `assert_repo_interpreter`, config.json pinning), gates, and cost planning. This doc owns claims + status; the runbook owns how to re-run them. When a row graduates, its runbook section is added in the same PR.

## 2026-08-07 heartbeat walk — the FIRST EVER (1.1 cut-line item 5)

No row had ever been marked `Maintained` before this walk. Status after the
first pass — this section is the walk's honest ledger; its completion for 1.1.0
is recorded at the end of the section (2026-08-24), with the rows that stay
PARTIAL and the steps that were waived named there:

- **Maintained (1 row, the first ever):** Tier-1 #6 substrate-primary
  discrimination — re-validated 2026-07-29 by the [Exp 42b triage re-run](../experiments/42b_drive_pain_fold_revalidation.md)
  (40 sub-sims, post-fold identical to baseline, per-run
  `executed_git_hash` provenance after a first attempt was retracted for
  running the wrong checkout). See the row for the saturation caveat.
- **Stale pending H1 (1 row):** the Exp 45 real-hardware orient row. Its own
  re-run triggers FIRED — motors 2+3 (broken for the entire 1.0+ era, the
  measurement platform for every arm) were replaced ~2026-08-05, which is
  exactly the "any change to how motion is commanded / any shell-acoustic
  mod → re-sweep first" trigger. Per the roadmap hardware note: **every
  MAGNITUDE claim in this row (the ≈0.33 boundary, 45b/45c/45d/45e magnitude
  arms) is PROVISIONAL** — delivered shift is exactly what a degraded
  platform corrupts, and those arms rested on n=1 sessions. Direction
  findings are likely robust (sign-based, survive proportional gain error)
  but re-validation is [H1](../experiments/protocols/h1_healthy_hardware_doa_preregistration.md),
  which also settles the CONTESTED 0.57-vs-0.19 gain discrepancy
  (progressive-degradation hypothesis). Per lifecycle rule: Stale blocks the
  next release until validated — H1 is on the 1.1 critical path.
- **Earned, hardware-independent, heartbeat re-run pending (walk remainder):**
  Tier-1 rows 1/1b/3 (Exp 10 / Exp 37+38 / EC + Roy-5), the SEM pain cascade
  row, the Exp 48 operant row (sim-only — the motor breakage does not touch
  its evidence, which is scripted + sim), and Tier-3 #9 (narrative reflexes,
  Exp 09). These need their minor-version heartbeat re-runs; none of their
  non-heartbeat triggers have fired. Estimated remaining effort matches the
  roadmap's 1.5–2.5 wk.

### Walk continuation — 2026-08-13, Free Chapter (runbook §Free)

- **SEM pain → NAc cascade (Tier 1): GREEN.**
  `tests/substrate/test_sem_pain_cascade.py + tests/unit/test_pain_bus.py`
  — 37/37 passed at `a4bff3ef` (fix/1-1-doc-truth-findings, whose src
  changes do not touch the cascade path). Row's evidence is the test
  itself (EARNED de facto); heartbeat satisfied.
- **EC pattern completion / separation (Tier 1 row 3, unit-guard level):
  GREEN.** `tests/unit/test_ec_centroid_drift_fix.py +
  tests/unit/test_roy_5_cosine_localization.py` — 41/41 passed at
  `a4bff3ef`. Per the runbook, full behavioral re-derivation is required
  only on a fired **Re-run on:** trigger (none has).
- **Provenance caveat (deviation from runbook preflight §1):** these two runs
  executed from the working fix branch at `a4bff3ef`, not the pinned walk
  worktree (`f05c63aa`) — the branch's src changes verifiably do not touch
  either row's code path, but the walk's value is UNIFORM attestation of one
  candidate commit. Both rows are pytest-only (seconds): **re-attested at the
  1.1.0 RC commit `88739318` on 2026-08-24** — SEM cascade + PainBus
  (`tests/substrate/test_sem_pain_cascade.py` + `tests/unit/test_pain_bus.py`)
  **37/37**, EC guards (`test_ec_centroid_drift_fix.py` +
  `test_roy_5_cosine_localization.py`) **41/41**. `88739318` is the release
  commit's PARENT; the release commit's only `src/` delta is the `__version__`
  string, so this attestation carries.
- Remaining after this continuation: Sim-Short (Exp 09 reflexes, Exp 10
  persistence — needs a local LLM server + the pinned walk worktree),
  Fleet (Exp 42 done 2026-07-29; **Exp 48 awaits its re-pre-registered [satisfied 2026-08-14, then superseded by Exp 52 2026-08-25]
  gate**, not just a re-run), Cloud (Exp 37 per-model, budgeted).

### Walk continuation — 2026-08-18/19, Sim-Short Chapter (runbook §Sim-Short) — COMPLETE

Run on big-mac-mini, `Maxim-exp48-rebaseline` worktree, mistral-7b in the
auto-spawn/singleton topology, `llm.n_ctx 8192` (raised from 4096 mid-chapter
— see the apparatus note below). Sessions: p1 `20260818_010010`, p2
`20260818_233844`, p3 `20260819_000740`.

- **Exp 10 cross-session persistence (Tier 1 row 1): RE-VALIDATED — the
  cleanest pass this row has ever recorded.** Phase 2 resumed phase 1's
  session: the hippocampus opened at **exactly 108** (phase 1's closing
  store — the load is visible in the first enrichment trace's
  `hippocampus_size`), surfaced **3 memories/turn on 8 of 8 turns** (the
  ~3/turn gate), grew the store 108 → 535, and ACCUMULATED causal links
  (obs counts grew rather than re-deriving). Phase 3 (garden, same resume)
  showed **no negative transfer**: 2 dungeon-flavored log lines against 5
  enrichment passes, behavior fully garden-directed (`sense_presence`,
  flower-seeking plans), post-consolidation store 295 (session-end pruning
  working as designed).
- **Exp 09 narrative reflexes (Tier 3 #9): heartbeat GREEN with a stated
  caveat.** Firing confirmed — 7 reflex events incl. `reflex_attack` across
  an 8-turn dragon sim. The habituation/sensitization TRAJECTORY was NOT
  exercised: 0 `SEM_DAMAGE` events reached the body (the mistral-7b narrator
  dropped ~half its tool calls, so harm delivery never happened — a
  narrator-quality artifact, not a reflex defect). Original Exp 09 evidence
  stands; a full-trajectory re-attestation needs a longer run or a stronger
  narrator, noted as optional follow-up.
- **Apparatus note (the chapter's real cost and real yield):** completing
  these two rows surfaced and fixed FIVE apparatus defects — multi-runner
  contamination (#510), dead/wrong-narrator campaigns (#511 preflight),
  the D12 lock-inheritance deadlock (#517 hard-abort, #518 bounded lock,
  #519 nudge-proof abort clock), and **D13** (silent planning-turn drop →
  livelock, [bugs ledger](../bugs/README.md) — OPEN, fix contract recorded;
  the wedge cost is capped at ~4min by the abort meanwhile). The n_ctx
  raise 4096 → 8192 is an operator-config alignment (resume-enriched
  prompts ran at 87% window occupancy, degrading small-model tool-format
  fidelity — the D13 trigger); it is config-layer, not experiment-pinned
  apparatus. Gate evidence above was measured ONLY on completed, unwedged
  runs.
- **Walk COMPLETE for 1.1.0 (2026-08-24) — every chapter ran; two rows stay PARTIAL;
  two hardware-chapter steps waived, stated here.** Big-Model chapter RAN 2026-08-21
  (Exp 37 Qwen32B re-fire → instrument finding [L8](../limits/README.md); row stays
  PARTIAL; record at `docs/experiments/data/37_heartbeat_1.1_qwen32b.jsonl`).
  Hardware chapter: H1 Parts A–C ran (yaw_verify, sweep, the `_big` delivered-shift
  block 2026-08-24, PR #535); runbook step 4 (`orient_merge_arm.py`, hardware-free)
  RAN at the RC on 2026-08-24 — **PASS**, left 1.00 / right 1.00 / merged 1.00
  (magnitude 1.00) on the July s1b + m1 NAcs
  ([record](../experiments/data/heartbeat_1_1/45_merge_arm_rc_2026-08-24.txt));
  step 3 (`live_3_learn.py` learned-policy probes, robot + audio) did NOT run —
  waived because the fired trigger was the instrument/platform, not the
  mechanism, and the 2026-07 probe evidence stands; run it at the next live session. Sim-Short DONE
  (records committed, PR #536). Exp 42 is attested by the 2026-07-29 42b re-run
  (accepted at the walk opening, not re-run inside the 1.1 window). Exp 48 stays
  PARTIAL under gate v2 (superseded 2026-08-25 by Exp 52's Earned row). No row is `Stale` or `Broken`, which is the release rule.
  Pytest-only rows re-attested at the RC parent commit above. Fleet: Exp 42 Maintained
  2026-07-29; Exp 48 re-baselined 2026-08-18 under gate v2 (PARTIAL —
  see its row).

### Tier 1 — Thesis-load-bearing (MUST graduate, or 1.0 is dishonest)

These are the bio-claims of the product itself. Shipping 1.0 without cited experiments for these undermines the [framing strategy](../../CLAUDE.md) (bio-inspired LLM harness, not academic cog-sci).

| Claim | Bio-mechanism | Status |
|---|---|---|
| Cross-session memory persistence (substrate carries memory across sessions) | Hippocampus persistence + RECALL reconsolidation | **EARNED** — 3 memories/turn on resume per [Exp 10](../experiments/10_cross_session_enrichment.md); confirmed unaffected by the 2026-06-06 Exp 37 fire. **Re-run on:** encoder swap, hippocampus persistence schema change, minor-version heartbeat. **Regression guard:** [docs/experiments/10_cross_session_enrichment.md](../experiments/10_cross_session_enrichment.md) + [tests/integration/test_memory_hub.py](../../tests/integration/test_memory_hub.py). **S4 data status (2026-08-24): the ORIGINAL Exp 10 raw records are LOST** — the experiment logged to `/tmp/v1_phase{1,2,3}.jsonl` (see the doc's commands) and no copy was ever committed; macOS clears `/tmp`. The surviving raw record is the 2026-08-18/19 heartbeat re-run that RE-VALIDATED this row (sessions `20260818_010010` / `20260818_233844` / `20260819_000740`, mistral-7b) — **S4 copy COMMITTED 2026-08-24** at [docs/experiments/data/heartbeat_1_1/sim_short_2026-08-18/](../experiments/data/heartbeat_1_1/sim_short_2026-08-18/) (hippocampus 108 → 535 → 295 re-derived from the copied `aut_hippocampus.json` files). |
| Cross-session behavioral delta from substrate-carried memory (under LLM-AUT) | Wire-A annotation + Wire-1 annotation + NAc reward bias → action selection | **PARTIAL — reframed (SETTLED for 1.0; cross-session behavioral-*override* claim pulled, mechanism-reality + memory-persistence stand). ⚠ REPRODUCIBILITY CAVEAT (2026-08-22): the four-model Goldilocks map below rests on fires whose serving environment was never recorded, and that environment has since changed. Re-running the ANCHOR fire's identical commit (`54d60226`) on its identical seeds today yields Arm A = 0.710 (n=5, SD 0.119) where June measured 0.420 (n=5, SD 0.27) — the code is exonerated and the baseline moved anyway. **The Qwen32B +1.43 SD PASS therefore cannot currently be re-derived: unverifiable, not refuted.** Cite the Goldilocks framing WITH this caveat. Details, data, and the required record-stamping fix: [L8](../limits/README.md).** per [Exp 37](../experiments/37_cross_session_graduation.md) + [Exp 38 counter-prior](../experiments/38_counter_prior_substrate.md), with a **Goldilocks-zone** four-model evidence base ([cross-model results](../experiments/37_cross_model_results.md)). **Qwen14B fire (2026-06-06):** primary Δ = −0.06 SD (FAIL), 0/4 corroborating — priors too weak to leverage substrate. **Qwen32B fire (2026-06-08):** primary Δ = **+1.43 SD (PASS)**, **2/4 corroborating PASS**, robustness PASS — substrate-transfer signal IS detectable; priors in the sweet spot. **Mistral24B fire (2026-06-11):** primary Δ = −0.40 (FAIL, ceiling effect) — Arm A = **1.000, SD = 0.000** (fresh-agent priors already solve the cradle task perfectly, no headroom for substrate). **DeepSeek-R1-Distill-Qwen-32B fire (2026-06-13, reasoning axis):** primary Δ = **+2.11 SD (PASS, largest of any fire)**; Arm A = 0.259 (reasoning overlay LOWERS fresh-agent priors → deeper in the zone, MORE headroom); **secondary 1/3 PASS — Wire-A ablation shrinks B's delta by +1.13 SD, the FIRST clean single-mechanism attribution across all four fires.** **The four fires form a Goldilocks zone:** substrate-transfer signal is detectable only when the base LLM's priors leave headroom between first-encounter behavior and optimal behavior; that headroom is governed by training method at least as much as parameter count (Mistral24B at 24B hits the ceiling Qwen32B at 32B does not; the R1 reasoning overlay widens the headroom vs its Qwen32B base). **Bio-mechanism attribution:** 0/3 ablations cleanly attribute at Qwen14B/Qwen32B/Mistral24B, but **R1's reasoning overlay surfaces Wire-A (cluster-bias substrate-voice annotation) as the carrying channel (+1.13 SD shrinkage PASS)** — a mediation present-but-buried at Qwen32B (+0.56 suggestive). Reasoning-trained models are *better* substrate consumers and make the mechanism statistically legible (bucket R-A, opposite of the pre-registered reasoning-drowns-substrate worry). **Combined 1.0 framing:** substrate carries cross-session memory at all scales (Exp 10 + all four fires); whether the carried memory produces a measurable behavioral shift depends on the Goldilocks condition; single-mechanism attribution is supported ONLY under the R1 reasoning model (Wire-A), not under the non-reasoning models. The strong "substrate drives action selection via specific bio-mechanisms" claim is **still pulled from 1.0 bio-framing** (the Arm-C confound — any resumed prior shifts behavior up — gates clean fire-specificity attribution in every fire including R1); substrate-driven action selection stays deferred to Exp 38 substrate-primary work post-1.0 (the Goldilocks finding *strengthens* the Exp 38 case — to demonstrate substrate-driven behavior unambiguously you must remove the LLM-prior confound, since under LLM-AUT the signal is only visible in a narrow prior-strength band). **Exp 38 counter-prior (2026-06-13) — the matched-control confound-removal — has now run and confirms the pull empirically** ([38_counter_prior_substrate.md](../experiments/38_counter_prior_substrate.md)): a world where the prior is *wrong* (a `hearth` whose `warm_self` is inverted to hurt), with a matched safe-fire control so the cross-scenario **interaction** isolates substrate-specificity from the Arm-C "any resumed prior shifts behavior" confound this entry flags. Result: **dominance across all 4 frontier models** (Sonnet / GPT-4o / DeepSeek-V3 / R1) — carried substrate (including direct cross-session pain at this exact hearth) does **not** override the wrong `fire→warm` prior. **R1 is again the sharpest:** its substrate is causally load-bearing (turning off Wire-A / Wire-1 / NAc-bias drops deceptive-hearth warming *below* a fresh Arm A — clean ablation attribution, interaction +2.25 SD) but it **amplifies the carried prior association, not the corrective pain** → maladaptive against a counter-prior. So the cross-session behavioral-*override* claim is now **tested-and-dominated, not merely deferred** — the bio-framing pull is empirically earned rather than precautionary. The R1 reasoning-amplification result opens a complementary 1.1+ "substrate-aware reasoning models" direction — now observed on *both* sides: adaptive amplification on the prior-aligned task (Exp 37, +2.11 SD) and maladaptive amplification on the counter-prior (Exp 38). **1.1 heartbeat re-fire, Qwen32B (2026-08-21) — INCONCLUSIVE; the instrument, not the claim, is what failed.** Complete design (60/60 rows, N=5, all arms). Result: **A = 0.750, B = 0.567, Δ = −0.56 SD FAIL** against the runbook's in-zone PASS gate — but **the baseline is what moved**: the prior fire had A = 0.420 → B = 0.800 (+1.43 SD), so Arm B now lands roughly where Arm A used to be and the headroom the Goldilocks effect depends on has largely closed. Two reasons this is NOT scored as a regression: (1) the comparison is confounded — ~145 PRs merged between fires and the row's own "matching git_hash" condition was not met, so per the verify-the-apparatus discipline "the substrate regressed" is unestablished; (2) the gate scores Δ, which moves when EITHER arm moves, so it cannot separate mechanism regression from baseline drift — filed as **[L8](../limits/README.md)** (BINDING), which also amends L6: headroom is a property of the (model, task, apparatus) triple, not of the model, and a per-model Goldilocks map has a shelf life. Secondary observations: the Arm-C confound this row flags **inverted** (C = 0.800 now sits inside A's band while B is the outlier below, where previously C ≈ B > A), and the Wire-A ablation moves B **up** 0.37 SD — the wires are load-bearing but unhelpful at this operating point. `sharp_rock` again produced structurally-absent primaries, reproducing the known four-fire scenario defect (confirmatory, not new). **Disposition: the row stays PARTIAL — status unchanged — with this fire recorded as an instrument finding.** Resolving it needs either a re-fire at the original git hash (separating apparatus drift from mechanism) or the pre-registered dose-response experiment L8 describes (hold model fixed, sweep A, gate on `B − C` rather than `B − A`). Raw: [docs/experiments/data/37_heartbeat_1.1_qwen32b.jsonl](../experiments/data/37_heartbeat_1.1_qwen32b.jsonl) (S4-committed 2026-08-24 from big-mac-mini `~/exp37_heartbeat/`; 60 rows, sessions 2026-08-19 → 2026-08-20, harness `git_hash dad9125b`). **Re-run on:** encoder swap, prompt construction change, Wire-A/Wire-1/NAc-bias mechanism refactor — re-validation re-fires Exp 37 with the same N=5 design at the matching git_hash across the model set (see L8: also record A/B/C and gate on `B − C`, or the re-fire is un-interpretable). **Regression guard:** [docs/experiments/37_cross_session_graduation.md](../experiments/37_cross_session_graduation.md) + [docs/experiments/37_cross_model_results.md](../experiments/37_cross_model_results.md) + per-fire data: [37_results.jsonl](../experiments/data/37_results.jsonl)/[.md](../experiments/data/37_results.md) (Qwen14B), [37_results_qwen32b.jsonl](../experiments/data/37_results_qwen32b.jsonl)/[.md](../experiments/data/37_results_qwen32b.md) (Qwen32B), [37_results_mistral24b.jsonl](../experiments/data/37_results_mistral24b.jsonl)/[.md](../experiments/data/37_results_mistral24b.md) (Mistral24B), [37_results_r1_distill_qwen_32b.jsonl](../experiments/data/37_results_r1_distill_qwen_32b.jsonl)/[.md](../experiments/data/37_results_r1_distill_qwen_32b.md) (DeepSeek-R1) + **Exp 38 counter-prior** [38_counter_prior_substrate.md](../experiments/38_counter_prior_substrate.md) + `docs/experiments/data/38_results_{sonnet,gpt4o,deepseek,r1_distill_qwen_32b}.jsonl` + analyzer at [scripts/analyze_exp37.py](../../scripts/analyze_exp37.py). |
| EC pattern completion / separation | EC centroid + threshold tracking NAc fallback | **EARNED** — Roy-2c Phase 4 + Roy-5 H1C (the centroid-drift invariant in [docs/agents/bio-memory.md](../agents/bio-memory.md)). **Data lost (2026-08-29 annotation):** Roy-2c / Roy-5 ran in 2026-05/06 before `docs/experiments/data/` existed; no record file was ever committed — the write-ups [20_roy_2c.md](../experiments/20_roy_2c.md), [27_ec_drift_phase_4_behavioral.md](../experiments/27_ec_drift_phase_4_behavioral.md) and [22_roy_5a.md](../experiments/22_roy_5a.md) are the surviving record, and the reproduction protocol is [protocols/20_roy_2c_reproduction.md](../experiments/protocols/20_roy_2c_reproduction.md). **Re-run on:** EC threshold / centroid-update change, encoder swap. **Regression guard:** [tests/substrate/test_p1_recognition.py](../../tests/substrate/test_p1_recognition.py) (the behavioral pattern-completion/separation guard the config pin [tests/unit/test_ec_centroid_drift_fix.py](../../tests/unit/test_ec_centroid_drift_fix.py) itself names; `requires_model_cache`, nightly lane) + [tests/unit/test_ec_centroid_drift_fix.py](../../tests/unit/test_ec_centroid_drift_fix.py) (threshold config pin, fast suite). |
| SEM pain → NAc cascade | ToolPainBridge direct-attribution + record_outcome | **RE-VALIDATED 2026-09-02 — UN-STALED, and the judgement is stated because this row's evidence is TESTS, not an archived measurement.** The trigger fired correctly: D53 changed both surfaces this row names. But the cascade's own path is `embodiment_failed → _V.NEGATIVE` — the FIRST branch of the new three-tier rule, which is semantically identical to the old `not success → negative`; the neutral tier D53 added sits in the `elif` the cascade never reaches, so D53 preserved this path by construction rather than by luck. Confirmed end to end on the fixed code: the full cited guard set passes (26 tests, incl. both named guards — `test_nac_records_negative_after_one_cascade` and `test_rusty_sword_slash_to_shatter_records_negative`), and `test_sem_pain_cascade.py::test_slash_at_low_durability_produces_negative_causal_link` asserts `links[0].outcome_valence == Valence.NEGATIVE` through the real bus. **What that does and does not buy:** it establishes that D53 did not regress the cascade. It does NOT add behavioural weight — the archived measurement still does not exist (see the data-lost note below), so this row's status continues to rest on in-suite tests, and only one of the three cited files exercises the new three-tier surface at all. Prior status: STALE 2026-08-31 — the D53 credit-path fix (`fix/neutral-tier-credit-path`) changed `ToolPainBridge.record_tool_complete` (widened to carry a three-tier `outcome_valence`) AND the `record_outcome` credit rule — this row names both verbatim in its own trigger list, and the fix commit did not originally mention it. Per the lifecycle rule a fired trigger without a re-run is `Stale`, which blocks the next release until validated. Re-run commands are in the sweep lesson [docs/lessons/ternary-collapse-sweep-2026-08-31.md](../lessons/ternary-collapse-sweep-2026-08-31.md). **Operator note (corrected 2026-09-01):** `~/.maxim` is NOT the contamination surface — both harnesses isolate `MAXIM_DATA_HOME` per (arm, seed). `benchmark_cradle_mother.py` WIPES its per-run sandbox; `benchmark_exp42_preference.py` does NOT, so clear `data/sim_sandbox/exp42_runs` before an Exp 42 re-run. And `--out` is append-only: give each config a FRESH path, or the analyzer silently pools the new cohort with the old one and reports their average (this happened on 2026-09-01). Prior status: **EARNED (de facto)** — Substrate P2 cascade test end-to-end on rusty_sword (the ToolPainBridge / PainBus layering invariants in [docs/agents/embodiment.md](../agents/embodiment.md), borderline-tagged behavioral). **Data lost (2026-08-29 annotation):** the P2 cascade validation was an in-suite run (2026-05, `tests/substrate` P2), never a record under `docs/experiments/data/`; the de-facto status rests on the tests below, not on an archived measurement. **Re-run on:** ToolPainBridge attribution change, `record_outcome` credit change. **Regression guard:** [tests/substrate/test_sem_execution_production.py](../../tests/substrate/test_sem_execution_production.py) (`test_nac_records_negative_after_one_cascade` — the in-suite P2 cascade) + [tests/substrate/test_sem_pain_cascade.py](../../tests/substrate/test_sem_pain_cascade.py) + [tests/unit/test_build_executor.py](../../tests/unit/test_build_executor.py) (`test_rusty_sword_slash_to_shatter_records_negative`). |
| Affordance concept transfer | Affordance LinguisticEncoder + EC pattern completion across entities | **PARTIAL — reframed (SETTLED for 1.0; mechanism PoC stands, behavioral-transfer claim pulled)** — the *mechanism* is validated as a PoC: cross-entity affordance clustering at **0.785 cosine** ("flame"/"fire" map to the same EC node, so NAc reward learned on one entity transfers to another via EC pattern completion). The *behavioral* claim (that this clustering produces a measurable cross-entity behavioral transfer in action selection) is **pulled from 1.0 bio-framing for the same reason as #2**: under LLM-AUT the model already knows the synonymy, so any substrate transfer is behaviorally redundant/dominated — a standalone behavioral graduation run would re-derive the Exp 37/38 dominance result, not new evidence. **Deferred to 1.1+**, where it becomes testable under the confound-free substrate-primary path (#6 / Exp 39): once the substrate drives action selection without the LLM, cross-entity transfer can be measured behaviorally. **Re-run on:** encoder swap, affordance-decomposition change, substrate-primary graduation. **Regression guard:** affordance encoder PoC ([CLAUDE.md affordance-transfer lesson](../../CLAUDE.md) + [similarity/decomposer.py](../../src/maxim/similarity/decomposer.py) `AFFORDANCE_STRATEGY`) + [temporal_credit_validation.md](../experiments/temporal_credit_validation.md). |
| Substrate-primary action selection | `NAc.propose_via_substrate` with confidence gate | **EARNED post-1.0 (v1.0.1) via [Exp 42](../experiments/42_substrate_primary_preference.md) — substrate-primary discrimination GRADUATE; was reframed-settled-PARTIAL at 1.0 (keystone test blocked by fixation, then Exp 41 VOID)** — graduation experiment pre-registered ([Exp 39](../experiments/39_substrate_primary_counter_prior.md), 2026-06-13), the keystone test. Harness shipped (PR #228) and the EC sensor-encoding blocker is **CLOSED** (`SensorEncoder` → EC `interoception` modality, wired into `propose_via_substrate`). Exp 39 sharpens the existing mechanism-level Phase-0 gate into a **behavioral counter-prior test**: run `cradle_prelinguistic_deceptive` (the harmful hearth) in `--aut-mode substrate-primary` and measure whether the substrate ALONE — no LLM in the action path — learns to stop warming the harmful affordance (within-session pain-avoidance curve + cross-session first-contact carry, NAc-ablation-attributable). This is the **only** test that removes the LLM-prior confound that dominated Exp 37/38; a positive result graduates this entry AND reframes #2 from "substrate dominated" to "substrate works, masked by LLM priors." Runs **locally, no LLM cost**. Disposition settles on Exp 39's frozen verdict (graduate / partial-graduate / reframe). One known pre-flight risk: the smooth-drift-collapses-to-one-cluster degeneracy ([13_phase0_harness_smoke.md](../experiments/13_phase0_harness_smoke.md)) — Exp 39 §3 gates on it. **Post-1.0 update (2026-06-17):** the substrate-primary triage surfaced exactly that failure mode — the bio-systems form EC clusters + NAc causal links + drive-conditioned proposals but **fixate on the first high-confidence link with no exploration** (deterministic argmax at `NAc.recommend_action`, no stochasticity/novelty/visit-count — [nac.py:1741](../../src/maxim/decisions/nac.py)), so Exp 39's frozen Phase-1 test cannot complete and #6 reframed-settled at 1.0. The forward path is [substrate_exploration_policy.md](archive/substrate_exploration_policy.md) (break the fixation, ~145 LOC, rides on the existing selection chokepoint — no new mechanism per the front-gate) validated by [Exp 41](../experiments/41_substrate_primary_exploration.md) (substrate-primary + counter-prior + exploration — does the unmasked substrate learn from embodied pain alone to override its own built-in drive-affinity prior?). PASS/PASS on Exp 41 moves this entry toward EARNED; FAIL/FAIL settles it honestly as "fixation is deeper than selection." **Exp 41 FIRED 2026-06-19 → VOID (neither — entry UNMOVED):** the substrate-primary embodied-learning loop is real (try → pain → avoid; harm → 0 in both arms, no LLM in the action path), but the exploration 2×2 couldn't isolate exploration's role — drive-tuning made the harmful action drive-tempting, so the deterministic arm engaged-and-learned it too (A_dec ≈ B_dec; exploration nudged harm the *wrong* way), and the harm-rate metric floored under try-once dynamics. The 0.10 VOID floor was not lowered post-hoc. #6 stays reframed-settled; the strong claim awaits the Exp 42 redesign (terminal-preference metric, not harm-rate) sketched in [Exp 41 §10](../experiments/41_substrate_primary_exploration.md). **Exp 42 FIRED 2026-06-23 → GRADUATE (post-1.0, v1.0.1) — substrate-primary discrimination EARNED.** The redesign (counterbalanced safe-vs-harm warmth sources, exploitation-phase `safe_pref` metric replacing the floored harm-rate, both-arms test) cleared every gate at 10 seeds/arm: `safe_pref` **0.984 / 0.975** (both ≥ 0.66), counterbalance identity-flip **+0.96** (preference tracks the *swapped* safety contingency, not source identity), C2 per-source nets harm < safe (both arms). The unmasked substrate — no LLM in the action path — learns from embodied pain to prefer the safe source and tracks the contingency across the counterbalance. **The gating-OFF ablation graduated IDENTICALLY** (`safe_pref` 0.984 / 0.965, +0.95 flip), so the enabling mechanism is **B8 delta-attribution (clean per-source credit assignment) + the pre-existing drive-affinity heuristic**, NOT the drive-gating crutch (B7 proved redundant → marked Dormant). The Exp 41 flooring + a bystander-pollution gap were root-caused and fixed (B6 introspection filter, B8 delta-attribution). **Scope:** substrate-primary is near-deterministic (LLM removed, SD ≈ 0.002–0.015), so this is a **mechanism-level** graduation — the substrate, given correct credit assignment, drives adaptive feedback-tracked behavior. The LLM-AUT *override* result (Exp 38/40 dominance) is the separate, complementary half and is untouched: substrate-primary *discriminates* where LLM-AUT *does not override* a wrong prior. [docs/experiments/42_substrate_primary_preference.md](../experiments/42_substrate_primary_preference.md) §Results. **MAINTAINED (2026-08-07 heartbeat walk, re-validated 2026-07-29):** the [Exp 42b triage re-run](../experiments/42b_drive_pain_fold_revalidation.md) (40 sub-sims, 10 seeds/arm × 2 counterbalanced arms × gating ON/OFF, 0 failed, 0 floored) reproduced the GRADUATE identically post-transition-drive-pain-fold: H1 0.996/1.000, C1 +0.996, C2 PASS, harm_net −0.25 every seed, per-run executed_git_hash provenance. CAVEAT: safe_pref is saturated (SD 0.000 across all configurations), so the re-run supports 'did not break discrimination' but cannot detect a moderate regression — a future degradation arm needs a more sensitive statistic. **Re-run on:** SensorEncoder / EC-interoception change, NAc confidence-gate change, substrate exploration-policy change, B8 delta-attribution change, **`record_outcome` credit-rule change (added 2026-08-31)**. That last trigger was MISSING until the D53 sweep: all four collapsed credit sites were live in this experiment's path (`--aut-mode substrate-primary`, `drive_relief_only=False`, no `MAXIM_OPERANT_ONLY_CREDIT`), and no trigger named the rule, so a credit change could alter this result without the ledger noticing. **RE-VALIDATED 2026-09-01 post-D53 — GRADUATE reproduced; the credit fix did not break discrimination.** 20 runs/config at `ebc37743`, clean tree on `main`: treatment `safe_pref` **0.996 / 1.000**, gating-OFF **0.996 / 1.000**, C1 identity flip **+0.996** in both — every post-fix figure above its 2026-06-23 counterpart (0.984/0.975 treatment, 0.984/0.965 gating-OFF), and the gating-OFF ablation again graduates identically. **C2 is UNESTABLISHED, not passed:** the live records were lost to an operator error and rebuilt from the surviving per-run sandboxes; `harm_net`/`safe_net` derive from a live set-diff over `substrate_telemetry_*.jsonl` that cannot be attributed post hoc. Arm assignment was VERIFIED against independently-captured `n_contact` means (treatment 277/106, gating-OFF 279/108 — all four match) rather than trusted from the recovery's session ordering, per 42b's "did the actuation actually happen?" rule. **The saturation caveat is now STRONGER — `safe_pref` SD is 0.000 on both arms in both configs**, so this supports "discrimination did not break" and cannot detect a moderate regression. **Unexplained, worth a look:** `n_contact` rose 66→277 (arm a) and 75→106 (arm b), a 4x vs 1.4x asymmetry the saturated primary statistic cannot see. Data: [42d53_results.jsonl](../experiments/data/42d53_results.jsonl) / [42d53_results_gateoff.jsonl](../experiments/data/42d53_results_gateoff.jsonl) (operator note, corrected 2026-09-01: `~/.maxim` is NOT the contamination surface — both harnesses isolate `MAXIM_DATA_HOME` per (arm, seed). `benchmark_cradle_mother.py` WIPES its per-run sandbox; `benchmark_exp42_preference.py` does NOT, so clear `data/sim_sandbox/exp42_runs` before an Exp 42 re-run. `--out` is append-only: give each config a FRESH path or the analyzer silently pools cohorts). Caveat that bounds what the re-run can show: `safe_pref` is saturated (SD 0.000), so a PASS supports "discrimination did not break" but cannot detect a moderate regression. **Regression guard:** [Exp 39 pre-registration](../experiments/39_substrate_primary_counter_prior.md) + [Exp 41 pre-registration](../experiments/41_substrate_primary_exploration.md) + [substrate_exploration_policy.md](archive/substrate_exploration_policy.md) + substrate-primary harness ([simulation/substrate_telemetry.py](../../src/maxim/simulation/substrate_telemetry.py) + `propose_via_substrate` in [agent_loop.py](../../src/maxim/runtime/agent_loop.py)) + data: [docs/experiments/data/42_results.jsonl](../experiments/data/42_results.jsonl) / [42_results_gateoff.jsonl](../experiments/data/42_results_gateoff.jsonl) (2026-06-23 GRADUATE) + [42b_results.jsonl](../experiments/data/42b_results.jsonl) / [42b_results_gateoff.jsonl](../experiments/data/42b_results_gateoff.jsonl) (2026-07-29 re-validation, per-run `executed_git_hash`). |
| Real-hardware substrate sensorimotor learning (orient-to-center, Layer 1) | NAc `cluster_reward_bias` + `recommend_action` (substrate-primary, no LLM in action path) + `potential_diff` relief credit on live DoA | **EARNED post-1.0 (2026-07-16) via [Exp 45](../experiments/45_reachy_orient_live.md); RE-VALIDATED 2026-08-08 via H1 sessions 1+2 (un-staled)** — the row's own re-run trigger fired: motors 2+3, broken for the ENTIRE 1.0+ era, were replaced ~2026-08-05 (roadmap hardware note), so every arm below was measured on a degraded platform. **All MAGNITUDE claims (the ≈0.33 boundary, 45b/45c/45d/45e magnitude arms) are PROVISIONAL** — delivered shift is exactly what a degraded platform corrupts, and those arms rested on n=1 sessions; direction findings are likely robust (sign-based). H1 re-validates on healthy hardware. **H1 session 1 (2026-08-08) — sensor half PASSED:** version-verified two-geometry sweep on the repaired platform: gain 0.578 front (replicated 0.575) / 0.645 displaced, R² 0.984–0.996, NO staircase, fold captured — progressive-degradation confirmed, CONTESTED banner resolved, H2 did not fire (boundary shift ~0.01 az; magnitude YAML stays frozen); d(head)/d(body)=+1.024. Row stays STALE because the delivered-shift half is still unmeasured: session 1 also found a constant motor-zero miscalibration on the repaired platform (all-zero commands plateau at roll +18°/pitch +9°/yaw +6°, daemon-confirmed — per-motor recalibration of the replaced motors 2+3 required) plus a retained-axes ratchet in the controller (probe contamination; envelope numbers retracted). **UN-STALED 2026-08-08 — H1 session 2:** per-motor recalibration fixed the zero attractor (verified level, yaw_verify improved 0.934→0.951), the healthy sweep passed (gain 0.578 replicated, no staircase), and the delivered-shift half is now MEASURED through the production affordance path (39 turns: turn_left 0.890±0.042, turn_right 0.937±0.075, position-dependent load characterized, credit chain sign-correct on hard cases incl. a fold-crossing transition). **_big delivered shift MEASURED 2026-08-24 (n=8/side, production affordance path, continuous audio): 0.943 ± 0.001 (left) / 0.942 ± 0.003 (right) of command, Δaz +0.49 ± 0.07 left / −0.40 ± 0.08 right — the provisional flag on the `_big` delivered-shift magnitude is CLEARED.** One session, within-session population SD — cross-session replication outstanding. The `_big` YAML magnitudes stay frozen (H2 did not fire; the right-side Δaz reads ~20% under the modeled −0.50 while delivered BASE rotation is symmetric — not base actuation; head-pose drift (D30), head ride-along on the production path (unmeasured in-block), sensor tilt and source placement remain candidates — a mirrored-source repeat recording head pose per turn is to be pre-registered, not a retune). One folded DoA reading reached the credit path in 18 turns (L10, now observed in production; fold guard filed as D31) and head roll drifted to −8.7° under repeated body-only commands (D30). Sensor-side magnitude claims (the ≈0.33 boundary) are re-validated by the healthy sweep ([H1 results](../experiments/protocols/h1_healthy_hardware_doa_preregistration.md), Part C follow-up). Original evidence: the project's first learned policy on physical hardware (Reachy Mini). Three pre-registered arms, all PASS: (1) **learning curve** — frozen-policy probe 0.00 → 1.00 by trial 10 (s1b), replicated on a fresh substrate at 0.50→1.00 greedy chance-to-perfect (m1, probe 1.00 by trial 15); (2) **cross-session transfer** — loaded NAc probes 1.00 at trial 0, all 31 greedy actions correct (s2); (3) **merge/fleet mechanics** — two independently-trained NAcs `nac_merge` to a 1.00 policy ≥ both inputs **[ARM 3 DOWNGRADED 2026-09-01 — VACUOUS GUARD, see D62. The sentence is true as written; its evidential status is not. Re-run on the real recorded NACs, the gauntlet PASSES against `nac_merge` replaced by `return left` AND by `return right` — only `return {}` fails — and the real merge is argmax-identical to `return left` in all four bins. Both parents share `agent_id` and a hardcoded 4-bin symbolic cluster space by construction, so D43 structurally cannot fire: "independently trained" is true, "independent agents" is not. `orient_merge_arm.py` is this row's cited `Regression guard:` under a `Re-run on: nac_merge semantics change` trigger, and that trigger CANNOT FIRE. Arms (1) and (2) are unaffected and stand.]**, exported as the first gauntlet-passed substrate bundle (`queen_mind_orient_v0_1.zip`). Trials apparatus-generated (`--perturb`) with a contamination guard: ground truth never reaches NAc. **Extended 2026-07-16 by [Exp 45b](../experiments/45b_orient_magnitude.md) (PASS, magnitude 0.75) then [Exp 45c](../experiments/45c_flip_bins.md) (PASS, magnitude 1.00): the substrate also learns MAGNITUDE** — given a 2×2 action set it learns which way AND how far, from relief alone, no LLM. **Evidential weight — read before citing: each magnitude arm is n=1 hardware session** (45b=mag2, 45c=flip1), scoring a metric quantized to {0, .25, .5, .75, 1.0} over 4 bins — against the 10-seeds-with-SD standard of the row above. 45c's own sim scores 0.75 on 2 of 6 seeds, so a single draw had ~1/3 chance of landing PARTIAL; "stable across 13 probes" are autocorrelated re-reads of one converged policy, not independent trials. **Replication DONE 2026-07-23 via [Exp 45d](../experiments/45d_magnitude_replication.md) — the outstanding-flag is CLEARED.** Three clean flip-bins seeds (boundary frozen at the derived 0.330 via `--az-gain 0.55`): direction **unanimous 1.00**, magnitude **{0.75, 1.00, 0.75}** (mode 0.75, mean 0.83) — matching the sim's ⅔-at-0.75. The magnitude ceiling is characterized as **single-far-bin cell-starvation** (per seed, exactly one far bin's big-turn cell never gets a positive exploration sample; e.g. seed 3 far_right learned turn_right_big at +0.585 while far_left stayed 0.0), a **coverage limit of per-cell tabular argmax, not a capability limit** — seed 2 reached a clean 1.00/1.00 when both far cells were covered. **45d also added the arm 45/45b/45c never ran — cross-session transfer of the *magnitude* policy:** seed 2's 1.00/1.00 NAc loaded into a fresh session probes **1.00/1.00 at trial 0** and holds across all 9 probes (Exp 45's cross-session arm carried direction only; this extends it to magnitude). The starvation ceiling is the data-grounded motivation for the Layer-2 continuous-readout work (population-vector / cerebellar gain-adaptation, log/Weber-Fechner magnitude geometry) — **now run: [Exp 45e](../experiments/45e_orient_s4_population_readout.md) (2026-07-27) resolves the seed-3 starvation via the population-vector readout, and the fix is READOUT-INDEPENDENT** (population readout directs greedy execution to the starved cell's borrowed magnitude → the cell earns real credit → plain `argmax` then reads magnitude 1.00: `far_left turn_left_big` 0.93 ≫ `turn_left` 0.42). So the mechanism is an *exploration bootstrap*, not a readout cosmetic. **n=1, seed 3 only — multi-seed replication of the bootstrap is outstanding**, and the seed-3 left/right asymmetry has a known left-motor hardware component (replacement pending); population fixed magnitude-appropriateness, NOT centering (settled residual unchanged). The DIRECTION arms (Exp 45) are not affected by the n=1 caveat. 45c closed the magnitude gap by fixing the STATE REPRESENTATION rather than the learning: the near bin straddled the derived decision boundary (`gain*(|Δbig|+|Δnormal|)/2` = 0.33), so it held two opposite correct answers; moving the boundary took magnitude 0.75 → 1.00 with every bin decisive (near_right: normal +0.352 vs big −0.570 — the substrate learned the big step is *harmful* there) and greedy turned-toward rising 0.286 → 1.000 (starting BELOW chance — the strongest learned-vs-servo separation in the line). **Method correction (2026-07-16):** an earlier "the XVF3800 DoA is a tracking estimator" finding was **retracted** — it was an artifact of a `head=None` bug that counter-rotated the head-mounted mic array (mics rotated 0.32 rad for a 0.9 rad body command). **The result arms are unaffected**: direction/cross-session/merge are sign-based or substrate-only and survive a proportional gain error; magnitude required the fix (mag1 incoherent → mag2 PASS immediately after). True sensor characterization post-fix: gain 0.562 az/rad, 0.23 s convergence, stable. **Re-run on:** NAc `cluster_reward_bias`/`recommend_action` change, `nac_merge` semantics change, orient-affordance YAML change, DoA front-end / Reachy transport change, **any change to how motion is commanded (verify `d(head)/d(body)` ≈ +1.0 via [yaw_verify.py](../../scripts/orient_backbone/yaw_verify.py) first — the mics must actually rotate)**, any shell/acoustic mod (re-sweep first via [doa_sweep.py](../../scripts/orient_backbone/doa_sweep.py)). **Regression guard:** [scripts/orient_backbone/delivered_shift_block.py](../../scripts/orient_backbone/delivered_shift_block.py) + [docs/experiments/data/h1_partc_big_block.jsonl](../experiments/data/h1_partc_big_block.jsonl) (the `_big` delivered-shift evidence, re-runnable) + [docs/experiments/45_reachy_orient_live.md](../experiments/45_reachy_orient_live.md) + [docs/experiments/45d_magnitude_replication.md](../experiments/45d_magnitude_replication.md) + [docs/experiments/45e_orient_s4_population_readout.md](../experiments/45e_orient_s4_population_readout.md) (S4 population-readout resolution of the starvation ceiling) + [scripts/orient_backbone/](../../scripts/orient_backbone/) (live_3_learn.py probes incl. `--readout population` + orient_merge_arm.py gauntlet — re-runnable end-to-end; merge arm needs no hardware) + durable data at [docs/experiments/data/45_orient_sessions.jsonl](../experiments/data/45_orient_sessions.jsonl) / [45_doa_sweep_baseline.jsonl](../experiments/data/45_doa_sweep_baseline.jsonl) / [45d_magnitude_replication.jsonl](../experiments/data/45d_magnitude_replication.jsonl). **Provenance disclosure (2026-08-29, surfaced by `scripts/lint_prereg_precedes_data.py`'s clean-tree rule):** the `h1_partc_big_block.jsonl` `_big` block (run `20260824T213320Z`, `b01a6589`) stamps `working_tree_dirty_src_scripts: true` with no allowance — the `--allow-dirty` door did not exist. Grandfathered by explicit entry; a re-run under the refusal rule is owed (item 16.7) before this row's delivered-shift evidence is cited again. |

| Operant orienting, embodied (a caregiver teaches a driveless infant to orient) | `NAc.credit_operant_reward` operant-only mode + extero/intero seam (per-modality encode → direction in its own `audio` EC cluster → operant credit routed there) | **SUPERSEDED 2026-08-25 by the Exp 52 row below (relief-sourced credit on the shuffled apparatus: GRADUATE). This row stays as the apparatus case study — v1 contest → v2 re-baseline → sweep → the shuffle — and its PARTIAL verdict stands for the constant-credit apparatus it measured.** Prior status: PARTIAL — RE-BASELINED 2026-08-14 (apparatus-v2, gate v2; no longer Stale/CONTESTED-unresolved). The re-baseline the 2026-08-11 contest demanded has run on the fixed apparatus (locks + fresh sandboxes #510, narrator-liveness preflight #511, gate v2 frozen pre-data #512): 12 seeds/arm, exposure-matched 48 turns/seed, `executed_git_hash a9ea66fe`, deterministic greedy-narrator regime (~712s/run — a reproducibility property; the v1 variance was thrashing). **MOTHER-TAUGHT re-earned decisively: taught late 0.649 vs no_feed 0.167 (+0.482 vs the 0.20 margin; control structurally BELOW chance — anti-phase alternation, now an interpretable phenomenon). LEARNED-v2 FAIL: late 0.649 vs 0.65 (0.001 miss, not retuned per S7) and rise +0.104 vs 0.15 from the act1-only bin** → the pre-registered PARTIAL branch. **Sweep COMPLETE (2026-08-18) and decisive in the third direction: every cell across ew 1.5/1.0/0.75 is an exact seed-invariant twelfth (taught late 8/12 → 8/12 → 4/12; control 2/12 → 2/12 → 6/12 — the CONTROL moved with zero teaching, and at 0.75 the arms INVERT). Directedness on the deterministic apparatus measures PHASE ALIGNMENT between turn and stimulus cycles; the credit tips the taught arm between phase-locked attractors. This QUALIFIES the re-earned mother-effect: real and causal, but credit-tipped attractor selection, not graded orienting skill. Next (sanctioned in the doc): randomised stimulus order — the contest's other pre-registered control — + a v3 gate frozen pre-data on the randomised apparatus. Sweep dimension exhausted; no further weights.** The v1 magnitudes (0.875/+0.211) stay retired as v1-apparatus artifacts; apparatus-v2 numbers are the reference baseline. Verdict + data: [Exp 48 §Apparatus-v2 re-baseline](../experiments/48_cradle_mother_seam.md). ORIGINAL CONTEST (2026-08-11, retained): magnitudes did not reproduce AND the mechanism was in conflict with how the apparatus behaved. The re-run failed LEARNED (+0.079 vs +0.15). Four configurations refuted all three natural explanations: not a code regression (matched n=12: graduation commit +0.055 vs current +0.079 — current marginally BETTER), not new thrashing (present at the graduation commit), not the narrator model (qwen32b worse at −0.059). What is established: the arm difference is real (taught 0.748 vs no_feed 0.399, +0.349; control flat across all four acts). What is CONTESTED is *why*: the exploration bonus is an anti-repetition term and, on this driveless body, the sole source of action — producing 50/50 L/R alternation against which a learned bias below ~0.11 is invisible; the mother credits the last action, a coin flip under alternation, so the teaching signal may be uncorrelated with the taught behaviour; the arms are exposure-unmatched (86 vs 48 mother-turns); outcomes are autocorrelated (0.62 vs 0.50) with both stimulus and agent alternating deterministically, so directedness may measure a phase relationship; and actions/turn is wall-clock ÷ 0.5s, i.e. a latency reading rather than a behaviour. What's broken: the gate's `early = mean(act1, act2)` folds the already-learned act2 into the baseline (learning completes act1→act2 at +0.133, then saturates), and the plateau height is capped by a ~20% no-move floor from ~60 near-balanced turn actions per mother-turn. **The 1.1 block is LIFTED (2026-08-14): the re-baseline against a fixed apparatus has run, under the S7-re-pre-registered gate v2 (#512) — bin alignment (act1-only early) + ceiling clause + exposure contract all landed as demanded.** The row is PARTIAL, not Stale: the mother-effect is re-earned at v2; the LEARNED ceiling is under the pre-approved sweep. NOT retracted. Full analysis + durable data: [Exp 48 §1.1 heartbeat re-run](../experiments/48_cradle_mother_seam.md). ORIGINAL (2026-07-23) below, retained for the record: **EARNED post-1.0 via [Exp 48](../experiments/48_cradle_mother_seam.md)** — the embodied `cradle_mother` sim that measured at CHANCE in [Exp 46](../experiments/46_operant_orient_creche.md) now GRADUATES after the [extero/intero seam](archive/exteroception_interoception_seam.md) (PR #411). 12 seeds/arm, substrate-primary (no LLM in the action path), 56 turns: **taught** late-bin directedness **0.875** with a clean developmental rise 0.51→0.82→0.85→0.90; **no_feed** control flat at chance **0.448**. Both pre-registered gates PASS with margin — LEARNED (0.875 ≥ 0.65, rose +0.211 ≥ 0.15), MOTHER-TAUGHT (+0.427 ≥ 0.20, > 2× margin). The infant learns to orient toward the sound PURELY from the mother's contingent feeding, with NO intrinsic orient drive. **Scope (narrow by pre-registration):** this is the *embodied confound-check* that the seam's de-dilution payoff survives the sim machinery (narrator, gates, turn caps) that pinned Exp 46 at chance — NOT a re-derivation of the mechanism (that is the scripted `TestMultiDriveOrientLearnsEndToEnd` + Exp 46's scripted arms). It complements the Reachy row above (Exp 45, real-hardware orienting) and the substrate-primary discrimination row (Exp 42): together they show the substrate learns sensorimotor policies — direction+magnitude on hardware, safe-vs-harm discrimination, and operant orienting from a caregiver — with no LLM in the action path. **Held constant:** the `turn_left,turn_right` tool whitelist (out-competing a snowballing always-succeed tool is the separate, still-open credit-on-progress question). **Re-run on:** extero/intero seam change (ModalityChannel registry, `recommend_action(current_clusters=)`, `record_outcome(clusters=)` credit routing), `NAc.credit_operant_reward` / `set_pending_operant_action` change, `infant_operant` body or `cradle_mother` arc change, SensorEncoder/EC-modality change. **Regression guard:** [docs/experiments/48_cradle_mother_seam.md](../experiments/48_cradle_mother_seam.md) + [scripts/benchmark_cradle_mother.py](../../scripts/benchmark_cradle_mother.py) + [scripts/analyze_cradle_mother.py](../../scripts/analyze_cradle_mother.py) (pre-registered verdict constants) — **re-run with `--credit constant`** (2026-08-25: the harness default became Exp 52's relief-sourced credit; this row's rows are constant-credit) + [tests/unit/test_modality_seam.py](../../tests/unit/test_modality_seam.py) (the seam mechanism it rides on). |
| Caregiver-taught orienting through hunger relief (an infant with NO orient drive learns to turn toward the mother's voice because being fed relieves its hunger) | `cradle_mother.reactive_mother_tick(credit="relief")` — operant credit = sign(Σ `drive_comfort_progress`) over the drives the feed touched, scored on the recipient via `tool_bridge._drive_potential_diff`, delivered through the one-turn `set_pending_operant_action`/`credit_operant_reward` trace; zero relief → no credit; extero/intero seam routes the credit to the direction-bearing cluster | **RE-VALIDATED 2026-09-02 — UN-STALED.** The trigger fired and Phase B was re-run whole on the fixed code (`main` @ `2c9f1579`, big-mac-mini, 36 runs / 3 arms x 12 seeds, clean tree, `mock: False`, fresh `--out`): under the SAME frozen v3 gate, **taught late 0.837 / satiated 0.413 / no_feed 0.413 — LEARNED, MOTHER-TAUGHT and HUNGER-NECESSARY all PASS, GRADUATE reproduced.** Deltas from the original (taught −0.042, satiated −0.028, no_feed 0.000) sit inside the per-seed SD, so the fix did not move the result. Phase A was not re-run (scripted; its credit does not route through `record_outcome`). Data: [52d53_phaseB_embodied.jsonl](../experiments/data/52d53_phaseB_embodied.jsonl). **One sentence of the original write-up is RETRACTED, not the result:** per-seed values are not reproducible run-to-run (only 4 of 36 cells identical; per-seed late correlates at r = +0.66 taught / +0.29 no_feed / −0.24 satiated), so the "one weak seed (taught 48, late 0.54)" story below — which reads **0.667** in the re-run — must not be cited as a property of that seed. Arm means replicate; per-seed narratives from this apparatus do not. Prior status: STALE 2026-08-31 — the D53 credit-path fix (`fix/neutral-tier-credit-path`) changed how `drive_potential_diff` is consumed (measured exactly-zero progress no longer takes the tool-success floor) and the `record_outcome` credit rule. Per the lifecycle rule a fired trigger without a re-run is `Stale`, which blocks the next release until validated. Re-run commands are in the sweep lesson [docs/lessons/ternary-collapse-sweep-2026-08-31.md](../lessons/ternary-collapse-sweep-2026-08-31.md). **Operator note (corrected 2026-09-01):** `~/.maxim` is NOT the contamination surface — both harnesses isolate `MAXIM_DATA_HOME` per (arm, seed). `benchmark_cradle_mother.py` WIPES its per-run sandbox; `benchmark_exp42_preference.py` does NOT, so clear `data/sim_sandbox/exp42_runs` before an Exp 42 re-run. And `--out` is append-only: give each config a FRESH path, or the analyzer silently pools the new cohort with the old one and reports their average (this happened on 2026-09-01). Prior status: **EARNED 2026-08-25 via [Exp 52](../experiments/52_nurture.md)** (pre-registered, frozen gates, amendments 1–2 pre-data). **Phase A** (scripted, 8 seeds × 600 ticks): taught **0.892** vs satiated 0.496 / yoked 0.496 / no_feed 0.496 — satiated ≡ no_feed to the digit (a feed without need had zero effect); yoked took every taught credit on random actions and stayed at chance. **Phase B** (embodied `cradle_mother`, apparatus v3 = shuffled stimulus order, 12 seeds/arm, exposure-matched): taught late **0.878** (act1 0.61 → 0.89), satiated **0.441** (fed 35% of turns, credited 0%), no_feed 0.413 — LEARNED / MOTHER-TAUGHT / HUNGER-NECESSARY all PASS under gate v3; L2 apparatus check clean (per-seed SD 0.08–0.13; the v2 seed-invariant twelfths are gone) and S3 in-sim assertions OK. One weak seed (taught 48, late 0.54) is explained by the L1 margin instrumentation: median `|learned_margin|` 0.12 at the ~0.11 visibility floor, exploration deciding 18% of its choices. **Scope, stated:** the sign-only credit discriminates nonzero-from-zero relief (hunger at feed ≈ 0.05–0.1), not hungry-vs-sated; no secondary reinforcement of the voice, no devaluation; one session, n = 12/arm — cross-session replication outstanding. This is the claim the 1.1 "Sensorimotor" cut was reopened for. **Re-run on:** `reactive_mother_tick` credit path change, `_drive_potential_diff`/`drive_comfort_progress` change, `NAc.credit_operant_reward`/`set_pending_operant_action` change, `infant_operant*` body or `cradle_mother` arc change, stimulus-order or explore-bonus change, minor-version heartbeat. **Regression guard:** [docs/experiments/52_nurture.md](../experiments/52_nurture.md) + [protocols/exp52_nurture_preregistration.md](../experiments/protocols/exp52_nurture_preregistration.md) + [scripts/orient_substrate/9_hunger_relief_orient.py](../../scripts/orient_substrate/9_hunger_relief_orient.py) (Phase A, seconds) + [scripts/benchmark_cradle_mother.py](../../scripts/benchmark_cradle_mother.py) `--stimulus-order shuffled --credit relief` + [scripts/analyze_cradle_mother.py](../../scripts/analyze_cradle_mother.py) `--gate v3` + [tests/unit/test_cradle_mother.py](../../tests/unit/test_cradle_mother.py) (relief/satiated/constant credit pins) + data [52_phaseA_scripted.json](../experiments/data/52_phaseA_scripted.json) / [52_phaseB_embodied.jsonl](../experiments/data/52_phaseB_embodied.jsonl). |
| Cross-context readout of a nursery-taught want on physical hardware (the Exp 52 infants' persisted NAc+EC, loaded unchanged, drive the Reachy Mini toward a speech source) | NAc `cluster_reward_bias` + `recommend_action(current_clusters=)` over `_encode_current_clusters` (audio ModalityChannel) on the live `DoAFeed` azimuth sensor; `NAc.load(apply_decay=False)` + `EntorhinalCortex.load` pair (D2); production `ReachyOrientMotorBackend` dispatch — no credit path on the robot | **STALE — trigger has now fired TWICE, one hardware block owed (updated 2026-09-02).** **(1)** The D53 credit-path fix changed `ReachyOrientMotorBackend.execute`, which this row names verbatim (it now reports a learning tier alongside mechanical success). **(2) NEW 2026-09-02:** the Exp 52 post-D53 re-run produced new persisted infant NAc+EC files, and `Exp 52 re-run (new files)` is also in this row's trigger list — so the re-run should read out from the NEW nursery files, not the August ones. They are on big-mac-mini at `~/exp52/phaseB/<arm>_seed<N>_ew1.5/sim_reports/<ts>/{aut_nac,aut_ec}.json`, which is the layout `cmd_manifest` expects. **This is the only row still blocking the 1.1.3 release, and it cannot be discharged off-hardware:** the manifest and sweep steps run offline, but the claim is *delivered directedness on the physical Reachy Mini*, so the `run` phase needs the robot. **Sequencing warning — do NOT run it as an isolated session:** L11's selected mitigation (A4 nonlinear gain, 1.1.4) changes `_sensor_embed`, which is also in this row's trigger list under "the representation is what transfers" — running 53b now and shipping A4 next re-stales it immediately. This re-run, L11's post-mitigation re-measure and 1.2's n=12 two-robot replication are one hardware block and should be planned as one. Prior status: EARNED, which this row names verbatim in its trigger list (it now reports a learning tier alongside mechanical success). Per the lifecycle rule a fired trigger without a re-run is `Stale`, which blocks the next release until validated. Re-run commands are in the sweep lesson [docs/lessons/ternary-collapse-sweep-2026-08-31.md](../lessons/ternary-collapse-sweep-2026-08-31.md). **Operator note (corrected 2026-09-01):** `~/.maxim` is NOT the contamination surface — both harnesses isolate `MAXIM_DATA_HOME` per (arm, seed). `benchmark_cradle_mother.py` WIPES its per-run sandbox; `benchmark_exp42_preference.py` does NOT, so clear `data/sim_sandbox/exp42_runs` before an Exp 42 re-run. And `--out` is append-only: give each config a FRESH path, or the analyzer silently pools the new cohort with the old one and reports their average (this happened on 2026-09-01). Prior status: **EARNED 2026-08-26 via [Exp 53/53b](../experiments/53_cross_context_readout.md); provenance-clean replication R1 2026-08-28 at tag `v1.1.0` (PASS 1.00 / 0.00 / 0.50 — the status now rests on R1)** (pre-registered; 53 amendments 1–2 pre-data; 53b = one declared change). **Provenance caveat on the original runs (disclosed 2026-08-28):** both 53 and 53b executed from a dirty tree at `68f9026e`, a branch commit not reachable from `main`; R1 re-ran the frozen protocol from a clean worktree at `df881b87` — see §Replication R1. **Instrument:** 120/120 live percepts pattern-complete into the nursery's three `audio` clusters (FAR-LEFT / CENTRE / RIGHT), for taught and control agents alike. **Exp 53** (δ 0.55 rad): taught direction correct 36/36, delivered directedness 0.75/0.75/0.75 with every miss the −0.2 target (overshoot) → the pre-registered APPARATUS verdict, no claim taken. **Exp 53b** (δ 0.30 rad = the `reachy_mini` body's own turn step): taught **1.00 / 1.00 / 1.00** (36/36 delivered toward, sign agreement 1.0), satiated **0.00** (no action 36/36), no_feed **0.50** (`turn_right` 54/54, learned bias 0 — persisted causal credit, side-blind). The +0.2 exploratory placement turns the wrong way 18/18 across both runs, predicted pre-data from the sweep: the nursery's representation is three bins, and the centre bin spans 0…+0.3. Seed 48 (mis-learned in the nursery) reads out mis-learned. **Scope:** readout, not learning — nothing credits the NAc on the robot; one session, one room, one source, 3 seeds/arm, fixed step, front hemisphere; nothing about magnitude or loudness. **Re-run on:** `_encode_current_clusters` / `_sensor_embed` / EC `pattern_complete_or_separate` change (the representation is what transfers), NAc/EC persistence format change, `DoAFeed` or `ReachyOrientMotorBackend` change, `infant_operant` body change, Exp 52 re-run (new files), minor-version heartbeat. **Regression guard:** [scripts/orient_backbone/exp53_cross_context_readout.py](../../scripts/orient_backbone/exp53_cross_context_readout.py) (`manifest` / `run --phase 1\|2 --delta 0.30` / `verdict`, dry-run offline) + the committed agent files [docs/experiments/data/53_agents/](../experiments/data/53_agents/) (SHA-256 in `53_agents_manifest.json`) + data [53_cross_context_readout.jsonl](../experiments/data/53_cross_context_readout.jsonl) / [53b_cross_context_readout.jsonl](../experiments/data/53b_cross_context_readout.jsonl) / **R1 [53b_cross_context_readout_replication_2026-08-28.jsonl](../experiments/data/53b_cross_context_readout_replication_2026-08-28.jsonl) (clean tree at `v1.1.0`)** + the two pre-registrations. |

**1.0 commitment:** all six pre-1.0 rows reach a settled disposition before 1.0 ships (post-1.0 additions: Exp 45 EARNED; Exp 48 PARTIAL → superseded by Exp 52 EARNED 2026-08-25; Exp 53b cross-context readout EARNED 2026-08-26 — ten rows as of 1.1.0). EARNED for claims with cited validation evidence; PARTIAL with the bio-framing for that mechanism explicitly pulled from 1.0 release notes (no silent omission) for claims that empirically fail to earn at the pre-registered threshold. Per the 2026-06-07 Exp 37 results and row 1b above, the "substrate drives action selection" framing is explicitly pulled — 1.0 ships as a bio-inspired LLM harness (the [framing strategy](../../CLAUDE.md) bio-inspired-LLM-harness positioning), with substrate-driven action selection deferred to Exp 38 / Oasis post-1.0 work.

### Tier 2 — Scaffolding (stays `[engineering]` permanently, no apology)

Engineering walls around the bio-substrate. Code-correctness contracts, builder invariants, persistence formats, type freezes. **Do not burn experiments earning these.** They will never graduate, and that's fine.

Representative examples (not exhaustive — see CLAUDE.md "Architectural invariants" section for the full list):

- All canonical-builder rules: `build_executor`, `build_pain_bus`, `build_default_network`, `build_reaction_bus`, `build_memory_hub`, `build_bio_stack`
- `_format_version` contract, `atomic_write_json` persistence, frozen-dataclass freeze (CC3)
- `_MaximPeerBackend` one-HTTP-call + `for_url` instance-key + `health_check` rules
- Memory tier progression direction (FORMING → SHORT_TERM → LONG_TERM)
- `mesh.yml` parser frozen + declarative-vs-mutable split
- `Tool.cancel()` / `Tool.input_schema` API freezes (CC9, CC11)
- `PerceptSource` / `ActionSink` protocol minimality (CC8)
- HTTP errors typed + httpx stream context lifetime
- Role detection ordering in `cli.py::main`
- HTTP call sites must use `maxim/utils/http.py`

**Why these don't graduate:** the failure mode is "code breaks loudly without it." There is no behavioral measurement that would meaningfully validate `atomic_write_json` — its job is to not corrupt files on power loss, which is a correctness property, not a bio-claim. Same for the rest. Surfacing this as a deliberate "no graduation needed" set is itself useful — it bounds the graduation work to a tractable scope.

### Tier 3 — Middle subset (graduate-or-downgrade per checkpoint)

These are the mechanisms where the bio-flavored framing in CLAUDE.md (or in the shipping commit messages / experiments / plans) makes an implicit behavioral claim that isn't yet cited. Each gets a graduation predicate or an explicit "can't predicate yet — claim fuzzy" flag.

**Status conventions** (split between pre-graduation and post-graduation lifecycle):

Pre-graduation (1.0 path):

- **Pending — predicate written.** Setup + metric + checkpoint named below. Ready to run when fixture lands.
- **Pending — needs fixture design.** Experimental shape is clear but fixture / metric threshold need design work.
- **Pending — can't predicate yet.** Multi-confound or "what counts as the dependent variable is partially the experiment." These are the most honest finding — flagged so they're visible, not hidden.
- **Borderline scaffolding.** On reflection this probably belongs in Tier 2; surface for decision rather than predicate.

Post-graduation (regression-discipline path):

- **Earned `<date>`.** Cited experiment shipped; CLAUDE.md tag flipped (or scheduled). Entry now lists **Re-run on:** trigger events and **Regression guard:** path to the experiment / test that re-validates it.
- **Maintained `<last-validated-date>`.** Was Earned, has been re-validated on a triggering event without regression. Same fields as Earned.
- **Stale.** Triggering event fired (encoder swap, bio-system refactor in scope, etc.) without re-run completing. **Blocks next release.** Convert to `Maintained` by re-running, or to `Broken` if re-run fails.
- **Broken (regression detected `<date>`).** Re-run failed. Either fix the regression OR explicitly downgrade the mechanism (with bio-framing pulled in release notes). **Blocks next release.**
- **Retired `<date>`.** Mechanism removed from the codebase. Entry stays in the table as historical record so future readers can see why the claim no longer needs validating. Often pairs with a `Dormant` marker on the mechanism per Principle 2.
- **Dropped.** Couldn't earn the predicate by checkpoint and the bio-framing got pulled. Mechanism stays as engineering scaffolding with no apology. Different from Retired — the code still exists, just doesn't carry a behavioral claim.

## Trigger events for re-validation

Not every architectural change should trigger every re-run — maintenance cost would be prohibitive. **Triggers are category-scoped**: only entries depending on the changed category re-run.

| Trigger | Categories that must re-run |
|---|---|
| **Encoder model swap** (sentence-transformers / paraphrase-mpnet / etc.) | All entries citing cosine thresholds or substrate encoding: Tier 1 (EC pattern completion, affordance concept transfer), Tier 3 #1, #5, possibly #10 |
| **Bio-system refactor** touching module M | Entries referencing M's behavior. Cross-check via grep of the regression-guard path against M's source files. |
| **Substrate-pipeline change** (LinguisticEncoder, EC, NAc, ATL, SCN — anything in the dual-write path) | All Tier 1 + substrate-bearing Tier 3 entries (#1, #2, #3, #4, #5, #7, #8) |
| **LLM model swap / prompt-builder change** | LLM-dependent entries only: Tier 3 #11 (Acting Coach), #12 (pre-deliberation). Substrate-primary entries (Tier 1 #5, B5-derived) are by-design LLM-independent and DO NOT re-run on this trigger. |
| **PainBus / ReactionBus / NAc reward pipeline change** | SEM cascade entries (Tier 1 #3, Tier 3 #10, #15) |
| **Cradle / drive / SEM body change** | Drive + reflex entries (Tier 3 #6, #9, #10, #15) |
| **Minor-version heartbeat** (each `0.X+1` release pre-1.0; each `1.X+1` release post-1.0) | Walk all `Earned` Tier 1 entries, confirm Maintained or schedule re-run. Tier 3 walks are opportunistic. |
| **Major-version cut** (1.0 → 2.0, etc.) | Walk the entire table top to bottom; explicit dispositions on every entry. |

**Discipline:** triggers go in the entry's **Re-run on:** field at graduation time, not retroactively. When you graduate an invariant, you're committing to a re-validation contract — naming the triggers is part of earning the tag.

**Catching the "non-code drift" class:** triggers above are mostly code-change events. The Roy-3c bisect showed that non-code drift (encoder weight changes, library version bumps, pip-resolved transitive dependency shifts) can break behavioral claims without any code-side trigger firing. The minor-version heartbeat is the safety net — it re-runs Tier 1 even if no specific trigger fired, catching slow drift.

## Initial seed table — Tier 3 candidates (v0.1, expand iteratively)

| # | CLAUDE.md ref | Mechanism | Bio-claim | Graduation predicate | Status |
|---|---|---|---|---|---|
| 1 | L162 (affordance encoder split) | LinguisticEncoder + AffordanceDecompositionStrategy | Affordance names decompose to substrate concepts that transfer across entities via shared EC nodes | **Setup:** cross-entity transfer fixture (e.g., "flame" trained on torch, evaluated on dragon affordances). **Metric:** NAc `reward_bias` propagation to held-out entity ≥ cosine threshold across N=10 entity pairs. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — predicate never run, and it duplicates the Tier-1 "Affordance concept transfer" row, which is **PARTIAL with the behavioural-transfer claim explicitly pulled**. The nearest evidence ([concept_decomposition_validation.md](../experiments/concept_decomposition_validation.md), +63.6 pp) measures *sentence* decomposition for cross-modal recall, not affordance-name decomposition for cross-entity reward transfer. Resolve at the Tier-1 row, not here. |
| 2 | Wire-A (across CLAUDE.md + env table) | Cluster reward bias annotation with split tau | Substrate-voice multi-turn annotation modulates action selection at horizons > single-turn reward bias | **Setup:** Roy-3a-retry with engineered priming fixture; arms = annotation-on vs annotation-off with split tau (300 cluster vs 50 reward). **Metric:** post-priming action distribution shift sustained across N≥5 turns. **Checkpoint:** Tier 2 tau-split Phase 1 + Roy-3a-retry → 0.9.2 or 1.0 | **Dropped 2026-08-30** — the predicate RAN AND FAILED, three times. [Exp 30](../experiments/30_wire_a_tau_validation.md) PRIMARY (Arm A `sense_food_source` ≥ 1) returned **0** with Wire-A rendering on every submission; [Exp 33](../experiments/33_wire_a_post_fix_a.md) and [Exp 34](../experiments/34_wire_a_post_fix_a_b.md) re-ran post-fix and returned **0/0/0**. This is a refutation, not an absence of evidence. Exp 37's R1-Distill secondary is model-conditional and carries the L8 un-reproducible caveat; Exp 44/44b are self-labelled EXPLORATORY/PILOT. |
| 3 | Wire 1 (CLAUDE.md L37) | Variance annotation on tool descriptions | Risk-sensitive action selection — agent avoids high-variance affordances when low-variance alternatives exist | **Setup:** Roy-3 ablation arm with two affordances at same mean reward, different variance. **Metric:** preference shift toward low-variance affordance ≥ Δ across N attempts. **Checkpoint:** Roy-3 ablation → 0.9.2 or 1.0 | **Dropped 2026-08-30** — predicate never run. Mechanism is live and ablatable (`MAXIM_DISABLE_VARIANCE_ANNOTATION`), but the only Wire-1 ablation data ([Exp 38](../experiments/38_counter_prior_substrate.md) `B-wire-1-off`) scores `warm_self_engagement_fraction` on the deceptive-hearth task — a different dependent variable that says nothing about risk sensitivity. |
| 4 | L161 (SCN oscillator) | OscillatorNetwork anticipatory pre-activation | Diurnal event-type patterns pre-activate relevant percepts/affordances before predicted events | **Setup:** repeated-pattern fixture (e.g., periodic thermal-discomfort event). **Metric:** NAc eligibility-trace pre-activation at predicted event time ≥ baseline by ≥ 0.2x weight. **Checkpoint:** 1.0 if cradle exercises oscillator; 1.1 otherwise | **Dormant since 2026-08-30** — unwired at BOTH ends, so no fixture could run today. `temporal_credit.py::TemporalCreditDistributor.anticipatory_pre_activate` already carries `Dormant since 2026-05-26` (the distributor closed the loop; the production loop never registered a per-tick caller), and its input side is [D9](../bugs/README.md) — 5 of 6 `TemporalEvent` categories have no producer, itself DORMANT since 2026-08-29. Revival: [scn_event_producer_gap.md](deferred/scn_event_producer_gap.md). |
| 5 | L160 (SCN temporal coupling for traces) | `NAc._temporal_anchors` phase-similarity fallback at 0.3x weight | When fast-decay eligibility traces expire, temporal-phase similarity restores credit to nodes activated in same phase | **Setup:** affordance concept transfer fixture with delayed reward beyond fast-decay window. **Metric:** temporal-coupling-on arm credits same-phase nodes at ≥ X cosine vs temporal-coupling-off arm. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — mechanism IS wired in production (`nac.py::distribute_reward`, `NACConfig.temporal_credit_weight = 0.3`, built at `bio_stack.py`), but its validation doc [temporal_credit_validation.md](../experiments/temporal_credit_validation.md) is an **unfilled template** (`Date: TBD`, every result cell blank) — it was never run. **Escalation:** that same never-run template is cited as a `Regression guard:` on the Tier-1 "Affordance concept transfer" row. A Tier-1 row guarded by an experiment that never ran is a worse defect than this row. |
| 6 | L163 (drive protocol) | HomeostaticDriveSpec / EntropicDriveSpec → behavior | Drive deviation beyond `comfort_band` shifts action selection toward corrective affordances | **Setup:** Cradle scenario with thermal / hunger perturbation; measure action selection on corrective vs distractor affordances. **Metric:** corrective preference rate > random + significance threshold when drive deviation > `comfort_band`. **Checkpoint:** 1.0 — cradle B4 is the natural test bed | **Dropped 2026-08-30** — the closest mechanism was ablated and found NOT load-bearing. [Exp 42](../experiments/42_substrate_primary_preference.md)'s B7 drive-gating arm restricts the exploitation candidate set when a drive exceeds its gate; the **gating-OFF ablation graduated identically** (`safe_pref` 0.984 vs 0.965), overturning the pre-registered prediction, and B7 was marked Dormant. What carries the result is B8 plus the cold-start drive-affinity heuristic. Exp 52's HUNGER-NECESSARY PASS shows drive state gates *credit*, not *selection* — adjacent, not this predicate. |
| 7 | L156-157 (valence on edges) | `spreading_activation(propagate_valence=True)` + `apply_hebbian_on_close` + `salience_spike_rule` | Negative-valence-tagged paths attenuate spreading activation; positive amplify | **Setup:** synthetic episode graph with valence-tagged edges; measure recall preference distribution. **Metric:** recall probability for negative-valence path < random baseline; positive > baseline. **Checkpoint:** 1.0 | **Dormant since 2026-08-30** — the production path is inert. `memory/episode.py::apply_hebbian_on_close` carries `Dormant since 2026-08-29: inert on the main percept path (bugs ledger D6)` — `memory_hub` stashes a 1-tuple of substrate nodes and the function returns early on `len(nodes) < 2`, so the binding graph never grows from llm-primary percepts. The 2026-04-17 [valence PoC](../experiments/valence_annotation_poc.md) PASSed on a scripted two-episode fixture, but the dormancy finding supersedes it: the path it exercised is not the production percept path. Resurrection trigger: the 1.3 fabric stage. |
| 8 | L159 (NAc per-tick decay wired) | `decay_eligibility` + `decay_reward_biases` per agent_loop tick | Decay enables credit to track recency; without decay, traces stay forever and disrupt new learning | **Setup:** two arms — decay-on vs decay-disabled — running affordance learning across N tasks. **Metric:** decay-on arm shows faster convergence on later tasks (less interference from earlier reward biases). **Checkpoint:** 1.0 | **Dropped 2026-08-30** — the two-arm predicate was never run. The mechanism is unambiguously live and load-bearing by construction: the per-tick decays run every tick from `agent_loop.py` §8.5, extracted 2026-08-30 to `agent_loop.py::_loop_bio_tick_maintenance` and now pinned by [tests/unit/test_loop_section_extraction.py](../../tests/unit/test_loop_section_extraction.py) (all six decays, in order, every tick). That is an **engineering** guard, not the behavioural predicate — the interference claim stays unearned. Adjacent only: Exp 44's apparatus note that holding decay (`tau=1000`) moved substrate presence 12/100 → 29/30 prompts, which is about annotation coverage, not interference. Calibration parked at [decay_consolidation_calibration_plan.md](deferred/decay_consolidation_calibration_plan.md). |
| 9 | Reflex system (per `project_percept_reflex_system_shipped` memory) | Keyword-matched narrative percept reflexes (`attack_flinch`, `fire_burn`, `impact_brace`, `startle`) | Reflexes fire below deliberation; shape learned avoidance over repeated exposure (habituation + sensitization) | **Setup:** validated per [Experiment 09](../experiments/09_percept_reflex_poc.md) — `bodies/base_humanoid` against dragon attack/fire narration (**NOT** infant thermal contact; a 2026-08-07 doc-truth pass corrected both the filename — the file is `09_percept_reflex_poc.md` — and the description, which had drifted to a different experiment entirely). **Metric:** reflex fires on keyword match + habituation/sensitization trajectories measured. **Re-run on:** Cradle / drive / SEM body change; PainBus / ReactionBus / NAc reward pipeline change; minor-version heartbeat. **Regression guard:** [docs/experiments/09_percept_reflex_poc.md](../experiments/09_percept_reflex_poc.md) + reflex YAMLs in `_data/reflexes/`. **S4 data status (2026-08-24): the ORIGINAL Exp 09 raw log is LOST** — it was `/tmp/reflex_poc.jsonl` (see the doc) and was never committed. The 2026-08-18/19 Sim-Short heartbeat re-run (dragon sim, 7 reflex events incl. `reflex_attack`) is the surviving raw record — **S4 copy COMMITTED 2026-08-24** at [docs/experiments/data/heartbeat_1_1/sim_short_2026-08-18/20260818_004321/](../experiments/data/heartbeat_1_1/sim_short_2026-08-18/20260818_004321/). **HALO CAVEAT (roadmap bio-fidelity #8):** this row covers the keyword-matched NARRATIVE reflex path only. A future audio orienting reflex (different modality, trigger, circuit, and output) does NOT inherit this EARNED status. | **EARNED — Experiment 09** (scope: narrative keyword reflexes only) |
| 10 | L164 (entity acquisition) | `entity_acquired` / `entity_released` contact sensation | Acquired entities contribute sensors to body damage model while equipped; behavioral effect of contact persists | **Setup:** cradle / sim scenario with damaging vs benign acquired entity. **Metric:** damage propagation through acquired entity sensors validated end-to-end; behavioral preference shift over N pickups. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — the behavioural half was never measured. [Exp 08](../experiments/08_component_damage_poc.md) tests the damage cascade, not acquisition: **H2 (affordance blocking) was never demonstrated**, all damage targeted torso, and its own Limitations say so. [Exp 11](../experiments/11_cradle_sensorimotor_poc.md) logged exactly **1** `infant_humanoid_pick_up` call. The `sharp_rock` route additionally reproduces the known four-fire scenario defect recorded on Tier-1 row 1b. |
| 11 | B3 Acting Coach (per `project_07_r0_b3_shipped` memory) | Acting Coach prompt with bio-system modulation (NAc caution, pain anticipation, cerebellum predictions) | Substrate-informed meta-prompt steers affordance exploration toward salient/cautious actions per NAc state | **Setup:** generative sim with Acting Coach on vs off; measure exploration breadth + caution markers. **Metric:** caution rate scales with NAc reward bias magnitude in on-arm. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — prerequisites shipped but the arms were never validly run, and the owning plan is DEFERRED (2026-08-27: "the arms were never validly run (2026-07-15 ops-stack block; the drive-pain cadence changed under it since) and no roadmap item owns them"), naming *this row* as its own revival trigger — a cycle that resolves by dispositioning here. The only measurement, [Exp 12](../experiments/12_v1_phased_attribution.md) Phase C−A ("3 respond hits vs 1"), is a descriptive single-run difference with confounded arms. Prior note: **2026-07-14 deep-dive: Layers 2+4 (pain anticipation + drive modulation) have NEVER run in production** — `body_state` is unwired (`memory_hub.embodiment` never passed at either production `build_memory_hub` site), so the on-vs-off setup as previously written would compare Layers 1+3 only. The ablation design this row needs is now pre-registered at [acting_coach_body_state_ablation.md](deferred/acting_coach_body_state_ablation.md) (Exp 44 — pre-registered; prerequisites shipped on `exp/44-body-state-wiring`); its decision rules feed this row directly (graduate / Dormant-mark per outcome) |
| 12 | Pre-deliberation (per `project_pre_deliberation_shipped`) | ThoughtGate + BioEnrichment Layer 1 pre-LLM | Bio-enrichment of pre-deliberation thought stream improves downstream action selection | **Setup:** comparative arms — pre-deliberation on vs off — on a benchmark task. **Metric:** task success rate or action coherence delta. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — retains "can't predicate cleanly" (multi-confound; entangles with prompt construction), and nothing has moved since 2026-05-27. Both candidate experiments are test-level only: [Exp 08 deliberation](../experiments/08_deliberation_system.md) is "PASS — 18 stress + 25 unit tests green", and [Exp 09 PFC](../experiments/09_pfc_deliberation_cycle.md) is **"PROTOCOL DEFINED — awaiting live LLM run"** with a test count as its results section. A passing test suite is not a behavioural delta. |
| 13 | Working memory (per `project_working_memory_plan`) | `WorkingMemorySet` Exec-owned active reference | Active-reference items in WMS bias recall + action selection toward relevant context | **Setup:** distractor-task fixture; WMS-on vs WMS-bypassed arm. **Metric:** recall latency / accuracy delta on WMS-held items. **Checkpoint:** 1.0 if predicate sharpens; 1.1 if not | **Dropped 2026-08-30** — retains "can't predicate cleanly" (what counts as "relevant" is partially the experiment) and the predicate never sharpened. `agents/working_memory.py` is live and read by `hippocampus_retrieval.py` / `bio_enrichment.py`, but a sweep of `docs/experiments/` returns exactly one hit — the awaiting-live-run PFC protocol. No fixture, no metric, no data. |
| 14 | Imagination (per `project_i1_i2_imagination_shipped`) | `ImaginationTrigger` w/ DN arousal gate + ComponentIndex lookup + energy budget | Novel entity mentions trigger LLM-driven entity design only when DN arousal + energy budget permit | **Setup:** sim with novel-entity-rich percepts; measure design call rate vs arousal floor. **Metric:** design rate scales with arousal above floor; below floor → zero rate. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — the wiring is validated, the dose-response predicate is not. [Exp 07](../experiments/07_imagination_wiring.md) is an integration PoC whose arousal-gate line is a boolean check ("blocks during high arousal"), not a rate measurement, and its own conclusion is scoped to "correctly integrated". No data file. Scope limit worth carrying: only the **sim** AUT gets imagination — CLI non-sim, headless `api.py` and the Reachy runtime do not. |
| 15 | L166 (three interaction levels) | Observe / Touch / Pick up sensation layering | All three converge on sensor change → `evaluate_failures` → PainBus → NAc; each layer adds distinct behavioral signal | **Setup:** cradle / sim exercising all three interaction types on damaging entity; measure NAc learning rate per layer. **Metric:** NAc learning observed in all three branches; layering doesn't collapse to single-layer behavior. **Checkpoint:** 1.0 | **Dropped 2026-08-30** — the layering predicate was never tested; only one branch ever ran. The convergence path's *pain-cascade* half is the Tier-1 EARNED row, but this row's specific claim (learning in **all three** branches, no collapse) has no run: [Exp 08](../experiments/08_component_damage_poc.md) exercised the damage branch only, all damage on torso, with H2 absent from its results table and its Limitations recording "No affordance blocking demonstrated". No per-layer learning-rate data exists. |
| 16 | L152 (`Hippocampus.recall` touch + RECALL reconsolidation) | `memory.touch` + WorkingMemorySet RECALL entries on recall | Recall strengthens accessed memories; reconsolidation pull-into-active improves subsequent recall | **Setup:** repeated recall fixture across sessions; measure access strength growth on touched items. **Metric:** touched-item recall probability > untouched-item probability after N repetitions. **Checkpoint:** 1.0 — partial coverage via V1 cross-session work | **PARTIAL** — V1 cross-session shows 3 memories/turn on resume (per Exp 10), but the isolated reconsolidation effect is not yet ablated against a no-touch baseline |
| 17 | ComponentIndex (per `project_e25_component_index_shipped`) | Two-layer semantic discovery (alias hash O(1) + embedding cosine) | Semantic discovery surfaces relevant components faster than naive scan; alias hits dominate cosine on common queries | **Setup:** discovery benchmark with N entity queries; measure alias-hit rate, cosine-fallback rate, latency. **Metric:** alias hit > 50% on synonym-rich corpus; total discovery latency < baseline scan. **Checkpoint:** could ship before 1.0 | **Borderline scaffolding** — performance property of a discovery mechanism, not a bio-claim. Surface for decision: probably belongs in Tier 2 (engineering, no graduation needed). |
| 18 | L116 (Hippocampus + NAc + ATL separate stores) | Three separate `EpisodicMemory` instances | Coexistence (not merger) is bio-correct AND behaviorally distinct from a merged store | **Setup:** comparative — merged-EpisodicMemory arm vs separate-stores arm. **Metric:** separate-stores arm shows behavior X absent from merged arm. **Checkpoint:** N/A | **Borderline scaffolding → Tier 2 (2026-08-30)** — the underlying invariant is already tagged `[engineering]` **with a regression guard** in [bio-memory.md](../agents/bio-memory.md) ("maintain SEPARATE EpisodicMemory instances — intentional coexistence, not tech debt. Regression guard: `integration/memory_hub.py::build_memory_hub`"). The comparative merged-store arm would need the substantial rebuild the row itself names. No graduation needed. Prior note: **can't predicate cleanly** (architectural / structural; the alternative requires substantial rebuild and isolating "what behavior differs" is the experiment). The 0.9.x checkpoint passed without one. |
| 19 | L157 (NAc `_reward_bias` clamping) | `_reward_bias` clamps to `[0, max_reward_bias]`; negative rewards handled via valence-on-edges | Bias only widens EC recognition, never narrows; pain avoidance routed via valence path not bias narrowing | **Setup:** compare arms — negative-clamped-to-zero vs negative-narrows-recognition. **Metric:** clamping arm shows pain-avoidance via valence; non-clamping arm shows narrowing artifacts. **Checkpoint:** N/A | **Borderline scaffolding → Tier 2 (2026-08-30)** — architectural choice, predicate fuzzy, and no predicate emerged. The clamp is live (`nac.py::credit_node`, `max_reward_bias = 0.20`) and its clamping arm is demonstrated in a scripted fixture ([sem_learning_loop_poc.md](../experiments/sem_learning_loop_poc.md) Scenario A: "NAc bias from pain: None (0.0) — bias clamps to `[0, max]`, pain can't narrow"), with pain-avoidance routed via valence instead. The non-clamping comparison arm would require reimplementing the bias formula. Correctness property, not a behavioural claim. |
| 20 | L85 (PainBus refractory gate) | `(entity, failure_mode)` refractory dedup | Fine-grained refractory prevents within-tick duplicate dispatch while preserving distinct entities | **Setup:** multi-entity pain-burst fixture (two distinct entities fire same-tick). **Metric:** 2 distinct dispatches preserved; same-entity-same-failure collapsed to 1. **Checkpoint:** N/A | **Borderline scaffolding** — correctness property already pinned by regression test, not a behavioral claim. Surface for decision: belongs in Tier 2. |

### Honest accounting — dispositioned 2026-08-30

**The 16 rows the score card named now carry dispositions.** Rows 1–8, 10–15 and 18–19 were
resolved on 2026-08-30 (1.1.2 Cluster C); the card's Research-integrity condition (4) named
exactly that set. **Rows 17 and 20 are NOT included** — they were outside the card's list, and
both still read "Surface for decision: probably belongs in Tier 2", which is an open question
rather than a decision. Stating otherwise would be the over-claim this pass exists to remove;
they are the remaining Tier-3 surface. The v0.1 seed's own accounting block, which this
replaces, had been stale since 2026-05 and was internally inconsistent (it announced "5 with
predicates" over a seven-item list).

All 20 resolve as:

- **1 EARNED** (#9 — narrative keyword reflexes via Exp 09), scope-limited, with the
  data-loss caveat and the audio-reflex halo caveat recorded on the row.
- **1 PARTIAL** (#16 — reconsolidation): V1 broadly covers it; the isolated effect is still
  not ablated against a no-touch baseline.
- **2 Dormant since 2026-08-30** (#4 SCN pre-activation, #7 valence-on-edges): mechanisms
  unwired on the production path, both backed by dated bugs-ledger dispositions (D9, D6) and
  both with named 1.3-fabric resurrection triggers. No fixture could run today.
- **12 Dropped** (#1, #2, #3, #5, #6, #8, #10, #11, #12, #13, #14, #15): the code stays as
  engineering scaffolding, with no apology and no behavioral claim.
- **2 Borderline scaffolding → Tier 2, decided** (#18, #19): correctness/architectural
  properties, not bio-claims; #18's invariant already carries an `[engineering]` tag and a
  regression guard.
- **2 still surfaced for decision** (#17, #20): the same shape, but never formally
  dispositioned and outside the card's list. Left open deliberately rather than swept in.

**EARNED count among the 16 newly dispositioned rows: zero.** None had an experiment
measuring a behavioral delta attributable to its mechanism with a citable
`docs/experiments/data/` file. That is the honest result, and it is the point of the exercise.

**Three of the Dropped rows are stronger than "never tested" — their predicates ran and
FAILED**, and writing them off as absent evidence would understate what is known:

- **#2 (Wire-A)** — Exp 30 PRIMARY returned 0, then Exps 33 and 34 returned 0/0/0 post-fix.
- **#6 (drive specs)** — Exp 42's B7 gating-OFF ablation graduated *identically*, overturning
  the pre-registered prediction; B7 was marked Dormant.
- **#8 (per-tick decay)** — not refuted, but the opposite shape: the mechanism is provably
  load-bearing by construction and now carries an engineering regression guard, while the
  *interference* claim it was written to earn remains unrun.

**Two findings surfaced outside the ask**, both worth their own follow-up:

1. `docs/experiments/temporal_credit_validation.md` is an **unfilled template**
   (`Date: TBD`, every result cell blank) — and it is cited as a `Regression guard:` on the
   **Tier-1** "Affordance concept transfer" row. A Tier-1 row guarded by an experiment that
   never ran is a more serious defect than anything in Tier 3.
2. `docs/experiments/results/` holds records (`concept_decomposition_validation.json`,
   `c1_smoke_*`, `ec_drift_phase_2_fine_sweep.json`) that sit **outside**
   `docs/experiments/data/` — the directory the S4 data-citation rule and
   `scripts/lint_prereg_precedes_data.py` actually scan. Worth a sweep as that rule is
   mechanized.

## Status check cadence

The cadence has two layers: **scheduled walks** (heartbeat) and **event-driven re-runs** (triggers above).

**Scheduled walks:**

- **0.9.x checkpoint** (each minor release pre-1.0): walk the full table. Flip `Earned` tags as experiments ship. Drop entries that didn't earn predicates. Fold borderline-scaffolding entries down to Tier 2. Add new bio-mechanisms shipped this minor as fresh Pending entries.
- **1.0 readiness review:** Tier 1 must be 100% `Earned`. Tier 3 entries that can't be earned have explicit dispositions: scaffolding-downgrade, dormant per Principle 2, or 1.1+ deferral. No `Stale` or `Broken` entries allowed.
- **1.X minor heartbeat** (each minor release post-1.0): walk all `Earned` Tier 1 entries. For each, confirm `Maintained` or schedule a re-run. Opportunistic walks of Tier 3.
- **Major-version cut** (1.0 → 2.0 etc.): walk the entire table top to bottom; explicit disposition on every entry.

**Event-driven re-runs:** any trigger in the table above fires a re-run of the entries it covers. The re-run flips status `Earned → Maintained` (pass) or `Earned → Broken` (fail). If a trigger fires and the affected entries haven't been re-run, they go `Stale` and **block the next release**.

**Release gates:**

- **No release ships with `Stale` or `Broken` entries.** Either re-run (Stale → Maintained) or explicitly disposition (Broken → fix-or-downgrade).
- **The minor-version heartbeat catches slow non-code drift** (encoder weight shifts, library version bumps, transitive dependency drift) that wouldn't fire a code-change trigger. This is the safety net for the Roy-3c-class regressions.

**This is a living doc, not a one-shot.** New bio-mechanisms entering at any version track here from inception, not retroactively. The seed table below is v0.1; expect it to grow with each minor release.

## Cross-references

- [CLAUDE.md "Working principles for new mechanisms"](../../CLAUDE.md#working-principles-for-new-mechanisms) — Principle 1 (two-tier invariant tracking) is the gate that triggered this doc; Principle 5 (regression-guard / experiment citation) is the format convention that aligns this doc's Earned entries with the CLAUDE.md invariants surface.
- [behavioral_convergence_practice.md](deferred/behavioral_convergence_practice.md) — "does the agent get better" (system-level, ongoing). Sibling doc, different question.
- [v1_refinement.md](archive/v1_refinement.md) — 1.0 unified plan. Tier 1 entries here line up with V1 validation work.
- [release_0_9_1.md](archive/release_0_9_1.md) — current active release. Tier 3 #2 + #3 (Wire-A + Wire 1) hinge on Roy-3 ablation arms shipping there.
- [cluster_reward_bias_decay_tau_split.md](archive/cluster_reward_bias_decay_tau_split.md) — Tier 3 #2 depends on Phase 1 implementation.
- [grounded_language_acquisition.md](grounded_language_acquisition.md) — Tier 1 substrate-primary entry lines up with B5 work.
- Roy harness fixtures live in [`scenarios/cradle/roy_0_smoke.yaml`](../../scenarios/cradle/roy_0_smoke.yaml) + [`scenarios/roy/`](../../scenarios/roy/) (the orphan `docs/plans/roy/` copy was removed 2026-08-27).

## What this doc is NOT

- **Not a substitute for individual experiment plans.** Predicates here are seeds; full experimental designs live in `docs/experiments/` once they're being run. The **Regression guard** field on Earned entries points back to the experiment doc.
- **Not a wish list.** Entries without predicates after the 0.9.x checkpoint cycle drop or downgrade — they don't sit indefinitely as aspirations.
- **Not exhaustive at any point in time.** v0.1 seeds 20 candidates; expect the table to grow with each minor release as new bio-mechanisms ship. The table being incomplete is normal; the table being stagnant is the failure mode.
- **Not the 1.0 release gate by itself.** Benchmarking is the sibling gate; both must pass.
- **Not just a 1.0 artifact.** The discipline persists indefinitely — Earned invariants get re-run on triggering events post-1.0, and `Stale` / `Broken` entries block subsequent releases.
