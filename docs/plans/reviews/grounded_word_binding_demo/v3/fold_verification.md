# Fold-verification lens — grounded_word_binding_demo.md v3

**Round:** v3 review, 2026-09-24. **Target:** `docs/plans/grounded_word_binding_demo.md` v3
(`00f74b37`, merged via #881/#882). **Reviewer:** Claude subagent, read-only. The report below
is verbatim. Corrections found during re-verification are recorded in [README.md](README.md),
not edited into the report.

---

FOLD-VERIFICATION of `docs/plans/grounded_word_binding_demo.md` (v3) against the six reviews in `docs/plans/reviews/grounded_word_binding_demo/`. Reviews were written against v2 (`6ebcf9b1`); v3 (`00f74b37`) is the unreviewed fold.

1. **BLOCKER — the recalled-situation input has no owning stage.** wiring DNB-1: "`NAMES` has no reader, and the reader it needs has no slot… Name the read seam in the plan before Stage 2 is built," plus a guard test proving the evoked cluster writes no credit, no fear, no situation. v3 names it as front-gate item 1 (L151) and gives it a row in decision 1 (L115), then never builds it: Track L is L0 (capture) → L1 (text joins the situation, "recording-only") → L2 (binding, "recording-only") → Exp A, whose phase 3 depends entirely on the recall read. Front-gate's "Everything else rides" list assigns stages to the other three new mechanisms (foreign layer → S1, ATL payload → J1, mirror → S1); the recall slot gets none, and L2's only guard is L1's text-slot test. Fix: add the read seam to L2 (evoked input computed in `propose_via_substrate` from `find_by_relationship(text_id,"NAMES")`, read only by `anticipatory_threat_need` and the `recommend_action` cluster term, named ATL handle) with wiring DNB-1's guard test.

2. **BLOCKER — internal contradiction on the pre-boot path.** Decision 2 (L131): "The **pre-boot** bundle ingest keeps today's merge path unchanged." J1 (L294–295): foreign relations "on a pre-boot ingest are discounted and capped." Both cannot hold, and v3's own what-exists table (L99) records today's path as "want merges as a plain mean with no foreign discount". security D1's fallback is explicit: if merge-in is kept, "add a foreign-want discount before Stage 6." Exp B tests pre-boot vs live as non-inferiority, so the undiscounted path is a demo path. Fix: state the pre-boot discount/cap as a J1 deliverable and delete "keeps today's merge path unchanged".

3. **SHOULD-FIX — the foreign layer's weight makes "own wins" vacuous exactly at the trigger.** security D1 requires: read = own value when own evidence ≥ k, else the *discounted* foreign value; foreign want discounted exactly as fear; foreign fear extinguishable by own non-aversive visits. v3 (L124, L243) has only `trust(source) × (1 − own confidence)` plus generic "decay", no k, no discount floor — and S2's trigger fires precisely when own confidence ≈ 0, so foreign advice enters at `trust(source)`, which is constant because learned trust is deferred to a follow-up (L266). Fix: add a static foreign discount floor and make decay of a foreign entry driven by the agent's own disconfirming visits (security D1 items 2–4). Same gap answers wiring DNB-3.

4. **SHOULD-FIX — the entry index shipped without its anti-rollback half.** security D3's recommended option is a signed entry index *and* "the client tracks the newest release sequence it has seen, which blocks rollback"; without it a host can serve a superseded (stale) entry. v3 decision 3 (L133–138) and S1 carry the index, the per-release verification and the per-entry journal, but no monotonic release sequence — while Exp C's headline condition is *staleness*. Fix: add "client tracks and refuses a release sequence below the newest seen" to S1.

5. **SHOULD-FIX — the V8 amendment is not recorded.** architecture D2 makes entry-level idempotence "a dated amendment to the contract, not a bypass". v3 records only contract §1's amendment as a DECISIONS.md entry (L295–297); the §4 / V8 per-entry amendment is described mechanically (L138) but never recorded. Fix: name both amendments in J1's DECISIONS.md entry.

6. **SHOULD-FIX — Exp C isolation dropped three of security D4's six asserted items.** v3 (L281–283) keeps the ephemeral key, test-only namespace and tmpdir. Missing: loopback-only bind; "corrupted bundles are never committed as `.zip` — commit the recipe (seed + corruption spec)"; and, load-bearing, "never simulate corruption through `--allow-unsigned`: the corrupted arm must pass the same signature path as the clean arm, or Exp C measures the signature check instead of the gate." Fix: restore all three as harness assertions.

7. **SHOULD-FIX — ATL payload fold dropped the residual.** architecture D3 requires, beyond NAMES-only: text EC nodes screened through `is_identity_bearing` with the threshold stated, `public_oasis.md` Phase 0 item 3 privacy re-run for any release carrying text nodes, and "name the inversion residual in the plan" (a single-member text centroid is close to one sentence embedding). J1 (L287–290) fixes the concepts and says nothing about the EC centroid; Exp B's "dangling" arm confirms EC nodes still ship. Fix: add the screening + privacy re-run + one sentence on the inversion residual.

8. **SHOULD-FIX — v3 asserts review conclusions the reviews do not support.** (a) §Biological basis is headed "*Verified by the bio-fidelity lens against the sources (2026-09-24)*" yet its bullets endorse v3-only mechanisms the lens never saw: "v3's transport matches it more closely than v2's" (L54–56) and "v3's foreign layer is that distinction, and it is what makes 'own experience wins' enforceable" (L66–67) — the bio lens reviewed the live merge, and its S7 says own-wins is confidence-weighted, not absolute; per finding 3 v3 has not made it enforceable. (b) Decision 2 asserts "a pure-Python merge too slow for a ≈1 s tick"; architecture D1 says the opposite — it "needs measuring before anyone assumes it fits." (c) The Review record folds architecture D1 under "the live merge could not keep 'own experience wins'"; D1 was about an undefined critical section losing updates, not own-wins. Fix: scope the verified-by line to the citations and trigger corrections, and restate the merge cost as unmeasured.

9. **NIT — env E1/E2 remainders.** Dropped: "record the teacher's join time so no join line falls inside a window"; the client-side `time.monotonic()` receipt stamp; correcting `l11_real_trace_remeasure.py`'s false docstring and pinning the new bridge fields in the FakeBridge lockstep test. L0 keeps only the bridge stamp, 100 ms cadence and out-of-range parking.

10. **NIT — the count is wrong.** Actual DO-NOT-BUILDs: confounding 2 (DNB-1/2), bio-fidelity 1 (B1), wiring 3 (DNB-1/2/3), environment 2 (E1/E2), architecture 3 (D1/D2/D3), security 4 (D1–D4) = **15**, not the header's and Review record's "14" (L9). Deduplicating the cross-confirmed pairs would give fewer than 14, not 14, so no reading rescues the number. Fix: "15 DO-NOT-BUILD findings".

**Tally:** of 15 DO-NOT-BUILDs — **8 positively verified as folded** (confounding DNB-1, DNB-2; bio B1; wiring DNB-2; environment E1, E2; architecture D1; security D2), **6 partial/ambiguous** (architecture D2, D3; security D1, D3, D4; wiring DNB-3), **1 violated** (wiring DNB-1: named as new, described as required by Exp A, built by no stage).

**Verdict: the fold does not hold as claimed — one DO-NOT-BUILD is unbuilt, one mechanism is described two contradictory ways, the stated count is wrong, and v3 credits the bio-fidelity lens with verifying mechanisms it never reviewed; fix findings 1, 2 and 8 before this plan is treated as reviewed.**
