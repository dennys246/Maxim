# Phase 0 — Lens 4 (TRUTH): CLAUDE.md vs code drift audit

**Worktree:** `/Users/dennyschaedig/Scripts/Maxim-wt-claudemd` (CLAUDE.md identical to main checkout, 669 lines / ~256K chars).
**Method:** every suspicious claim checked with grep/sed/python against the actual tree; all cited paths in the invariant bodies spot-checked for existence; every explicit line-number reference resolved; "pending"/"follow-up" claims checked against git log + `.github/workflows/test.yml` + `scripts/`.

## Main table — non-TRUE rows only

| # | Claim (quoted, CLAUDE.md location) | Verification performed | Verdict | Corrected wording for the split |
|---|---|---|---|---|
| 1 | "Current version: v0.7.0 on PyPI as `pymaxim`" (Active initiatives, opening line) | `pyproject.toml:7` → `version = "1.0.6"`; `src/maxim/__init__.py:16` → `__version__ = "1.0.6"`; `docs/plans/README.md` header: "Current version: **1.0.6** … Now: **1.1 'Sensorimotor'**" | **DRIFTED** (16 months of releases behind) | "Current version: **v1.0.6** on PyPI as `pymaxim`; active theme **1.1 'Sensorimotor'** — see [docs/plans/README.md](docs/plans/README.md)." |
| 2 | Entire "Active initiatives" section framing: "**Gating 1.0** (three focused substrate plans…)", "**Recently shipped (2026-04-24)**", "**Previously shipped (2026-04-17)**" etc. | All listed "gating 1.0" plans are marked COMPLETE *inside the same section*; 1.0.0 shipped 2026-06-17 per plans README; the "recently shipped" blocks are from April 2026 | **STALE** (self-contradicting fossil) | Replace the whole section with 3–5 lines: current version, current theme, link to `docs/plans/README.md` (the roadmap index it already defers to). All shipped-history moves to lesson/plan files. |
| 3 | "(config_unification.md C1+C2, 2026-06-02, **PR pending**)" — config.json operator-config invariant; same "PR pending" on the C3 role-detection invariant and the C4 auto-migration invariant | `docs/plans/archive/config_unification.md` (archived); `src/maxim/runtime/config_writer.py` exists; post-impl fold commit `27a9c5ed` merged; CI grep step "config_unification.md C2 + C6 invariants (IM2 fold)" live in workflow | **STALE-SHIPPED** (×3 entries) | Drop "PR pending" from all three; cite `docs/plans/archive/config_unification.md` and the shipped CI grep. |
| 4 | "a diff-scoped CI lint (grandfather existing, block new) is the tracked follow-up" (No-NEW-silent-swallows invariant) | Commit `30b31e2f` "ci(lint): no-silent-swallows lock (fail-loud Stage 4)"; workflow step "No-silent-swallows lint" at `.github/workflows/test.yml:675`; `scripts/lint_no_silent_swallows.py` implements exactly the grandfather-at-origin/main-count design | **STALE-SHIPPED** | "Regression guard: `scripts/lint_no_silent_swallows.py` (CI step 'No-silent-swallows lint', Stage 4 of measurement_path_fail_loud) — per-file swallow counts grandfathered at origin/main; a branch may not increase any file's count." (Count note: the lint's own counter reports **432** today, so the "≈450 as of 2026-07" figure is still roughly right — update to "≈430" or drop the number.) |
| 5 | "65 seed components across 7 categories (bodies, creatures, environments, items, npcs, vehicles, weapons)" (Architecture Essentials → SEM Component Registry) | `find src/maxim/_data/components -name "*.yaml" | wc -l` → **96**; `ls` shows **8** categories (adds `archetypes`) | **DRIFTED** | "~96 seed components across 8 categories (archetypes, bodies, creatures, environments, items, npcs, vehicles, weapons)" — or drop the counts entirely (they churn). |
| 6 | "8 cloud providers … across 15 cloud profiles, plus 15 local profiles" (Architecture Essentials → LLM routing) | Parsed `_BUILTIN_PROFILES` in `models/language/config.py`: 33 total = 15 cloud (correct) + **18 local** (14 llama-cpp GGUF + 4 torch) | **DRIFTED** (local count only) | "…15 cloud profiles, plus 18 local profiles" — or "~33 built-in profiles" without the split. |
| 7 | "17 verb-based functions, all lazy-loaded" (Python API section) | `_API_VERBS` frozenset in `src/maxim/__init__.py:19` contains **19** names (comment notes get_session/list_sessions since removed, but 19 remain) | **DRIFTED** | "19 verb-based functions" — or "the `_API_VERBS` set in `__init__.py`" without a number. |
| 8 | "The full suite has ~4,000 tests" (Testing → run narrow first) | `grep -h "def test_" tests/**/test_*.py | wc -l` → **8,816** test functions | **DRIFTED** (≈2× low) | "~9,000 tests" or "several thousand tests" (numberless is safer). |
| 9 | "Six logical call sites in `src/maxim/` (cli.py, simulation/orchestrator.py × 2, embodied_runtime/agentic_runtime.py, api.py, simulation/tools.py) — seven physical calls" (`build_executor` canonical-wiring invariant) | AST/grep of real (non-docstring) calls: `api.py:514`, `runtime/agent_factory.py:499`, `simulation/tools.py:1276`, `simulation/foundry.py:588`, `embodied_runtime/agentic_runtime.py:580` + `:615`. **Zero** calls in cli.py or orchestrator.py (migrated into AgentFactory per F3–F5) | **DRIFTED** | "Five logical call sites (api.py, runtime/agent_factory.py, simulation/tools.py, simulation/foundry.py, embodied_runtime/agentic_runtime.py) — six physical calls (agentic_runtime has an if/else pair). CLI + sim orchestrator now route through `AgentFactory.create_full_agent`." |
| 10 | "`lane_backends.py:2474`" (Running-sims n_ctx rule: server-side spawn precedence) | n_ctx spawn precedence now at `lane_backends.py:2601-2605` (`MAXIM_LLM_N_CTX` / `_cli_n_ctx`); :2474 is now profile-resolution code | **DRIFTED** (line ref) | Cite `runtime/lane_backends.py` spawn-precedence block (currently ~:2601) or drop the line number. |
| 11 | "`router.py:203`" (budgeter reads `router.n_ctx` = profile default) | `n_ctx` property now at `router.py:206-209`; still returns raw `cfg.n_ctx` (substance intact) | **DRIFTED** (minor line ref) | Drop the line number; "the `LLMRouter.n_ctx` property". |
| 12 | "`build_executor` threads `distributor=` into ToolPainBridge (`:439`) but constructs `Embodiment(entity, pain_bus=..., agent_id=...)` without it (`:456`)" (SCN oscillator CONCERN-VOID note) | Now `bootstrap.py:459` (distributor into bridge) and `:489` (Embodiment without distributor). Substance verified still true — including the malformed `TemporalEvent(temporal_signature=/metadata=)` call still present verbatim at `embodiment/body.py:430-438` inside the `except Exception: log.debug` swallow | **DRIFTED** (line refs only; claim itself TRUE) | Cite `:459` / `:489`, or drop numbers and name the two construction sites. |
| 13 | "DefaultNetwork ([default_network/network.py:360]) currently constructs `PainBus()` directly with split subscriber ownership (NAc via `PainCircuitBridge`, hippocampus via the external consumer in `embodied_runtime/agentic_runtime.py:719`); this is **deliberately deferred to Wave 2**" (build_pain_bus invariant) | `PainBus(_allow_raw=True)` construction now at `network.py:396` and only as the **no-injection fallback**; Wave 2 shipped — `build_default_network` (bootstrap.py:670) takes `pain_bus=` and both production callers inject; `agentic_runtime.py:520` comment confirms bio_stack wires the subscribers; the ":719 external consumer" no longer exists (line 719 is tool registration) | **SUPERSEDED** (contradicted by the later `build_default_network` invariant in the same document) | Rewrite the paragraph to the current truth: "DefaultNetwork receives an injected PainBus via `build_default_network(pain_bus=…)` (Wave 2, shipped 2026-04-16); direct `PainBus(_allow_raw=True)` construction at `network.py:396` survives only as the no-injection fallback." |
| 14 | "the user-facing API decision (default-on vs default-off bio-learning for headless `pymaxim` agents) belongs to `agent_factory_canonicalization.md` Stage F5" (build_pain_bus invariant) | Plan is archived (`docs/plans/archive/agent_factory_canonicalization.md`); the bio_stack invariant elsewhere says "the AgentFactory consolidation finished". `api.py:514` still passes `pain_bus=None` on one headless path | **STALE-SHIPPED** (decision venue closed) | State the outcome instead of the pending venue: check F5's resolution in the archived plan during the split and record it (as of today the headless `api.py` executor path remains an explicit `pain_bus=None` opt-out at `api.py:514`). |
| 15 | "api.py headless ([api.py:436]) keeps its explicit `pain_bus=None` opt-out" / "api.py:450 was the one site that passed NOTHING" | `pain_bus=None` now at `api.py:514`; the LLMWorker n_ctx construction now at `api.py:457` | **DRIFTED** (line refs) | Cite `api.py:514` / `api.py:~457`, or drop numbers. |
| 16 | "the broad-guard narrowing contemplated at `tool_pain_bridge.py:367-371`" (build_pain_bus tripwire paragraph) | The narrowing note (iterate `_pending_contexts` and match) is now at `tool_pain_bridge.py:~430-440` | **DRIFTED** (line ref) | Drop the line range; "the narrowing contemplated in `_on_embodiment_pain`'s docstring". |
| 17 | "pre-CC9, `runtime/agent_loop.py:2702-2722` iterated `input_schema.items()`…" (Tool dual-schema invariant) | Lines 2702-2722 are now unrelated proposal-logging code; the reference is explicitly historical ("pre-CC9") but the line range no longer landmarks anything | **DRIFTED** (historical line ref) | Move the line range into the lesson file as "at the time"; the stub needs only "pre-CC9 the dynamic-tool prompt branch in agent_loop iterated `input_schema.items()` directly". |
| 18 | "Remaining doctor enhancements are tracked inline as TODO comments in [src/maxim/doctor/]" (doctor section) | `grep -rin "todo\|FIXME" src/maxim/doctor/*.py` → **0 matches** | **DRIFTED** | Drop the sentence (nothing is tracked there anymore), or re-point at wherever doctor follow-ups actually live now. |
| 19 | "frozen `_VALID_TIER_NAMES`" (lane capability/placement invariant — no path given, but the neighboring tier-names guard points at `lane_models.py`) | `_VALID_TIER_NAMES` lives at `runtime/config_loader.py:67`, not lane_models.py | **DRIFTED** (location, if a path gets added during the split) | If the split adds a path: `runtime/config_loader.py::_VALID_TIER_NAMES`. |
| 20 | "(transition_based_drive_pain.md Phases 0-1 …)" — channel-split invariant header cites the plan bare; the tick-cycle entry cites `deferred/transition_based_drive_pain.md` | File exists only at `docs/plans/deferred/transition_based_drive_pain.md` (shipped work, file never moved to archive) | **DRIFTED** (minor: ambiguous path + "deferred" label on shipped work) | Use the full `docs/plans/deferred/transition_based_drive_pain.md` path consistently; note in the lesson file that the plan shipped 2026-07-28 despite living in deferred/. |
| 21 | "Exp 44 embodied llm-primary numbers still need re-validation against the Track-1 cadence" (tick-cycle invariant) | No re-validation doc found; `docs/experiments/44b_pilot.md` (2026-08-10/11) exists — PILOT COMPLETE, confirmatory campaign "not yet frozen" | **UNVERIFIABLE** (likely still open) | Keep the claim; add a pointer to `docs/experiments/44b_pilot.md` + `protocols/exp44b_preregistration.md` as the live venue. |

## Notable suspicions that turned out TRUE (resolved, no change needed)

- **n_ctx leg 3 / #484:** CLAUDE.md already records "(leg 3, PARTIALLY CLOSED 2026-08-09)" — matches commit `db5a8768` (#484). The remaining "cross-process reuse OPEN" claim verified in code: `lane_backends.py:2552-2553` deliberately leaves `served_n_ctx` unset on unowned-server reuse. Both guard tests exist (`test_lane_served_nctx.py`, `test_llm_worker_n_ctx_clamp.py`).
- **heartbeat.py stall allowlist "pending Stage 2":** still allowlisted at `.github/workflows/test.yml:166-169` — Stage 2 has NOT shipped; wording current.
- **Principle-5 lint follow-ups** (extend to graduation doc + existence-check cited paths): `scripts/lint_claude_md_invariants.py` still does neither — "tracked follow-up" wording current (and this diet plan is the named vehicle for the existence check).
- **"≈450 silent swallows as of 2026-07":** the shipped lint's own counter reports 432 — count essentially accurate (a naive `grep -E 'except Exception:\s*$'` over-counts at 839; the lint's parser is the canonical instrument). Only the "tracked follow-up" clause is stale (row 4).
- **Stall-detector known limitation** (orchestrator reads `MAXIM_LANE_LARGE_TIMEOUT_S` directly): still true — `simulation/orchestrator.py:~2594` comment + code confirm.
- **`agents/llm_agent.py` grandfathered backend import:** still imports `_LlamaCppBackend` directly (`llm_agent.py:28`) — allow-list wording current.

## Verified still-true (abridged; all checked this session)

- **Constants:** `_PROMOTION_PRESSURE_THRESHOLD = 3.0` (hippocampus_consolidation.py:19); `INFERENCE_BROKEN_BACKOFF_S = 15.0` (types.py:14); `_PROXY_CHAR_TO_TOKEN_RATIO = 3.5` / `_PROXY_DEFAULT_MAX_TOKENS = 2048` (leader_proxy.py:295/305); `_STABLE_BUDGET_FRACTION = 0.7` (prompt_budgeter.py:20); frozen modalities `{"interoception", "audio"}` identical in `ECConfig` (ec.py:236) and `DEFAULT_FROZEN_CENTROID_MODALITIES` (hivemind/merge.py:548).
- **Zero-match invariants hold:** `urllib.request.urlopen` outside utils/http.py = 0; `NucleusAccumbens`, `EnergyReactionBridge`/`MovementEnergyTracker`, `probe_llm_server`/`llm_server_responding_at`, `PromptAssembler`, `@resilient` — all 0 matches in src/maxim/.
- **cli.py startup ordering:** `configure_logging` (:546) → `detect_and_apply_role` (:556) → LeaderProxy start (:771) → `_normalize_args` (:835) — exactly as claimed.
- **`check_existing_llm_server` error message** includes the concrete `maxim config set llm.profile` command (llm_server.py:350).
- **All three spawning harnesses** call `assert_repo_interpreter` (`benchmark_exp42_preference.py:568`, `benchmark_exp41_exploration.py:352`, `benchmark_cross_session.py:1657`).
- **EmbodimentPerceptSource Dormant:** no constructor call outside its own module.
- **`maxim.robots` entry-point group** consumed at `api.py:748`.
- **Version sync:** pyproject.toml and `__init__.py` agree (1.0.6).
- **Env-var producers + conftest scrubs spot-checked:** `MAXIM_SUBSTRATE_ACTIONS_PER_TURN` (bridge.py:37), `MAXIM_PLACE_CODE_EXTEROCEPTION` (agent_loop.py:828), `MAXIM_NAC_CLUSTER_REWARD_BIAS_DECAY_TAU` (nac.py:602); `_isolate_maxim_place_code_env` / `_substrate_actions_per_turn_env` / `_motor_credit_trace_env` all present in tests/conftest.py.
- **Named tests exist:** `test_cancel_has_no_caller_in_executor_dispatch` (test_tool_cancel.py:77), `TestHarnessPreflight` (test_exp37_harness_smoke.py:1568), `TestCheckExistingLlmServer` (test_llm_server.py:141), `test_system_prompt_byte_stable_across_turns` (test_prompt_caching.py:137), plus test_reachy_{head_frame,retained_axes,workspace_safety}.py, test_transition_drive_pain.py, test_stable_hash_two_process.py, test_nac_persistence_decay.py, test_cross_session_persistence.py.
- **~40 cited doc/file paths existence-checked, all resolve**, including: scripts/_provenance.py, peer/{probe_classify,mesh_setup}.py, runtime/config_writer.py, utils/format_version.py, docs/experiments/{42b_drive_pain_fold_revalidation,31_roy_divergence_audit,45_reachy_orient_live,46_operant_orient_creche}.md, docs/plans/{behavioral_graduation_candidates,maxim_hivemind,acting_coach_body_state_ablation,simulation_apparatus_standards}.md, docs/plans/deferred/{scn_event_producer_gap,sense_tool_registry,transition_based_drive_pain}.md, docs/plans/archive/{config_unification,agent_factory_canonicalization,release_0_9_1,ec_centroid_drift_fix,v1_refinement,pain_bus_unification,substrate_p0_pilot,llm_path_refinement}.md, docs/user/{tool_side_effects,cli-reference}.md, docs/behaviors/*, docs/embodiment/reachy_mini/README.md, docs/troubleshooting/remote_update.md, scripts/check_oscillator_coldstart.py, bench/recovery_time.py, hivemind/bundle.py, the quick-reference table's module paths (percept_trace_buffer, temporal_event, causal_link, acting_coach, sim_types, working_memory, memory/rwlock, component_index), and the memory-file cross-link `~/.claude/projects/.../feedback_review_before_ship.md`.
- **Principle-5 lint step** referenced in workflow ("1.0 guard promotion" steps at :529 etc.); `scripts/lint_claude_md_invariants.py` exists and matches the described scope.

## Guidance for the split

1. **Fix rows 1–2 first** — the Active-initiatives fossil is the single largest truth debt and is pure deletion + one pointer.
2. **Drop line numbers wherever a symbol name suffices** (rows 10–17): every drifted reference this audit found was a line number; symbol/function citations aged fine. The compression contract's ≤4-line stubs should cite `file::symbol`, never `file:line`.
3. **Drop volatile counts** (rows 5–8) or round them ("~") — component counts, profile counts, test counts, verb counts all drifted while every named symbol held.
4. When compressing the build_pain_bus entry, **resolve the internal contradiction with the build_default_network entry** (row 13) rather than compressing both sides of a superseded statement into the stubs.