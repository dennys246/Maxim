# Known defects & limitations — running ledger

**What this is.** The fourth ledger. The repo already tracks *behavioral* claims
([behavioral_graduation_candidates.md](../plans/behavioral_graduation_candidates.md)),
*algorithmic* claims ([bio_faithful_roadmap.md](../plans/bio_faithful_roadmap.md)), and
*engineering rules* (the repository's agent-guidance invariants). This tracks **what is verifiably wrong or
bounded right now** — the axis on which findings otherwise evaporate into plan-doc asides.
(A fifth ledger, [docs/limits/README.md](../limits/README.md), tracks measured
*instrument* limits — nothing broken, but properties every experiment design must
respect. Defects go here; characterized measurement limits go there.)

**Why it matters more than a normal bug list.** The 2026-08-10 external critique's sharpest
point was that unstated limitations quietly become false claims. A defect that lives only
in one session's memory is indistinguishable, six months on, from a defect that was fixed.

## Rules (these are what stop this becoming a second CLAUDE.md)

1. **Verified only.** Every row cites `file:line` or a measurement. No suspicions — those
   belong in a plan's open-questions section.
2. **Every row has a disposition**, not just a description: `OPEN` (will fix, trigger
   named), `ACCEPTED` (deliberate limitation + why), `DORMANT` (the mechanism stays
   wired but nothing new builds on it — CLAUDE.md's "Dormancy over deletion"; names the
   resurrection trigger, added 2026-08-29 for D6/D9), or `FIXED` (PR + guard, then it
   leaves this file at the next prune).
3. **Rows expire.** A `FIXED` row is deleted once its guard exists — the guard is the
   durable record. An `OPEN` row with no trigger and no movement for two releases gets
   re-argued or becomes `ACCEPTED`.
4. **Claim linkage is mandatory** when a defect bounds a claim: name the graduation row it
   touches. A defect that silently invalidates an Earned row is the worst case this ledger
   exists to prevent.
5. **Deep investigations stay as their own doc** in this directory (the existing
   convention — see `console_seam_findings.md`). This file indexes them and carries
   standalone defects.

## Investigation clusters

| doc | scope | state |
|---|---|---|
| [console_seam_findings.md](console_seam_findings.md) | `maxim serve` console seams, sim_logger, PFC gate, aarch64 packaging | 12 fixed, 6 open |
| [display_print_corruption.md](display_print_corruption.md) | interactive display / print interleaving | see doc |
| [sim_embodiment_followups.md](sim_embodiment_followups.md) | sim embodiment wiring follow-ups | see doc |
| [repository_review_2026_08_19.md](repository_review_2026_08_19.md) | stable API contracts, lifecycle cleanup, architecture enforcement, offline tests | 6 open (D15–D20) |

## Standalone defects

Seeded 2026-08-11 from a four-lens architecture review; each was verified against the code
during that review and currently lives nowhere else.

### Substrate / persistence

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D1 | **`encoder_provenance` detects nothing at runtime.** It is recorded, persisted and reloaded, but nothing compares it against live encoder state — its only readers are the hivemind bundle/CLI export, and `record_encoder_provenance` *merges* divergence ("'mixed' is a finding, not an error"). A geometry change loads old-geometry centroids and cosine-scans them against new embeddings: **silently merged.** | **OPEN** — trigger: any encoder/threshold change. Prerequisite of the place-code default-ON gates. | `similarity/ec.py` (record/persist/load), `hivemind/bundle.py`, `hivemind/cli.py` |
| D2 | **There is no way to invalidate a stale EC substrate.** `cli_utils.py::MEMORY_PATHS` has keys for `hippo`, `nac`, `scn`, `atl`, `angular_gyrus` — **no `ec`**, not even under `all`. Clearing `nac` alone violates the NAc/EC pairing invariant. | **FIXED** (2026-08-23) — added the missing `"ec": ("agents/*/ec.json",)` key and a `MEMORY_PAIRS` table declaring `{nac, ec}` inseparable; `clear_memory` runs requested types through `expand_paired_types`, so clearing either half pulls in the other and prints why. `all` now includes `ec` (it previously wiped NAc and left EC on disk every time). Unpaired types are unaffected — the expansion is not a blanket widening. Guard: [tests/unit/test_memory_pair_invalidation.py](../../tests/unit/test_memory_pair_invalidation.py) (10 tests; 7 verified to fail against the pre-fix table). **Review fold (cross-confirmed):** the CLI table alone guarded only the CLEAR path, while the LOAD path — where the dangling biases actually manifest — was unguarded in two places. `check_nac_ec_pairing` now runs on the `load.agent()` restore and inside `build_bio_stack`, WARNING on a half-present pair (manual `rm`, interrupted write, partial backup). It is deliberately advisory and does NOT count as corruption for `on_corrupt="raise"`: every agent persisted before EC restore existed on the skeleton path has `nac.json` and no `ec.json`, so refusing would make every pre-existing agent unloadable — and re-saving, the actual fix, requires loading first. (Caught by the full suite during the fold: the first version did refuse, and broke three round-trip tests.) `build_bio_stack` also stopped discarding `NAc.load_safe`'s error return. | `cli_utils.py::MEMORY_PATHS` |
| D3 | **`ec_merge`'s cosine threshold is a hardcoded duplicate pinned by no test.** `cosine_threshold: float = 0.44` duplicates the EC default; unlike the frozen-modality set (which *is* pinned by `test_hivemind_frozen_modalities_match_ec_default`), nothing detects drift. The layer deliberately refuses internal imports, so it cannot read a threshold map. | **OPEN** — trigger: per-modality thresholds (plan F-A). | `hivemind/merge.py:557` |
| D4 | **A same-dimension encoding change defeats the merge dimension-guard, invisibly.** `_cosine` returns 0.0 on dim mismatch — but a place code keeps `dim=384` and the same `"audio"` tag, so old- and new-geometry nodes merge whenever partial cosine clears the threshold. Because `audio` is frozen, the corruption is undetectable: counts and contributors inflate, the centroid never moves. | **OPEN** — gate on `MAXIM_PLACE_CODE_EXTEROCEPTION` default-ON. | `hivemind/merge.py` |
| D5 | **`nac_merge` never folds cluster biases across agents.** Keys are `agent\x1fcluster\x1ftool` and cluster ids are per-agent UUIDs, so cross-agent cluster learning is unioned, never merged. | **RE-OPENED 2026-08-27 — resolves with 1.2 gate 6** (was: ACCEPTED for now). The 2026-08-26 Oasis case study made this load-bearing: gate 6 (a re-keyed `ec_merge` so a merged foreign want reads out) requires exactly the fold this row accepted not doing, and the Exp 52 seeds 42 + 43 must pass Gauntlet #2 merged. The earlier note stands as the design constraint: session-relative spatial bearings are unmergeable without a world anchor (the RSC plan's question, answered yes) — the re-keyed merge must say which bearings it refuses. Tracked as [roadmap gate 8(b)](../plans/roadmap_1_1_to_1_3.md#gates-before-12-oasis--hivemind). | `hivemind/merge.py:506-510`; [oasis_case_study_taught_orient.md](../plans/oasis_case_study_taught_orient.md) |

### Learning path

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D6 | **Hebbian episode binding is inert on the main percept path.** `memory_hub` stashes a **1-tuple** of substrate nodes, and `apply_hebbian_on_close` returns early on `len(nodes) < 2` — a silent no-op. So the binding graph never grows from llm-primary percepts, independent of any resolution question. | **DORMANT since 2026-08-29 (1.1.1, roadmap item 13).** Not wired and not deleted: multi-node percept activation is a hard dependency of the 1.3 fabric's orient-windowed Hebbian binding, so wiring it in 1.1.x would be 1.3 scope creep without 1.3's experiment. `Dormant since` markers are on both ends of the path (`episode.py::apply_hebbian_on_close`'s early return and `bio_integration.py::record_substrate_nodes`), and no docstring implies percept-driven binding learns in production. Resurrection trigger: the 1.3 fabric stage. | `integration/memory_hub.py:348`, `memory/episode.py:795` |
| D7 | **`min_delta = 0.05` is an undocumented second resolution limiter.** `SensorEncoder` short-circuits the EC scan and returns the cached node whenever no sensor moved ≥0.05 — a hard ~4.5% dead zone on a `[-1,1]` sensor **regardless of encoding**. Any population code past ~40 buckets is capped by this gate, not by geometry. | **ACCEPTED** (it is a real efficiency win) — but must be stated wherever resolution is claimed. | `similarity/encoder.py:519, 640-645` |
| D8 | **A read path mutates text centroids.** `bio_enrichment` calls `pattern_complete_or_separate(embedding, "text")` per enrichment query, with an in-code note that the centroid update is intentional reconsolidation (~1/(n+1) shift per query). Consequence not documented anywhere: **querying degrades text-cluster resolution over time.** | **OPEN** — needs measurement, then accept-or-fix. | `bio_enrichment.py:665-667` |
| D9 | **5 of 6 `TemporalEvent` categories have no producer.** Only `tool` emits; the drive emitter is both unwired *and* malformed (raises `TypeError` into a `except Exception: log.debug`). `record_event` even special-cases `deliberation` significance for a producer that does not exist. | **DORMANT since 2026-08-29 (1.1.1, roadmap item 13)** — producers stay deferred ([deferred/scn_event_producer_gap.md](../plans/deferred/scn_event_producer_gap.md)); revival requires answering whether per-event-type phase learning earns its keep. **The malformed half is FIXED:** the drive emitter passed `temporal_signature=`/`metadata=` (fields `TemporalEvent` does not declare) and omitted `event_id`/`event_signature`, and the resulting `TypeError` went into `except Exception: log.debug` — a dead path that looked alive at every log level anyone runs. The construction is correct now and a construction error is reported at WARNING; the emitter is still a no-op in production because nothing passes a distributor. | see plan |

### Claim scope

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D10 | **Exp 45's Earned status does not transfer to an EC-clustered orient policy.** The orient backbone builds state from `az_bin(...)`, a hand-written bin string passed as `current_cluster_id` — it never calls EC. That is hand-curated discretisation *upstream* of the substrate (the interim-contamination pattern), so any future EC-clustered orient policy needs its own experiment. | **OPEN (scope note)** — must be recorded on the Exp 45 graduation row. | `scripts/orient_backbone/live_3_learn.py:680`, `live_common.py:340` |
| D11 | **~432 bare `except Exception: pass` remain repo-wide.** 48 measurement-path sites were instrumented (#487); the rest are grandfathered and un-instrumented, so a silent failure outside that scope is still invisible. | **ACCEPTED** (scoped deliberately) — see [measurement_path_fail_loud.md](../plans/measurement_path_fail_loud.md). | `grep -rEA1 "except Exception:\s*(#.*)?$" src/maxim/` |
| D13 | **A failed planning turn dropped silently and the orchestrator idled forever between turns (the true mechanism behind the D12-era wedges).** A live 2026-08-18 trace ended with narrator HTTP 200 followed by a tool-parse failure, zero later backend calls, and an active-but-idle main loop after the legacy 120s await window closed. Because the orchestrator has no other percept source between turns, nothing could re-arm it. A second flaw existed in the proposed fix: a parsed proposal with `action=None,error=None` was stamped received but never handled, permanently defeating the backstop. | **FIXED** (2026-08-19, PR #523) — every non-executable outcome (fallback/parse failure, error, stale proposal, unregistered tool, parsed no-action, or exact worker job consumed without a proposal) routes through bounded recovery and normal teardown. Liveness now follows the exact job owned by that `LLMWorker`: `PENDING`, `RUNNING`, and `COMPLETED` (published but not consumed) keep the poll awake, eliminating both the 120s slow-model false abort and the call-end/result-queue race. It no longer consults the process-global call registry, which could observe an unrelated worker. Planning and worker-transport failures have separate three-retry budgets, so queue rejection cannot retry forever. Typed terminal status is `llm_wedged` for parse/provider exhaustion, `planning_failed` for responsive but invalid planning (including unregistered/no-action/stale), and `worker_unavailable` for worker/queue exhaustion. Bad-tool retries add the rejected name to the request's corrective prompt instead of replaying the same prompt. Scope remains opt-in (`run_agentic_loop(planning_liveness=True)`) and only the sim orchestrator enables it; operator opt-out is `MAXIM_SIM_PLANNING_LIVENESS=0` (S6 apparatus control). Guards: [tests/unit/test_planning_liveness.py](../../tests/unit/test_planning_liveness.py) (66 tests: exact job lifecycle through `RUNNING`/`COMPLETED`/`CONSUMED`, no-action handling, targeted bad-tool correction, separate bounded transport recovery, typed status, and wiring pins) plus [tests/unit/test_worker_pool.py](../../tests/unit/test_worker_pool.py) (queue rejection removes the orphan registry entry). | py-spy dump 2026-08-18 (MainThread active at `agent_loop.py:4584`, await-gate at `:1691`); `/tmp/heartbeat_e10_p2h.jsonl` backend trace (final call `status:200` then silence; W-logs at +108s; abort at +139s) |
| D14 | **The orchestrator spinner asserted work that was not happening.** The "Orchestrator planning next/first probe…" text could count for hours while py-spy showed no inference in flight, steering diagnosis toward the server rather than the dropped turn. | **FIXED** (2026-08-19, PR #523 — same PR as D13) — `spinner_truth_message` derives the display from observed call/byte state; the between-turn window is owned by `Spinner` and updates use a lock-guarded test-and-set, so stale monitoring cannot overwrite a new turn. Terminal text now uses the typed liveness status (`llm_wedged`, `planning_failed`, or `worker_unavailable`), and `Spinner.stop()` closes the planning window. Retry attempts remain visible in the EXEC stream. Guards: [tests/unit/test_planning_liveness.py](../../tests/unit/test_planning_liveness.py) (`TestSpinnerTruthMessage`, `TestSpinnerPlanningWindow`, and orchestrator wiring pins). | py-spy dump 2026-08-18 (active idle loop, zero calls in flight) vs. simultaneous pane capture ("planning next probe… (3286s)"); `simulation/orchestrator.py` spinner-start sites |
| D12 | **Orchestrator LLM calls blocked unboundedly — ROOT-CAUSED (2026-08-18, `sample` capture of a live 2.4h wedge): `router._inference_lock` inheritance deadlock.** The llm_worker timeout path abandons an orphan thread that can still be inside the locked region; its executor-replacement fallback gives new calls fresh threads but nothing frees the lock, so the untimed `with self._inference_lock` parked every subsequent call forever (~75 lock-waiter threads, ZERO network activity, stall detector blind because registry entry happens inside the lock — 265 impotent nudges observed). NOT a network-timeout escape; the model server was healthy and idle throughout. Deceptive symptom: AUT ticks sensors normally while starving for percepts ("bio systems not engaging"). | **FIXED** — bounded lock acquire + loud failure (`MAXIM_INFERENCE_LOCK_TIMEOUT_S`, default 600s) + the #517 sim-level hard-abort (exit 4) as defense in depth. Guards: [tests/unit/test_inference_lock_timeout.py](../../tests/unit/test_inference_lock_timeout.py) (held lock → bounded loud failure; verified the old behavior never returned) + [tests/unit/test_stall_hard_abort.py](../../tests/unit/test_stall_hard_abort.py). Prune at next sweep per rule 3. | `sample` thread-stack capture 2026-08-18 (80 threads: lock-waiters + zero recv); `models/language/router.py::_complete_text`; `agents/llm_worker.py` orphan path |

### Remaining items surfaced by the D13/D14 review round (2026-08-19)

D22 is now closed by the shared run-integrity contract. D23 remains open until
non-TTY output and lifecycle ownership are fixed globally.

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D22 | **A cleanly unwound aborted sim exited 0, and campaign harnesses did not read its typed status.** D12 only reached exit 4 through `_force_exit`; D13 recorded `llm_wedged`, `planning_failed`, or `worker_unavailable` in `finish_context`, but those values did not become process failure. A campaign could therefore count an aborted row as data. | **FIXED (2026-08-19).** `simulation_exit_code()` centralizes run-integrity policy: generic error exits 1; forced or clean aborts exit 4; semantic experiment outcomes remain exit 0. Direct CLI paths use the mapping, subprocess campaign scripts already reject non-zero return codes, and benchmark/curriculum/Roy/research/Console consumers now reject typed aborts before accepting metrics, chaining state, writing papers, comparing arms, or publishing a successful lifecycle event. Python surfaces retain structured `finish_reason` values. Guards: `tests/unit/test_sim_types.py`, `test_benchmark_runner.py`, `test_research_agents.py`, `test_console_event_seam.py`, plus curriculum/Roy integration tests. | `simulation/sim_types.py::simulation_exit_code`; CLI result exits; typed-abort consumer regression tests |
| D23 | **Raw-terminal spinners can still outlive careless callers and write ANSI frames to captured stderr.** `Spinner._spin` has no `sys.stderr.isatty()` check, and not every bridge caller guarantees `finish()`. The D13 regression test now closes its bridge and `Spinner.stop()` closes the planning window, but those surgical fixes do not make all callers lifecycle-safe or captured output hermetic. | **OPEN — 1.1.x hygiene**, pairs with D20's hermetic-suite work. Add non-TTY suppression and audit bridge ownership/cleanup across tests. | fast-suite stderr tail 2026-08-19; `simulation/spinner.py::Spinner._spin` raw-ANSI branch |

### Stable API / release enforcement

D15–D20 verified 2026-08-19 during the release-readiness review — detailed evidence
and required contracts: [repository_review_2026_08_19.md](repository_review_2026_08_19.md).
D21 and D24 come from the same day's independent claims-verification rounds
(scorecard and live public-site audit, respectively).

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D15 | **Stable `maxim.run()` arguments were silently ineffective.** `goal` was accepted but never reached the loop; `robot` connected but was not attached to the agent/executor/tool registry; `home_dir` remains only partially honored because loop state is CWD-relative. | **1.1 GATE FIXED (2026-08-19, v1.0.9)** for `goal`/`robot`: canonical initial-input ingress, explicit headless conflict, selected-controller motion, and wake/sleep/ownership lifecycle have facade and controller guards. The interruption-scoped lifecycle (`goal` does not auto-stop; `goal=None` is idle, not terminal-interactive) is now explicit. **OPEN — 1.1.x** for complete `home_dir` ownership. | `api.py::run`; `hardware/registry.py`; `tools/reachy.py::MoveTool`; `test_api_core.py`; investigation doc §D15 |
| D16 | **Stable API cleanup began after fallible side effects.** Environment mutation and worker startup preceded `run()`'s cleanup boundary; full-agent construction also abandoned its superseded skeleton ConceptExtractor worker; related setup windows remain in `imagine()`/`campaign()`. | **1.1 GATE FIXED (2026-08-19, v1.0.9)** for `run()`: worker, bio-stack factory rollback, robot lifecycle, run-owned LLM environment overrides, and the exclusive-run lock unwind from partial setup; cleanup stages continue after an earlier failure. **OPEN — 1.1.x** for `imagine()`/`campaign()` and broader process-global configuration isolation. | `api.py::run`; `integration/memory_hub.py::shutdown`; `runtime/agent_factory.py`; `test_api_core.py`; investigation doc §D16 |
| D17 | **`maxim.load.agent()` does not immediately restore every promised subsystem and may silently substitute fresh state for corrupt state.** ATL loads only at later session start; Hippocampus/SCN corruption can be swallowed. | **FIXED** (2026-08-23) — `_create_atl` now restores the ATL inside `create_agent` instead of deferring to `MemoryHub.on_session_start()`, which `load.agent()` returns without calling, so the documented "restores Hippocampus, NAc, ATL" promise is true at return. Corrupt Hippocampus/SCN `except Exception: pass` swallows and NAc's discarded `load_safe` return value now all report through `_note_corruption`, which logs a WARNING and records the file; `create_agent` raises one `MemoryCorruptionError` naming EVERY corrupt file when `AgentConfig.on_corrupt == "raise"`. `load.agent()` defaults to `on_corrupt="raise"` and takes `on_corrupt="fresh"` as the explicit opt-in to empty state (the corrupt file is left on disk). The factory default stays `"warn"` on purpose: `_create_memory_hub` restores SCN for EVERY caller, so a raising default would change behaviour far outside the stable load path (shared-builder activation rule). Guard: [tests/unit/test_load_agent_restore_contract.py](../../tests/unit/test_load_agent_restore_contract.py) (23 tests). **Review fold — three holes the first pass left, all closed here:** (a) `_create_memory_hub` handed `build_memory_hub` a bare `EntorhinalCortex()`, so `load.agent()` returned a RESTORED NAc bolted to a FRESH EC — the exact dangling-bias state D2 describes; EC is now restored from `agents/<id>/ec.json` on the same path. (b) The raise fired AFTER hub construction, leaking a live ConceptExtractor worker and the whole memory graph per attempt (a caller retrying on `MemoryCorruptionError` leaked per retry); the hub is shut down before aborting, as `create_full_agent` already did. (c) The OSError family — which `NAc.load_safe` deliberately does not catch — escaped through the outer `except Exception: return None` handlers leaving `corrupt` empty, so a `nac.json` that was a directory or chmod-000 yielded `nac=None, memory_hub=None` at WARNING level; all four constructors now report. `AgentConfig.on_corrupt` is `Literal["warn","raise"]` validated in `__post_init__` (a bare str made `"Raise"` a silent downgrade on a safety switch). `MemoryHub.on_session_start` no longer re-reads a just-restored ATL, which would have discarded post-load writes. **Verification note:** `PYTHONPATH` does NOT swap the code under test here — `tests/conftest.py` inserts `<repo>/src` at `sys.path[0]` — so a pre-fix check must edit files in place or build a separate tree. | `load.py::agent`; `runtime/agent_factory.py`; `integration/memory_hub.py::on_session_start`; investigation doc §D17 |
| D18 | **`register_tool()` is one-shot although the public extension contract implies continuing availability.** Injection clears the pending list while each API invocation builds a fresh registry. | **FIXED** (2026-08-23) — chose **persistent** registration: option 2 (rename as one-shot) was unavailable because `docs/user/extension_api.md` §2 declares `register_tool` stable and part of the 1.0 contract, which "will not break in 1.x". `_inject_registered_tools` no longer clears the list, so every later `run()`/`imagine()`/`campaign()` sees the tool; re-injection is idempotent because `ToolRegistry.register` keys on `tool.name` and last-wins. This also fixes a second silent symptom: `simulation/orchestrator.py` injects from a SECOND site, so whichever site ran first consumed the registrations and the other got none. Added `unregister_tool(name) -> bool` and `clear_registered_tools() -> int`, both exported via `_API_VERBS`/`__all__`, plus an autouse conftest scrub so a persistent registration cannot leak between tests. Guard: [tests/unit/test_register_tool_persistence.py](../../tests/unit/test_register_tool_persistence.py) (14 tests; the 6 core persistence guards verified to fail against the pre-fix injector). Review fold: `register_tool` now rejects a nameless object at the call site (permanent registration made an unnamed append unbounded and warn on every later `run()`), and `list_registered_tools()` exposes what will be injected. | `api.py::_inject_registered_tools`; `api.py::register_tool`; investigation doc §D18 |
| D19 | **The architecture audit is permanently red and therefore cannot detect regression.** The real audit reports 33 violations on current `main`; the unit test only checks that it returns a list, and CI does not enforce a reviewed baseline. | **FIXED 2026-08-24 (gate); 1.1.x debt burn-down stays open.** All 33 findings classified (10 typing-only under `TYPE_CHECKING`, 16 function-local (lazy) imports — none breaks an import cycle; each pulls a contract type or leaf helper that sits on the wrong side of the boundary, 7 module-level) into a reviewed accepted-debt baseline, `src/maxim/utils/architecture_baseline.json`, keyed by `file::imported_module::scope` + count + accepted symbol set (never line). The fast suite now fails on any finding outside the baseline, any stale entry (debt shrank — tighten the baseline), and any `UNREVIEWED` entry; `maxim --audit-architecture` reports against the same baseline. Verified to fail in place on `from maxim import tools`, on widening an accepted import's symbol list, on a deleted entry, and on an inflated count before landing. Row expires at the next prune; burn-down is roadmap 1.1.x item 11. | `tests/unit/test_architecture_audit.py::TestRealCodebase` (the gate) + `src/maxim/utils/audit.py::compare_to_baseline`; investigation doc §D19 |
| D20 | **The required fast suite was not offline/hermetic.** Installed optional ML dependencies could trigger model downloads; subprocess harnesses and teardown wrote under `~/.maxim`; CI ran only the narrower unit suite. | **1.1 GATE FIXED (2026-08-19, v1.0.9).** The test process and subprocesses use a unique temporary HOME/config/cache root; Maxim path caches reset per test; model hubs default offline; pretrained assets require explicit opt-in; DNS/socket/backoff host dependencies were removed; and CI now runs the documented wider command. Local exact-command evidence: 9,303 passed, 44 resource/platform skips, 41 slow deselections in 4m57s. | `tests/conftest.py`; `tests/unit/test_test_harness_hermeticity.py`; `requires_model_cache`; `.github/workflows/test.yml`; investigation doc §D20 |
| D21 | **`similarity/ec.py`'s `EntorhinalCortex` class docstring claimed "O(1) approximate nearest neighbor via LSH", but the substrate hot path (`pattern_complete_or_separate`) is a pure-Python linear scan over embedding centroids (O(Nd) per call) that never touches the LSH index.** The module docstring was already honest ("cosine-threshold clustering over embedding centroids; does NOT implement DG/CA3 attractor dynamics") — the class docstring contradicted it. | **FIXED (2026-08-19)** — the class docstring now distinguishes indexed signature queries from the exact substrate centroid scan. No runtime mechanism changed. Prune at the next ledger sweep. | `src/maxim/similarity/ec.py::EntorhinalCortex`; `::pattern_complete_or_separate` |
| D24 | **The canonical public website contradicts current release truth.** The 2026-08-19 crawl found Exp 48 still presented as `PASS/GRADUATE` with retired v1-apparatus numbers; the experiment index stops at Exp 48 and hard-codes 76; architecture is described as strictly enforced despite 33 audit findings; EC substrate lookup is described as constant-time; and fear review is claimed for every non-introspection tool although the wrapper is optional and disabled by stable `run()`. `docs.pymaxim.bio` also serves a duplicate marketing homepage instead of redirecting to the documentation entry point. | **CONTENT FIXED — 1.1 audit 2026-08-25 (maxim-web PR #7); browser pass still open.** The 2026-08-20 P0 fixes are verified live (Exp 48 retired numbers gone, derived count, D19 wording, conditional fear gate, O(N·d) EC, non-empty `goal`, `docs.pymaxim.bio` → 308 path-preserving redirect). The 1.1 audit against the exact 1.1.0 wheel added Exp 52 as the headline, superseded Exp 48, corrected the persona / `register_tool` / `--aut-mode` claims, and added the L8 / no-loudness caveats — per-route table in [pymaxim_bio_update_suggestions.md](../announcements/pymaxim_bio_update_suggestions.md) §1.1 audit. Remaining: post-deploy live re-crawl and the human visual/mobile/keyboard/accessibility pass. Original disposition: apply the cross-repo handoff, make the docs alias a path-preserving permanent redirect, rerun command/snippet/link/canonical checks, and complete the deferred visual/accessibility pass. Repo metadata now points directly at the canonical getting-started page so the wheel does not depend on the alias. | live 38-route sitemap crawl, 2026-08-19; [website handoff](../announcements/pymaxim_bio_update_suggestions.md); `AgentConfig.with_fear_gate`; `api.py::run`; D19/D21; Exp 48 apparatus-v2 disposition **2026-08-26: live re-crawl complete after 1.1.0 publish (fresh venv → 1.1.0, flag set identical); Exp 53/53b hardware card + release-notes/video pointers live (maxim-web #8, #9). Remaining: the human browser/mobile/accessibility pass → 1.1.x.** |

### Surfaced by the 1.0.9 pre-publish review (2026-08-20)

(D25/D26 — numbered after the website row D24 above, which the same release pass filed.)

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D25 | **Substrate validation tests OVERWROTE committed experiment records as a side effect of running, with no guard — so a degraded run silently replaced real evidence with artifacts.** `tests/substrate/test_p1_recognition.py`, `test_p2_reward_modulation.py`, and `test_concept_decomposition_validation.py` wrote their sweep output straight into `docs/experiments/results/*.json`, which are S4 committed raw records. Observed 2026-08-20: after the D20 hermeticity work forced `HF_HUB_OFFLINE=1`, the encoder could not load `paraphrase-mpnet-base-v2`, silently degraded, and one ordinary test run rewrote `p1_recognition_sweep.json` from `seeds_passing: 7 / mean_collapse_rate: 0.9168 / means_pass: true` to `0 / 0.0 / false`. The change was caught only because it appeared in `git status` before a commit; nothing in the harness objected. | **FIXED** (2026-08-20, PR TBD) — all three sweeps now publish through `tests/substrate/conftest.py::publish_sweep_results`, which writes to the test's `tmp_path` and prints the location. Updating the committed record requires the explicit `--write-experiment-results` flag AND passes the D25 apparatus check first, so replacing evidence is a deliberate, reviewable act instead of a side effect. No direct write to `docs/experiments/results/` remains in `tests/`. Guards: [tests/substrate/conftest.py::publish_sweep_results](../../tests/substrate/conftest.py) + [tests/substrate/test_evidence_write_policy.py](../../tests/substrate/test_evidence_write_policy.py). | the reverted `git diff docs/experiments/results/p1_recognition_sweep.json` from the 2026-08-20 offline run |
| D26 | **A missing encoder degraded into a fake scientific result instead of an error.** With the weights absent, `LinguisticEncoder` logged one WARNING and continued on `_fallback_embed` — whose own docstring says the vectors are "not semantically meaningful — paraphrase collapse will NOT work" — so `test_p1_recognition` reported `AssertionError: Substrate only beats random by -0.9%, need >30pp`, the exact shape of a genuine regression of the central claim. The three affected classes gated on sentence-transformers being *installed*, which the D20 work established is not evidence the weights are present. | **FIXED** (2026-08-20, PR TBD) — added `similarity/encoder.py::require_semantic_encoder(context=...)`, which attempts the real load and raises `ModelLoadError` naming the run, the cause, and both fixes. The three sweep classes call it in an autouse fixture BEFORE measuring, so a missing model is now a pytest ERROR on the apparatus rather than a FAILURE against the science (verified: the -0.9% assertion is replaced by the typed error). (`LinguisticEncoder.using_fallback` already existed for reporting realized state; the gap was that no measurement path consulted it.) The fallback itself is unchanged — it is correct for plumbing and wrong only for measurement, which is the distinction the assertion draws (apparatus standard S3). Guards: [tests/substrate/test_evidence_write_policy.py](../../tests/substrate/test_evidence_write_policy.py). | `similarity/encoder.py::require_semantic_encoder`; the pre-fix WARNING at `encoder.py::_get_encoder` followed by a science-shaped assertion |

| D27 | **Seven `scripts/` harnesses still overwrite committed S4 evidence unconditionally, and none asserts its apparatus.** D25/D26 were closed for `tests/substrate/`, but `fine_sweep_phase_2.py:221`, `measure_p1_at_threshold.py`, `p4_mug_test_sweep.py:288`, `p4_clip_calibration_sweep.py:345`, `p4_cross_modal_sweep.py:582` (which also rewrites a committed `.md`), `p4_vram_audit.py:383+`, and `p4_option2_measurement.py:736` write `docs/experiments/results/*.json` as a side effect with no flag, and ~18 files under `scripts/` construct an encoder without calling `require_semantic_encoder`. The defect class is identical — a degraded run publishes over real evidence — only the surface differs. Surfaced by the 1.0.9 two-lens review (cross-confirmed by both lenses). | **OPEN — RE-FILED 2026-08-27 as [1.2 gate 8(a)](../plans/roadmap_1_1_to_1_3.md#gates-before-12-oasis--hivemind)** (was "OPEN — 1.1 gate"; 1.1.0 shipped 2026-08-26 without it — the disposition was an orphan, caught by the scorecard reconciliation). Extend `test_evidence_write_policy.py`'s scan to `scripts/`, give the harnesses the same `--write-experiment-results`-style opt-in, and wire `require_semantic_encoder` into each measurement entry point. Rationale for the 1.2 placement: the Exp 52/54 files a bundle would share are produced by this class of harness. | `grep -rln "experiments/results" scripts/*.py` → 7 files; [tests/substrate/test_evidence_write_policy.py](../../tests/substrate/test_evidence_write_policy.py) scans `tests/substrate/` only |

### Surfaced by the D17/D18/D2 two-lens review round (2026-08-23)

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D28 | **`create.agent()` silently restores SCN from disk despite documenting "always start fresh."** `AgentFactory._create_memory_hub` binds `scn.json` and calls `scn.load(...)` whenever the file exists — **regardless of `auto_load`** — while `create_agent`'s own docstring says `auto_load=False` means "always start fresh (`maxim.create.agent()`)". So a "fresh" agent inherits the previous session's temporal state, and only SCN, making the fresh/loaded distinction incoherent rather than merely incomplete. Pre-existing; surfaced by the D17 corruption-collection work, which made the inconsistency load-bearing (a corrupt `scn.json` is now the one file a `create.agent()` call will actually read and report). | **OPEN — 1.1.x, and a [1.2 gate 8(c)](../plans/roadmap_1_1_to_1_3.md#gates-before-12-oasis--hivemind) prerequisite (2026-08-27):** must land BEFORE the Oasis ingestion contract is drafted — that contract defines what a bundle merges *into*, which is undefinable while "fresh" is not fresh. Gate the SCN restore on `auto_load` like the other three subsystems, or change the documented contract to name SCN as an exception. Not a 1.1.x release blocker: the behaviour is long-standing and the D17 guard now makes it visible rather than silent. | `runtime/agent_factory.py::_create_memory_hub` (`scn.load` under `if scn_file.exists()`, no `auto_load` check) vs `create_agent`'s `auto_load` docstring; `tests/unit/test_load_agent_restore_contract.py::test_create_path_is_unaffected_by_corrupt_files` |
| D29 | **A committed sweep record cited as a regression guard is half synthetic.** The first `sweep_start` in `docs/experiments/data/45_doa_sweep_baseline.jsonl` (and the copy inside `45_doa_sweep_post_headfix.jsonl`) carries `"dry_run": true`; re-running `doa_sweep.py --dry-run` reproduces its numbers exactly (+0.569 / +0.571, R² 0.9785 / 0.9800). `docs/plans/behavioral_graduation_candidates.md` cites the file as a regression guard for the orient row without noting that half of it is simulated, and the point records carried no marker at all. | **OPEN — 1.1.x.** `dry_run` now rides on every record going forward ([L9](../limits/README.md)), so new files are self-describing; what remains is to annotate or re-cut the committed 45-series files and correct the graduation-row citation. Analyses on this branch already exclude `dry_run` records. | `45_doa_sweep_baseline.jsonl` `sweep_start` #1; `scripts/orient_backbone/doa_sweep.py::emit`; graduation row citation |
| D30 | **Head roll drifts under repeated body-only turns and nothing re-pins it.** Over the 2026-08-24 H1 `_big` block (16 alternating ±51.6° body turns through the production `ReachyOrientMotorBackend`, which commands `body_yaw` only), the controller's F1 early-warning fired repeatedly on recenters: achieved `head_roll` diverged from the last commanded by −5.3° → −8.7° and held there (magnitudes from the operator console — the harness did not yet record head pose per turn, so this is unarchived evidence pending the next run, which records world head yaw/roll pre/post per turn). Session 2 pinned roll/pitch explicitly for the envelope measurement and saw ≤ ±4°; the production path never pins them. Consequence: the mic array tilts during long orient sessions — a candidate for the right-turn Δaz shortfall recorded in Part C. Bounds the [Exp 45 orient row](../plans/behavioral_graduation_candidates.md) (`_big` delivered-shift evidence). | **OPEN — 1.1.x.** Decide whether the backend should ship explicit roll/pitch = 0 with each body turn (one-line change in `execute`) or whether the controller should re-pin on divergence; either way add the drift to the block's per-turn record and re-run the block. Trigger: motor-binding Phase-3 gain calibration or any change to `goto_target`'s retained-axes handling. | `hardware/reachy/controller.py::_warn_commanded_vs_achieved` warnings in the 2026-08-24 block run; `hardware/reachy/motor_backend.py::execute` (body_yaw-only `MotionTarget`); [H1 Part C follow-up](../experiments/protocols/h1_healthy_hardware_doa_preregistration.md) **Re-measured 2026-08-26 (Exp 53/53b, head pose recorded per turn, 360 turns at δ 0.55 and 0.30 rad): roll 13–19°, pitch 3–11°, sustained across the session, with the F1 divergence warning on nearly every goto; pattern completion and the direction readout were unaffected, but this is the number the D30 fix should be measured against.** |
| D31 | **The production credit path has no fold guard: a mirror-image DoA reading became a credited azimuth transition.** `ReachyOrientMotorBackend._measure_azimuth_after` takes the FIRST feed window that begins after `t_end + settle` and returns it; on 2026-08-24 (H1 `_big` block, aborted run, record `i=1`) that window read +0.289 while an independent later median-of-5 read −0.289 — an L10 sign-flip, which then landed in `measured_drive_transitions` and therefore in the SIGN of the relief credit a graduated mechanism learns from. 1 of 18 turns in one session; the sweeps put fold rates far higher at larger `|psi|`. | **OPEN — 1.1.x.** Require sign agreement across two post-settle windows (or a window and the modeled sign) before a transition is credited; on disagreement return `None` (sparsity is acceptable, a fabricated sign is not — the backend's own contract). Land the guard test with the fix. Trigger: any change to `_measure_azimuth_after` / DoAFeed windowing, or the next live orient session. Bounds the [Exp 45 orient row](../plans/behavioral_graduation_candidates.md). | `hardware/reachy/motor_backend.py::_measure_azimuth_after`; `docs/experiments/data/h1_partc_big_block.jsonl` run `20260824T213320Z-76884` `i=1` (`d_az_backend` +0.1556 vs `d_az_script` −0.4222); [L10](../limits/README.md) |
| D32 | **`CONSTITUTION.md` is a root marker, not a source: the "CORE PRINCIPLES" prompt block is a hardcoded paraphrase in `agents/llm_context.py`, gated only on the file's existence — the document and the prompt can drift silently, and `pip`-installed users (no repo root above `site-packages`) get an empty preamble.** Surfaced by the 2026-08-26 cleanup review (both lenses). | **OPEN — 1.1.x.** Either load the block from the file (and ship it as package data) or state in `CONSTITUTION.md` that the runtime text lives in code; add a test that fails when the two diverge. | `src/maxim/agents/llm_context.py::_load_foundational_context`; `tests/unit/test_llm_context.py` |
| D33 | **`maxim-diagnostics` (`utils/reachy_diagnostics.py`) duplicates the daemon probe logic that `maxim doctor`'s `check_robot_reachable` now owns, and the archived plan's disposition ("becomes a thin shim over the doctor check") never ran — two probes drift (the script still lacks the SDK/daemon era check; the doctor still lacks ping / dashboard / :8443).** Surfaced by the 2026-08-26 cleanup review. | **OPEN — 1.1.x.** Convert the console script to the shim (keep the entry point — removing it is a public-CLI break) and port ping/:8443/dashboard into the doctor check. | `pyproject.toml` `[project.scripts]`; `docs/plans/archive/doctor_robot_reachable.md` §consolidation |
| D37 | **Every diff-scoped lint was a no-op on pull requests.** The `lint` job checked out at depth 1 and each step ran `git fetch origin main --depth=1`, leaving two disjoint shallow roots: `git merge-base origin/main HEAD` failed, and the shared graceful-skip path printed `INFO: no base ref … skipping` and returned 0. Verified in CI run `33259722155` (2026-08-29): the multi-agent marker lint and `lint_no_silent_swallows.py`'s check 2 had been vacuous on EVERY pull request since they shipped, so "no NEW bare swallows" and the P4 marker rule were enforced by nothing. Surfaced by the 1.1.1 Cluster B review round (Executor lens). | **FIXED 2026-08-29 (#570).** The `lint` job checks out with `fetch-depth: 0` and the `--depth=1` fetches are removed; `scripts/_lint_git.py::must_not_skip` turns a missing base ref into a hard ERROR whenever `GITHUB_EVENT_NAME == pull_request`, so the skip can never silently return again. All four diff-scoped lints share that path. | `.github/workflows/test.yml` (`lint` job checkout); `scripts/_lint_git.py::must_not_skip`; `tests/unit/test_lint_atomic_io_ratchet.py::test_must_not_skip_is_true_only_on_a_pull_request` |

### Surfaced by the Exp 53b R1 replication (2026-08-28)

Harness gaps found by [R1](../experiments/53_cross_context_readout.md) — filed against the harness, not the result; the Exp 53/53b EARNED row in [behavioral_graduation_candidates.md](../plans/behavioral_graduation_candidates.md) rests on R1's complete run, which the D34 fix selects by construction.

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D34 | **`exp53_cross_context_readout.py verdict` pooled every trial in the file across `run_id`s** — partial starts, re-runs and `--only` debugging subsets included; there was no run filter. The Exp 53b R1 file (2026-08-28) holds five `start` records, and the printed 1.00 / 0.00 / 0.50 aggregated 219 trials from three Phase 2 starts (harmless there — the complete run alone gives the same three numbers — and not in general). Found by R1 ([53_cross_context_readout.md](../experiments/53_cross_context_readout.md) §Harness gaps). | **FIXED 2026-08-29 (1.1.1 Cluster A, PR #569).** `verdict` groups records by `run_id`, selects the ONE complete run per (phase, condition) — complete = every loaded agent has `agent_done` and `run_end.status == complete` when present; a `--only` subset is `debug`, never a result — refuses (exit 2) when two complete runs exist for one block unless `--run-id` pins it, and records `runs_used` / `runs_excluded` in the gate record. Verified: identical gate numbers on the original 53 / 53b files and on R1 (exploratory placement n 15 → 9, the complete run alone). | `scripts/orient_backbone/exp53_cross_context_readout.py::select_run`; `tests/unit/test_exp53_verdict.py` |
| D35 | **The controller's H1 F1 early warning (achieved-vs-commanded pose divergence, `ReachyMiniController`) was console-only** — R1's one `body_yaw −57.1°` divergence and the per-move `head_roll`/`head_pitch` (D30) warnings exist nowhere in the archived JSONL; the file carries `head_pre`/`head_post` but not the controller's verdict. | **FIXED 2026-08-29 (1.1.1 Cluster A, PR #569).** `cmd_run` attaches a `logging.Handler` to the `maxim.hardware.reachy` logger for the run's duration; every WARNING+ becomes a `controller_warning` record (`logger`, `level`, `message`, `ts`) in the same file, joinable to trials by `ts`. | `scripts/orient_backbone/exp53_cross_context_readout.py::_WarningsToJsonl` |
| D36 | **A partial run left no marker saying whether it was interrupted or crashed** — a Phase 2 start ended by `Motion command rejected by controller` and one stopped by Ctrl-C look identical (last agent has no `agent_done`). | **FIXED 2026-08-29 (1.1.1 Cluster A, PR #569).** Every run closes with a `run_end` record: `status` complete (rc 0) / stopped (a stop rule returned non-zero, `rc`) / interrupted (KeyboardInterrupt) / error (`error: "Type: message"`); a run with no `run_end` at all died before the harness could write one. `verdict` reads it as authoritative. | `scripts/orient_backbone/exp53_cross_context_readout.py::cmd_run`; `tests/unit/test_exp53_verdict.py` |
| D38 | **The published v1.1.0 GitHub Release notes carry 7 repo-relative links, which 404 on the Releases page** (and v1.1.0rc1 carries 1). Named by the 2026-08-27 score card; the notes were published from a source that used `../` paths. | **OPEN — operator action, 1.1.1.** The SOURCE is fixed (`docs/announcements/release_1_1_0.md` rewritten to absolute URLs 2026-08-29, and `tests/unit/test_audit_release_tags.py` keeps every publishable source clean), so the NEXT release publishes correctly; editing an already-published Release body is outward-facing and left to the operator: `gh release edit v1.1.0 --notes-file docs/announcements/release_1_1_0.md`. Remove the `1.1.0` entry from `GRANDFATHERED_RELEASES` in the same act, or `release-audit` fails on "now PASSES". | `scripts/audit_release_tags.py::GRANDFATHERED_RELEASES`; `docs/announcements/release_1_1_0.md` |
| D39 | **v1.0.9, v1.0.0 and v0.9.0 GitHub Releases have ZERO assets**, and 0.6.0–0.9.1 have no Release object at all, while PyPI serves all of them — a tag says which commit, a Release hands over the artifact. | **OPEN — accepted debt.** Grandfathered by explicit list with reasons in `scripts/audit_release_tags.py`; the 1.0.9 wheel/sdist were not kept after upload, so attaching them means re-uploading files that cannot be proven identical to the published ones. Every new release must comply from 1.1.1 on. | `scripts/audit_release_tags.py::GRANDFATHERED_RELEASES`; `release-audit` CI job output |
| D40 | **`api.campaign()` documented three parameters and referenced none of them** (`npc_model`, `interactive`, `prompt_handler`) — a public argument with no observable effect (score card 2026-08-27 N1, reproduced black-box). Root cause for two of them is that `party_mode` is parsed into the campaign definition and read by NOTHING: there are no party-mode NPC agents, so `npc_model` has nothing to configure. | **PARTIALLY FIXED 2026-08-29 (1.1.1 Cluster D).** `interactive` is THREADED (`sim_logger.set_interactive_mode`, restored after the run — the orchestrator reads it when building the campaign's prompt handler); `npc_model` and `prompt_handler` now RAISE `NotImplementedError` before any side effect rather than being silently dropped. **Still open:** (a) `party_mode` remains inert, so party-mode NPC agents — and with them a real `npc_model` — do not exist; (b) `prompt_handler` COULD be threaded: `simulation/campaign_runner.py` already takes one and `bootstrap.build_tool_registry(prompt_handler=…)` consumes it — only `start_simulation_mode` lacks the parameter, so this is a missing passthrough, not an infeasibility. Both are 1.1.x+ work, not shipped here. | `src/maxim/api.py::campaign`; `tests/unit/test_api_expansion.py::TestCampaignParametersAreThreadedOrRejected` |
| D41 | **`AgentInstance.shutdown()` produced unloadable state** (score card 2026-08-27 N2): it calls `memory_hub.on_session_end()`, an atomic test-and-CLEAR that returns `{}` when no session was started — and neither `create.agent()` nor `load.agent()` called `on_session_start()`. The public create→mutate→shutdown→load cycle therefore wrote only `hippocampus.json` + `nac.json` and dropped EC, SCN, ATL and the Angular Gyrus, so every later `load.agent()` logged "Half-present NAc/EC pair" on the API's own output — the orphaned-bias state D2/D17 were fixed to DETECT, produced by the API itself. | **FIXED 2026-08-29 (1.1.1 Cluster D).** Root cause fixed at the lifecycle, not the symptom: the instance OPENS the session it later closes (`AgentFactory.create_agent`, both the create and load paths), and `MemoryHub.on_session_start()` is idempotent so the runtime's own `start_bio_session` no-ops on an adopted persistent agent instead of re-running the restore (which calls `atl.load()`, clearing before restoring). **Second review round (2026-08-29):** the first fix's `on_session_end` warning fired on EVERY clean runtime shutdown, because the agent loop's `start/end_bio_session` pair closes the session the instance opened — normal nesting, not the defect. The guard now keys on `_session_ever_started` (was a session EVER opened on this hub) rather than on `_session_active`, so it detects the defect and stays quiet on the nesting. Residual, documented: mutations made after the loop closes and before `shutdown()` are not persisted by that call. | `src/maxim/runtime/agent_factory.py::AgentInstance.start_session` (called on the FINAL hub — `create_full_agent` swaps the skeleton out, so opening inside `create_agent` opened a hub that was then discarded); `src/maxim/integration/memory_hub.py::MemoryHub.on_session_start`; `tests/unit/test_api_core.py::test_create_mutate_shutdown_load_round_trips_ec_scn_atl` |
| D42 | **`build_bio_stack` constructed a PATHLESS `SCN()`, so the runtime path never persisted temporal state.** `AgentFactory._create_memory_hub` binds `scn.json` at construction, but the bio-stack hub that `create_full_agent` swaps in (every `api.run()`, CLI, orchestrator AUT/NPC and console-handle agent) did not — so `on_session_end` had nothing to save to and every runtime agent silently lost its SCN signatures between sessions. Invisible because a pathless SCN is indistinguishable from an empty one. Found 2026-08-29 while verifying the D41 fix. | **FIXED 2026-08-29 (1.1.1 Cluster D).** `SCN(persistence_path=str(p / "scn.json"))` bound at construction (closing the same concurrent-construction race the factory closed), restored only when `load_persisted` — the orchestrator NPC's write-but-don't-read contract is preserved (that agent's `scn.json` is therefore rewritten empty each run, as its `nac.json`/`ec.json` already were). An UNREADABLE `scn.json` leaves the SCN pathless for that session — binding the path made this file writable for the first time, so a corrupt one would have been overwritten with empty state at session end; going pathless preserves it without touching it, loudly, and that session simply does not persist temporal state. | `src/maxim/runtime/bio_stack.py::build_bio_stack`; `tests/unit/test_api_core.py::test_full_agent_construction_opens_the_session_on_the_hub_it_keeps`, `::test_write_but_dont_read_agents_do_not_restore_scn` |
| D43 | **A merged foreign want reads out as exactly `0.0`, and the merge reports success.** `hivemind/merge.py::ec_merge` computes its right→left node alignment internally and **returns only the merged node set — the id map is discarded**, while `nac_merge` folds `cluster_reward_bias` on identical string keys only. Bias keys are the triple `(agent_id, cluster_id, tool_signature)` (`nac.py::NAc.get_cluster_reward_bias`), so a merged foreign want misses on **two independent axes**: the foreign `cluster_id` never matches AND the foreign `agent_id` never matches. The agent-id half also kills the prompt-annotation path (`nac.py::get_agent_tool_biases`, which filters `aid != agent_id`). No crash, no warning; `len(cluster_reward_bias)` grows, so the CLI prints success. Surfaced 2026-08-30 by the 1.2 scoping dive, confirming and extending 1.2 gate 6. | **OPEN — 1.2 gate 6 (blocking).** `ec_merge` must return the id map; `cluster_reward_bias` + `cluster_reward_source` must be re-keyed through it before `nac_merge`, with N→1 fold semantics decided (mean / max / count-weighted). **The agent-id half is in NO plan doc** — gate 6 and the Oasis case study name only the cluster half; a bias-key identity namespace is a genuine design gap. Third barrier behind both: `tool_signature` is body-prefixed (gate 7). | `src/maxim/hivemind/merge.py::ec_merge`, `::nac_merge`; `src/maxim/decisions/nac.py::NAc.get_cluster_reward_bias`, `::get_agent_tool_biases` |
| D44 | **No test asserts a BEHAVIORAL consequence of a merge, and the two federation experiments that pass deliberately dodge the failure mode.** `tests/unit/test_hivemind_bundle.py::test_end_to_end_hivemind_round_trip` runs the full pipeline but asserts dict equality on hand-set, already-matching keys — never `recommend_action`. The committed federation records (`scripts/orient_substrate/5_operant_creche_federation.py`, `7_graded_creche_federation.py`) pass on merged state, but every infant uses `AGENT = "infant"` with one shared encoder and EC — the script's own comment says *"SAME id for every infant so the merged cluster keys align"*, and its docstring says independent agents "need `ec_merge` alignment first". So the only green evidence for sharing is from the configuration in which D43 cannot fire. | **OPEN — 1.2 gate 6 (evidence).** A merge test must assert a behavioral delta (a `recommend_action` / directedness change), not dict contents, and must use two genuinely independent agents (different `agent_id`, independently encoded EC). Lands with D43's fix. | `tests/unit/test_hivemind_bundle.py::test_end_to_end_hivemind_round_trip`; `scripts/orient_substrate/5_operant_creche_federation.py` |
| D45 | **`SpatialMemoryBridge` restores zero, always — the cross-session spatial prior is structurally dead.** `bridges/spatial_bridge.py::SpatialMemoryBridge.on_session_start` reads `memory.perception.observations["position"]`, and **nothing anywhere writes a `position` key** into percept metadata (the only `"position"` producers are internal to `salience/salience_network.py`). The bridge is constructed and connected (`integration/memory_hub.py::MemoryHub.connect(spatial=...)`, `embodied_runtime/agentic_runtime.py`), so it looks wired. Companion finding: `memory/spatial.py::SpatialContext` (room/zone/x/y/z/heading) has **zero callers**. Surfaced 2026-08-30 by the 1.2 scoping dive. | **OPEN — 1.2 (spatial descope).** Either write `position` into percept metadata at the producing seam, or mark the bridge `Dormant since` with a resurrection trigger. `SpatialContext` is the intended carrier for the landmark-anchored symbolic location the 1.2 scoping adopted — wire it there or say it is unused. Do NOT build allocentric mapping: the Reachy Mini cannot translate. | `src/maxim/bridges/spatial_bridge.py::SpatialMemoryBridge.on_session_start`; `src/maxim/memory/spatial.py::SpatialContext` |
| D46 | **A docstring names a percept-transport module that does not exist.** `simulation/sources.py` refers to a "1.1 file `agents/remote_percept_source.py`"; the file was never written, and `mesh/message.py::MeshMessageType.PERCEPT_PUSH` (plus `EXPERIENCE_OFFER`, `CAUSAL_LINK_SHARE`) are enum slots with **zero producers** — `mesh/bus.py::LocalMessageBus` is in-process only. Live percept sharing therefore does not exist at any layer, while the docstring implies it shipped. | **OPEN — doc truth, 1.1.x.** Correct the docstring to name `deferred/mesh_perception_transport.md` as the owner, or delete the reference. The capability itself is deliberately deferred; only the claim is wrong. | `src/maxim/simulation/sources.py`; `src/maxim/mesh/message.py::MeshMessageType` |
| D47 | **A wheel built from a worktree ships ZERO Console UI files, and `twine check` PASSES it.** `src/maxim/console/ui_dist/` is `.gitignore`d with **zero tracked files** — it is copied in at release time by `scripts/vendor_console_ui.py` and exists only in the checkout that was vendored into. v1.1.0 shipped exactly 6 files under that path; a 2026-08-30 worktree build shipped 0, and `maxim serve` degrades silently to the "no UI installed" page for every pip user. Invisible on a dev box, which sets `config.json::console.ui_dist` to an external path and serves a working Console regardless of the wheel's contents. | **FIXED 2026-08-30 (1.1.2 Cluster D).** `scripts/audit_release_build.py` asserts `maxim/console/ui_dist/index.html` is in the built wheel, wired into a NEW `release-build` CI job (nothing in CI built a wheel before this) that stubs a bundle and builds — so the pyproject `console/ui_dist/**/*` package-data glob is exercised on every PR and push. The D47 branch's negative control is `tests/unit/test_audit_release_build.py::test_missing_ui_dist_fails`, which builds a synthetic wheel with the defect: the guard is shown to FAIL, not merely to pass. `scripts/vendor_console_ui.py --check` already detected this and is a documented MANUAL step in the publication guide — it had no AUTOMATED caller, which is why a build that skipped it sailed through. | `scripts/audit_release_build.py::audit_wheel`; `.github/workflows/test.yml` job `release-build`; `tests/unit/test_audit_release_build.py` |
| D48 | **A stale-branch build produced a 1.1.0-versioned wheel and `twine check` PASSED that too.** Nothing compared the BUILT artifact's version against `pyproject.toml`. `scripts/lint_version_sync.py` compares the repo's version strings to each other — a stale branch is internally consistent, just wrong — and `scripts/audit_release_tags.py` runs POST-publication against PyPI. So the window between "build" and "upload" had no version check at all, which is the only window in which the mistake is still cheap. | **FIXED 2026-08-30 (1.1.2 Cluster D).** `scripts/audit_release_build.py` asserts wheel-filename version == `.dist-info` METADATA version == `pyproject.toml` version, in the same `release-build` CI job. Both halves are checked because the filename alone is not enough (`::test_metadata_version_drift_fails`), and the version assertion is deliberately NOT waivable by `--allow-missing-ui-dist` (`::test_version_check_is_not_waivable_by_the_ui_flag`). | `scripts/audit_release_build.py::audit_wheel`; `tests/unit/test_audit_release_build.py::test_wrong_filename_version_fails` |
| D51 | **A clamped or unreached motion is credited to the substrate as a SUCCESS.** `tools/reachy.py::focus_on_sound` returns `ToolResult(success=True, …)` even when `clamped` is true or `reached` is false — deliberately, and its *payload* is scrupulously honest (`faced_sound`, `clamped_to_head_limit`, an explanatory `note` for the LLM). But `runtime/tool_dispatch.py::record_outcome` computes `learn_success = success and not embodiment_failed`, and a clamp populates neither `embodiment_failures` nor any other channel, so the substrate books `Valence.POSITIVE` + `cluster_reward = +1.0` (`credit_source="tool_success"`) for a motion the tool itself reports did not happen. Companion finding: `ReachyMiniController.last_clamped_axes` has exactly one consumer in `src/` (`tools/reachy.py`, LLM prose) and no path to the PainBus, the NAc, or any modality channel — a refused command is invisible to the learner. Surfaced 2026-08-31 by the microduck design pass. | **OPEN — 1.1.x.** **Claim linkage (rule 4): no EARNED row is bounded** — but not for the obvious reason. The graduated orient harnesses do not route credit through `record_outcome` at all: Exp 53/53b/54 (`scripts/orient_backbone/exp53_cross_context_readout.py`) are **readout-only by their own module docstring**; Exp 45 and Exp 52 Phase A call `NAc.update_cluster_reward` / `credit_operant_reward` directly; only Exp 52 Phase B (`scripts/benchmark_cradle_mother.py`) runs the full dispatch, and it sets `MAXIM_OPERANT_ONLY_CREDIT=1`. Note that flag nulls **only** the cluster-reward term — `learn_success`, the `Valence.POSITIVE` causal link via `nac.observe`, and `credit_goal(+1.0)` fire regardless — so the bound is over the **cluster-bias surface** the graduated readouts measure, not over the substrate as a whole. It bites the LLM-primary runtime driving a real robot. Fix must key on the OUTCOME (did azimuth improve — `potential_diff` already computes it), **not** on clamp-occurrence: flipping valence on every clamp would book NEGATIVE for a clamped turn that nonetheless centred the sound, inverting the bug. Preferred path is reviving `proprioception/pain.py::PainDetector::_check_movement_failure`'s graded comparator on the `agent_loop` path; routing clamps into `side_effects["embodiment_failures"]` was considered and REJECTED (a clamp is a refusal, not harm). Design discussion: `docs/plans/microduck_intent_layer.md` §5.2. **Id note:** numbered D51, deliberately skipping D49/D50 to leave room for PR #577's renumber of the duplicate D43/D44 ids; renumber this row if that lands differently. | `src/maxim/tools/reachy.py::FocusOnSoundTool` (the `success=True` return under `clamped or reached is False`); `src/maxim/runtime/tool_dispatch.py::record_outcome` (`learn_success = success and not embodiment_failed`); `src/maxim/hardware/reachy/controller.py::ReachyMiniController.last_clamped_axes` |

### Surfaced by the 2026-08-29 documentation-truth pass (pymaxim.bio reference gaps)

Found by writing the five reference pages that replace the retired
`dennyschaedig.com/maxim` guides, and checking every claim against the 1.1.0 wheel rather
than against the docs describing it. The doc corrections landed separately (branch
`docs/user-doc-truth-1-1`); these are the code gaps behind them, which did not.

Numbered after 1.1.1's D34–D42, which are now on `main`.

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D43 | **The benchmark suite format promises three things the runner does not do, and two of its four advertised tiers cannot run.** (a) A suite entry's `weight` is parsed into the scenario descriptor and never read again — the composite is a flat mean, not a weighted average. (b) There is no per-scenario score for weights to compose from; `per_scenario` holds raw metrics, folded across repeat runs with a running half-mean `(old + new) / 2`, which equals the true mean only for two runs. (c) `benchmark.metrics` does not select which metrics are scored — every collected metric enters the composite. (d) `maxim --benchmark tier2` / `tier3` without `--campaign` resolve to `scenarios/benchmarks/biosystem_suite.yaml` / `embodiment_suite.yaml`, which do not exist, so the run aborts with `FileNotFoundError` (verified on the 1.1.0 wheel); `maxim.benchmark(suite="biosystem")` fails the same way. (e) `_collect_tier3_metrics` needs an `embodiment_stats()` introspector method that nothing implements, so Tier 3 is always empty. | **OPEN — 1.1.x.** Cheapest honest fix: apply `weight` in the aggregation (or delete it from the schema), replace the half-mean fold with a proper mean, and either ship the two missing suites or drop `tier2`/`tier3` from the `--benchmark` choices. Docs corrected 2026-08-29 (`docs/user/benchmarks.md`) so the file no longer over-promises in the meantime. No claim on the site or in the ledgers rests on a benchmark number, so nothing downstream is bounded. | `simulation/benchmark.py::_load_suite` (`weight` stored at the `"weight": weight` entry, no reader), `::_aggregate_runs` (`per_scenario` half-mean; `_compute_composite_score(aggregated)`), `::_compute_composite_score`, `::_collect_tier3_metrics`; `cli.py` `_tier_campaigns`; `ls scenarios/benchmarks/` (6 files, no `biosystem_suite.yaml` / `embodiment_suite.yaml`) |
| D44 | **Two campaign-YAML keys are parsed onto `CampaignDef` and then ignored, so a campaign author is told nothing.** `party_mode` is loaded from the YAML and stored, but there is no `PartyDMRuntime`, NPCs get no Hippocampus or NAc, and `dm_runtime.py` never branches on it; `choice_resolution` (default `"pc_decides"`) is parsed and has no reader anywhere. **Relationship to D40 (1.1.1):** D40 covers the *public API* half — `maxim.campaign()` documenting parameters it never referenced — and 1.1.1 fixed it by making `npc_model` and `prompt_handler` raise `NotImplementedError`. This row is the half D40 does not reach: the YAML keys themselves still load silently, and `choice_resolution` is not mentioned by either. A campaign author who sets `party_mode: true` gets no warning and no party. | **OPEN — 1.1.x.** Warn at load when `party_mode: true` is set (one line in `load_campaign`), or implement the runtime; delete `choice_resolution` from the schema until something consumes it. Documented meanwhile in `docs/user/dm-campaigns.md`. | `simulation/dm_schema.py` (fields, parse, construct), `grep -rn "party_mode\|choice_resolution" src/maxim/simulation/dm_runtime.py` → no matches; `api.py::campaign`; D40 |

## Pending, not yet a defect

- **The S1 annotation renderer will break PR #497's S4 parser** (its regex matches the band
  exactly; adding `— why` inside the bracket makes it report "no annotation" silently).
  Not yet a defect because neither has merged — but they must land together with the
  parser fix, or this becomes D12 on the day they don't.
