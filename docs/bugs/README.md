# Known defects & limitations — running ledger

**What this is.** The fourth ledger. The repo already tracks *behavioral* claims
([behavioral_graduation_candidates.md](../plans/behavioral_graduation_candidates.md)),
*algorithmic* claims ([bio_faithful_roadmap.md](../plans/bio_faithful_roadmap.md)), and
*engineering rules* (the repository's agent-guidance invariants). This tracks **what is verifiably wrong or
bounded right now** — the axis on which findings otherwise evaporate into plan-doc asides.
(A fifth ledger, [docs/limits/README.md](../limits/README.md), tracks measured
*instrument* limits — nothing broken, but properties every experiment design must
respect. Defects go here; characterized measurement limits go there.)

**Why it matters more than a normal bug list.** The 2026-08-10 external critique's sharpest
point was that unstated limitations quietly become false claims. A defect that lives only
in one session's memory is indistinguishable, six months on, from a defect that was fixed.

## Rules (these are what stop this becoming a second CLAUDE.md)

1. **Verified only.** Every row cites `file:line` or a measurement. No suspicions — those
   belong in a plan's open-questions section.
2. **Every row has a disposition**, not just a description: `OPEN` (will fix, trigger
   named), `ACCEPTED` (deliberate limitation + why), `DORMANT` (the mechanism stays
   wired but nothing new builds on it — CLAUDE.md's "Dormancy over deletion"; names the
   resurrection trigger, added 2026-08-29 for D6/D9), or `FIXED` (PR + guard, then it
   leaves this file at the next prune).
3. **Rows expire.** A `FIXED` row is deleted once its guard exists — the guard is the
   durable record. An `OPEN` row with no trigger and no movement for two releases gets
   re-argued or becomes `ACCEPTED`.
4. **Claim linkage is mandatory** when a defect bounds a claim: name the graduation row it
   touches. A defect that silently invalidates an Earned row is the worst case this ledger
   exists to prevent.
5. **Deep investigations stay as their own doc** in this directory (the existing
   convention — see `console_seam_findings.md`). This file indexes them and carries
   standalone defects.

## Investigation clusters

| doc | scope | state |
|---|---|---|
| [console_seam_findings.md](console_seam_findings.md) | `maxim serve` console seams, sim_logger, PFC gate, aarch64 packaging | 12 fixed, 6 open |
| [display_print_corruption.md](display_print_corruption.md) | interactive display / print interleaving | see doc |
| [sim_embodiment_followups.md](sim_embodiment_followups.md) | sim embodiment wiring follow-ups | see doc |
| [repository_review_2026_08_19.md](repository_review_2026_08_19.md) | stable API contracts, lifecycle cleanup, architecture enforcement, offline tests | 6 open (D15–D20) |

## Standalone defects

Seeded 2026-08-11 from a four-lens architecture review; each was verified against the code
during that review and currently lives nowhere else.

### Substrate / persistence

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D1 | **`encoder_provenance` detects nothing at runtime.** It is recorded, persisted and reloaded, but nothing compares it against live encoder state — its only readers are the hivemind bundle/CLI export, and `record_encoder_provenance` *merges* divergence ("'mixed' is a finding, not an error"). A geometry change loads old-geometry centroids and cosine-scans them against new embeddings: **silently merged.** | **OPEN** — trigger: any encoder/threshold change. Prerequisite of the place-code default-ON gates. | `similarity/ec.py` (record/persist/load), `hivemind/bundle.py`, `hivemind/cli.py` |
| D2 | **There is no way to invalidate a stale EC substrate.** `cli_utils.py::MEMORY_PATHS` has keys for `hippo`, `nac`, `scn`, `atl`, `angular_gyrus` — **no `ec`**, not even under `all`. Clearing `nac` alone violates the NAc/EC pairing invariant. | **FIXED** (2026-08-23) — added the missing `"ec": ("agents/*/ec.json",)` key and a `MEMORY_PAIRS` table declaring `{nac, ec}` inseparable; `clear_memory` runs requested types through `expand_paired_types`, so clearing either half pulls in the other and prints why. `all` now includes `ec` (it previously wiped NAc and left EC on disk every time). Unpaired types are unaffected — the expansion is not a blanket widening. Guard: [tests/unit/test_memory_pair_invalidation.py](../../tests/unit/test_memory_pair_invalidation.py) (10 tests; 7 verified to fail against the pre-fix table). **Review fold (cross-confirmed):** the CLI table alone guarded only the CLEAR path, while the LOAD path — where the dangling biases actually manifest — was unguarded in two places. `check_nac_ec_pairing` now runs on the `load.agent()` restore and inside `build_bio_stack`, WARNING on a half-present pair (manual `rm`, interrupted write, partial backup). It is deliberately advisory and does NOT count as corruption for `on_corrupt="raise"`: every agent persisted before EC restore existed on the skeleton path has `nac.json` and no `ec.json`, so refusing would make every pre-existing agent unloadable — and re-saving, the actual fix, requires loading first. (Caught by the full suite during the fold: the first version did refuse, and broke three round-trip tests.) `build_bio_stack` also stopped discarding `NAc.load_safe`'s error return. | `cli_utils.py::MEMORY_PATHS` |
| D3 | **`ec_merge`'s cosine threshold is a hardcoded duplicate pinned by no test.** `cosine_threshold: float = 0.44` duplicates the EC default; unlike the frozen-modality set (which *is* pinned by `test_hivemind_frozen_modalities_match_ec_default`), nothing detects drift. The layer deliberately refuses internal imports, so it cannot read a threshold map. | **RESOLVED (pinned) — attested 2026-09-05, gate 2.** The trigger fired when D43 shipped `SENSOR_MODALITY_THRESHOLDS` (a NEW unpinned duplicate of 0.85); `tests/unit/test_gate2_geometry_and_thresholds.py::TestD3ThresholdPin` (1.1.3, per its own docstring) now pins the text default against `ECConfig().pattern_complete_threshold`, the sensor map against the sensor encoder's, and requires every frozen-centroid modality to carry a threshold — same lockstep-pin pattern as the frozen-modality set. The layer still deliberately refuses internal imports; the pin test IS the single source of truth. | `hivemind/merge.py::ec_merge_aligned` (defaults); `tests/unit/test_gate2_geometry_and_thresholds.py` |
| D4 | **A same-dimension encoding change defeats the merge dimension-guard, invisibly.** `_cosine` returns 0.0 on dim mismatch — but a place code keeps `dim=384` and the same `"audio"` tag, so old- and new-geometry nodes merge whenever partial cosine clears the threshold. Because `audio` is frozen, the corruption is undetectable: counts and contributors inflate, the centroid never moves. | **RESOLVED (guarded) — attested 2026-09-05, gate 2.** The geometry tag closed the same-dim hole: `encoding_geometry_tag` derives the space from the basis-set inputs (declared sensor names, normalization mode, model, A4 gain), so a place code that adds sensor names produces a DIFFERENT tag at the same dim and the merge equality gate refuses the fold (`strict_geometry=True` additionally refuses unstamped pairs; the 1.2 adapter runs strict per sharing_threat_model.md V3). Explicit and tested: `tests/unit/test_gate2_geometry_and_thresholds.py::TestD4GeometryGuard` + `TestGeometryTagIsMeaningful` (added sensor changes the tag; identical same-dim vectors in different geometries never fold; tag survives merge/save/load/ingest; process-stable). Residual on `MAXIM_PLACE_CODE_EXTEROCEPTION` default-ON: old audio nodes go stale-geometry and are CLEANED via `maxim substrate invalidate` (gate 1) — a migration event, no longer an invisible corruption. | `similarity/encoder.py::encoding_geometry_tag`; `hivemind/merge.py::ec_merge_aligned` (equality gate); `tests/unit/test_gate2_geometry_and_thresholds.py` |
| D5 | **`nac_merge` never folds cluster biases across agents.** Keys are `agent\x1fcluster\x1ftool` and cluster ids are per-agent UUIDs, so cross-agent cluster learning is unioned, never merged. | **RESOLVED 2026-09-05 — gate 6's pre-registered merged gauntlet PASSED** (`gate6-pass`, gated record `docs/experiments/data/gate6_merged_gauntlet/gate6_verdict_20260905.json`, provenance `bc9668af` clean): the donor's cluster biases re-key through the alignment map (`rekeyed=3, dropped=0, dangling=0`, both merge directions) and the merged want reads out at the parents' exact Exp 53 profile — the fold this row's ACCEPTED disposition declined now happens through `substrate_merge`. Scope, precise (protocol Dispositions section): the cluster axis is discharged mechanically on real evidence files + behaviorally at the D44 unit level; donor-vs-receiver readout is behaviorally indistinguishable at this pair's 3/3 identity fold, and the at-scale donor-want readout is the 1.2 benchmark's merged-taught arm. The earlier design constraint stands (session-relative spatial bearings remain unmergeable). Was: RE-OPENED 2026-08-27 (was: ACCEPTED for now). The 2026-08-26 Oasis case study made this load-bearing: gate 6 (a re-keyed `ec_merge` so a merged foreign want reads out) requires exactly the fold this row accepted not doing, and the Exp 52 seeds 42 + 43 must pass Gauntlet #2 merged. The earlier note stands as the design constraint: session-relative spatial bearings are unmergeable without a world anchor (the RSC plan's question, answered yes) — the re-keyed merge must say which bearings it refuses. Tracked as [roadmap gate 8(b)](../plans/roadmap_1_1_to_1_3.md#gates-before-12-oasis--hivemind). | `hivemind/merge.py:506-510`; [oasis_case_study_taught_orient.md](../plans/oasis_case_study_taught_orient.md) |

### Learning path

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D6 | **Hebbian episode binding is inert on the main percept path.** `memory_hub` stashes a **1-tuple** of substrate nodes, and `apply_hebbian_on_close` returns early on `len(nodes) < 2` — a silent no-op. So the binding graph never grows from llm-primary percepts, independent of any resolution question. | **DORMANT since 2026-08-29 (1.1.1, roadmap item 13).** Not wired and not deleted: multi-node percept activation is a hard dependency of the 1.3 fabric's orient-windowed Hebbian binding, so wiring it in 1.1.x would be 1.3 scope creep without 1.3's experiment. `Dormant since` markers are on both ends of the path (`episode.py::apply_hebbian_on_close`'s early return and `bio_integration.py::record_substrate_nodes`), and no docstring implies percept-driven binding learns in production. Resurrection trigger: the 1.3 fabric stage. | `integration/memory_hub.py:348`, `memory/episode.py:795` |
| D7 | **`min_delta = 0.05` is an undocumented second resolution limiter.** `SensorEncoder` short-circuits the EC scan and returns the cached node whenever no sensor moved ≥0.05 — a hard ~4.5% dead zone on a `[-1,1]` sensor **regardless of encoding**. Any population code past ~40 buckets is capped by this gate, not by geometry. | **ACCEPTED** (it is a real efficiency win) — but must be stated wherever resolution is claimed. | `similarity/encoder.py:519, 640-645` |
| D8 | **A read path mutates text centroids.** `bio_enrichment` calls `pattern_complete_or_separate(embedding, "text")` per enrichment query, with an in-code note that the centroid update is intentional reconsolidation (~1/(n+1) shift per query). Consequence not documented anywhere: **querying degrades text-cluster resolution over time.** | **MEASURED 2026-09-05 — verdict `separate-required`** (pre-registered, frozen rule: [protocol](../experiments/protocols/d8_read_mutation_preregistration.md), [gated record](../experiments/data/d8_read_mutation_20260905.json), provenance `ed6df4ce` clean). On the production 0.44 text threshold: probe identity churn 0/48 (identity robust) but one session-scale recall workload (192 completions) moved centroids to mean_cos 0.9757 / **min_cos 0.9521 < the 0.98 bound** — query traffic visibly resculpts the cluster space later recalls and merges read; recall also bought +192 member counts (evidence weight in `ec_merge`'s count-weighted mean — the M3 structural fact, handed to gate 4/V2). **RESOLVED 2026-09-05 — the separation shipped with its caller:** `EC.pattern_complete_readonly` (no centroid update, no count increment, no first-touch stamp — stamping is a write) and `bio_enrichment._query_hippocampus` switched to it; the mutating method is the ENCODE path by declared invariant (bio-memory brief). Caller sweep: bio_enrichment was the ONLY production recall caller (`atl.py`/`nac.py` reference the mutating method in docstrings only; `encoder.py` is encode). Guard: `tests/unit/test_ec_readonly_recall.py` incl. the mutating-path anti-vacuity arm and a spied caller test. | `bio_enrichment.py::_query_hippocampus` (the recall call, now `pattern_complete_readonly`); `similarity/ec.py::pattern_complete_readonly`; `scripts/exp_d8_read_mutation.py::decide_verdict` |
| D9 | **5 of 6 `TemporalEvent` categories have no producer.** Only `tool` emits; the drive emitter is both unwired *and* malformed (raises `TypeError` into a `except Exception: log.debug`). `record_event` even special-cases `deliberation` significance for a producer that does not exist. | **DORMANT since 2026-08-29 (1.1.1, roadmap item 13)** — producers stay deferred ([deferred/scn_event_producer_gap.md](../plans/deferred/scn_event_producer_gap.md)); revival requires answering whether per-event-type phase learning earns its keep. **The malformed half is FIXED:** the drive emitter passed `temporal_signature=`/`metadata=` (fields `TemporalEvent` does not declare) and omitted `event_id`/`event_signature`, and the resulting `TypeError` went into `except Exception: log.debug` — a dead path that looked alive at every log level anyone runs. The construction is correct now and a construction error is reported at WARNING; the emitter is still a no-op in production because nothing passes a distributor. | see plan |

### Claim scope

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D10 | **Exp 45's Earned status does not transfer to an EC-clustered orient policy.** The orient backbone builds state from `az_bin(...)`, a hand-written bin string passed as `current_cluster_id` — it never calls EC. That is hand-curated discretisation *upstream* of the substrate (the interim-contamination pattern), so any future EC-clustered orient policy needs its own experiment. | **OPEN (scope note)** — must be recorded on the Exp 45 graduation row. | `scripts/orient_backbone/live_3_learn.py:680`, `live_common.py:340` |
| D11 | **~432 bare `except Exception: pass` remain repo-wide.** 48 measurement-path sites were instrumented (#487); the rest are grandfathered and un-instrumented, so a silent failure outside that scope is still invisible. | **ACCEPTED** (scoped deliberately) — see [measurement_path_fail_loud.md](../plans/measurement_path_fail_loud.md). | `grep -rEA1 "except Exception:\s*(#.*)?$" src/maxim/` |
| D13 | **A failed planning turn dropped silently and the orchestrator idled forever between turns (the true mechanism behind the D12-era wedges).** A live 2026-08-18 trace ended with narrator HTTP 200 followed by a tool-parse failure, zero later backend calls, and an active-but-idle main loop after the legacy 120s await window closed. Because the orchestrator has no other percept source between turns, nothing could re-arm it. A second flaw existed in the proposed fix: a parsed proposal with `action=None,error=None` was stamped received but never handled, permanently defeating the backstop. | **FIXED** (2026-08-19, PR #523) — every non-executable outcome (fallback/parse failure, error, stale proposal, unregistered tool, parsed no-action, or exact worker job consumed without a proposal) routes through bounded recovery and normal teardown. Liveness now follows the exact job owned by that `LLMWorker`: `PENDING`, `RUNNING`, and `COMPLETED` (published but not consumed) keep the poll awake, eliminating both the 120s slow-model false abort and the call-end/result-queue race. It no longer consults the process-global call registry, which could observe an unrelated worker. Planning and worker-transport failures have separate three-retry budgets, so queue rejection cannot retry forever. Typed terminal status is `llm_wedged` for parse/provider exhaustion, `planning_failed` for responsive but invalid planning (including unregistered/no-action/stale), and `worker_unavailable` for worker/queue exhaustion. Bad-tool retries add the rejected name to the request's corrective prompt instead of replaying the same prompt. Scope remains opt-in (`run_agentic_loop(planning_liveness=True)`) and only the sim orchestrator enables it; operator opt-out is `MAXIM_SIM_PLANNING_LIVENESS=0` (S6 apparatus control). Guards: [tests/unit/test_planning_liveness.py](../../tests/unit/test_planning_liveness.py) (66 tests: exact job lifecycle through `RUNNING`/`COMPLETED`/`CONSUMED`, no-action handling, targeted bad-tool correction, separate bounded transport recovery, typed status, and wiring pins) plus [tests/unit/test_worker_pool.py](../../tests/unit/test_worker_pool.py) (queue rejection removes the orphan registry entry). | py-spy dump 2026-08-18 (MainThread active at `agent_loop.py:4584`, await-gate at `:1691`); `/tmp/heartbeat_e10_p2h.jsonl` backend trace (final call `status:200` then silence; W-logs at +108s; abort at +139s) |
| D14 | **The orchestrator spinner asserted work that was not happening.** The "Orchestrator planning next/first probe…" text could count for hours while py-spy showed no inference in flight, steering diagnosis toward the server rather than the dropped turn. | **FIXED** (2026-08-19, PR #523 — same PR as D13) — `spinner_truth_message` derives the display from observed call/byte state; the between-turn window is owned by `Spinner` and updates use a lock-guarded test-and-set, so stale monitoring cannot overwrite a new turn. Terminal text now uses the typed liveness status (`llm_wedged`, `planning_failed`, or `worker_unavailable`), and `Spinner.stop()` closes the planning window. Retry attempts remain visible in the EXEC stream. Guards: [tests/unit/test_planning_liveness.py](../../tests/unit/test_planning_liveness.py) (`TestSpinnerTruthMessage`, `TestSpinnerPlanningWindow`, and orchestrator wiring pins). | py-spy dump 2026-08-18 (active idle loop, zero calls in flight) vs. simultaneous pane capture ("planning next probe… (3286s)"); `simulation/orchestrator.py` spinner-start sites |
| D12 | **Orchestrator LLM calls blocked unboundedly — ROOT-CAUSED (2026-08-18, `sample` capture of a live 2.4h wedge): `router._inference_lock` inheritance deadlock.** The llm_worker timeout path abandons an orphan thread that can still be inside the locked region; its executor-replacement fallback gives new calls fresh threads but nothing frees the lock, so the untimed `with self._inference_lock` parked every subsequent call forever (~75 lock-waiter threads, ZERO network activity, stall detector blind because registry entry happens inside the lock — 265 impotent nudges observed). NOT a network-timeout escape; the model server was healthy and idle throughout. Deceptive symptom: AUT ticks sensors normally while starving for percepts ("bio systems not engaging"). | **FIXED** — bounded lock acquire + loud failure (`MAXIM_INFERENCE_LOCK_TIMEOUT_S`, default 600s) + the #517 sim-level hard-abort (exit 4) as defense in depth. Guards: [tests/unit/test_inference_lock_timeout.py](../../tests/unit/test_inference_lock_timeout.py) (held lock → bounded loud failure; verified the old behavior never returned) + [tests/unit/test_stall_hard_abort.py](../../tests/unit/test_stall_hard_abort.py). Prune at next sweep per rule 3. | `sample` thread-stack capture 2026-08-18 (80 threads: lock-waiters + zero recv); `models/language/router.py::_complete_text`; `agents/llm_worker.py` orphan path |

### Remaining items surfaced by the D13/D14 review round (2026-08-19)

D22 is now closed by the shared run-integrity contract. D23 remains open until
non-TTY output and lifecycle ownership are fixed globally.

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D22 | **A cleanly unwound aborted sim exited 0, and campaign harnesses did not read its typed status.** D12 only reached exit 4 through `_force_exit`; D13 recorded `llm_wedged`, `planning_failed`, or `worker_unavailable` in `finish_context`, but those values did not become process failure. A campaign could therefore count an aborted row as data. | **FIXED (2026-08-19).** `simulation_exit_code()` centralizes run-integrity policy: generic error exits 1; forced or clean aborts exit 4; semantic experiment outcomes remain exit 0. Direct CLI paths use the mapping, subprocess campaign scripts already reject non-zero return codes, and benchmark/curriculum/Roy/research/Console consumers now reject typed aborts before accepting metrics, chaining state, writing papers, comparing arms, or publishing a successful lifecycle event. Python surfaces retain structured `finish_reason` values. Guards: `tests/unit/test_sim_types.py`, `test_benchmark_runner.py`, `test_research_agents.py`, `test_console_event_seam.py`, plus curriculum/Roy integration tests. | `simulation/sim_types.py::simulation_exit_code`; CLI result exits; typed-abort consumer regression tests |
| D23 | **Raw-terminal spinners can still outlive careless callers and write ANSI frames to captured stderr.** `Spinner._spin` has no `sys.stderr.isatty()` check, and not every bridge caller guarantees `finish()`. The D13 regression test now closes its bridge and `Spinner.stop()` closes the planning window, but those surgical fixes do not make all callers lifecycle-safe or captured output hermetic. | **OPEN — 1.1.x hygiene**, pairs with D20's hermetic-suite work. Add non-TTY suppression and audit bridge ownership/cleanup across tests. | fast-suite stderr tail 2026-08-19; `simulation/spinner.py::Spinner._spin` raw-ANSI branch |

### Stable API / release enforcement

D15–D20 verified 2026-08-19 during the release-readiness review — detailed evidence
and required contracts: [repository_review_2026_08_19.md](repository_review_2026_08_19.md).
D21 and D24 come from the same day's independent claims-verification rounds
(scorecard and live public-site audit, respectively).

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D15 | **Stable `maxim.run()` arguments were silently ineffective.** `goal` was accepted but never reached the loop; `robot` connected but was not attached to the agent/executor/tool registry; `home_dir` remains only partially honored because loop state is CWD-relative. | **1.1 GATE FIXED (2026-08-19, v1.0.9)** for `goal`/`robot`: canonical initial-input ingress, explicit headless conflict, selected-controller motion, and wake/sleep/ownership lifecycle have facade and controller guards. The interruption-scoped lifecycle (`goal` does not auto-stop; `goal=None` is idle, not terminal-interactive) is now explicit. **OPEN — 1.1.x** for complete `home_dir` ownership. | `api.py::run`; `hardware/registry.py`; `tools/reachy.py::MoveTool`; `test_api_core.py`; investigation doc §D15 |
| D16 | **Stable API cleanup began after fallible side effects.** Environment mutation and worker startup preceded `run()`'s cleanup boundary; full-agent construction also abandoned its superseded skeleton ConceptExtractor worker; related setup windows remain in `imagine()`/`campaign()`. | **1.1 GATE FIXED (2026-08-19, v1.0.9)** for `run()`: worker, bio-stack factory rollback, robot lifecycle, run-owned LLM environment overrides, and the exclusive-run lock unwind from partial setup; cleanup stages continue after an earlier failure. **OPEN — 1.1.x** for `imagine()`/`campaign()` and broader process-global configuration isolation. | `api.py::run`; `integration/memory_hub.py::shutdown`; `runtime/agent_factory.py`; `test_api_core.py`; investigation doc §D16 |
| D17 | **`maxim.load.agent()` does not immediately restore every promised subsystem and may silently substitute fresh state for corrupt state.** ATL loads only at later session start; Hippocampus/SCN corruption can be swallowed. | **FIXED** (2026-08-23) — `_create_atl` now restores the ATL inside `create_agent` instead of deferring to `MemoryHub.on_session_start()`, which `load.agent()` returns without calling, so the documented "restores Hippocampus, NAc, ATL" promise is true at return. Corrupt Hippocampus/SCN `except Exception: pass` swallows and NAc's discarded `load_safe` return value now all report through `_note_corruption`, which logs a WARNING and records the file; `create_agent` raises one `MemoryCorruptionError` naming EVERY corrupt file when `AgentConfig.on_corrupt == "raise"`. `load.agent()` defaults to `on_corrupt="raise"` and takes `on_corrupt="fresh"` as the explicit opt-in to empty state (the corrupt file is left on disk). The factory default stays `"warn"` on purpose: `_create_memory_hub` restores SCN for EVERY caller, so a raising default would change behaviour far outside the stable load path (shared-builder activation rule). Guard: [tests/unit/test_load_agent_restore_contract.py](../../tests/unit/test_load_agent_restore_contract.py) (23 tests). **Review fold — three holes the first pass left, all closed here:** (a) `_create_memory_hub` handed `build_memory_hub` a bare `EntorhinalCortex()`, so `load.agent()` returned a RESTORED NAc bolted to a FRESH EC — the exact dangling-bias state D2 describes; EC is now restored from `agents/<id>/ec.json` on the same path. (b) The raise fired AFTER hub construction, leaking a live ConceptExtractor worker and the whole memory graph per attempt (a caller retrying on `MemoryCorruptionError` leaked per retry); the hub is shut down before aborting, as `create_full_agent` already did. (c) The OSError family — which `NAc.load_safe` deliberately does not catch — escaped through the outer `except Exception: return None` handlers leaving `corrupt` empty, so a `nac.json` that was a directory or chmod-000 yielded `nac=None, memory_hub=None` at WARNING level; all four constructors now report. `AgentConfig.on_corrupt` is `Literal["warn","raise"]` validated in `__post_init__` (a bare str made `"Raise"` a silent downgrade on a safety switch). `MemoryHub.on_session_start` no longer re-reads a just-restored ATL, which would have discarded post-load writes. **Verification note:** `PYTHONPATH` does NOT swap the code under test here — `tests/conftest.py` inserts `<repo>/src` at `sys.path[0]` — so a pre-fix check must edit files in place or build a separate tree. | `load.py::agent`; `runtime/agent_factory.py`; `integration/memory_hub.py::on_session_start`; investigation doc §D17 |
| D18 | **`register_tool()` is one-shot although the public extension contract implies continuing availability.** Injection clears the pending list while each API invocation builds a fresh registry. | **FIXED** (2026-08-23) — chose **persistent** registration: option 2 (rename as one-shot) was unavailable because `docs/user/extension_api.md` §2 declares `register_tool` stable and part of the 1.0 contract, which "will not break in 1.x". `_inject_registered_tools` no longer clears the list, so every later `run()`/`imagine()`/`campaign()` sees the tool; re-injection is idempotent because `ToolRegistry.register` keys on `tool.name` and last-wins. This also fixes a second silent symptom: `simulation/orchestrator.py` injects from a SECOND site, so whichever site ran first consumed the registrations and the other got none. Added `unregister_tool(name) -> bool` and `clear_registered_tools() -> int`, both exported via `_API_VERBS`/`__all__`, plus an autouse conftest scrub so a persistent registration cannot leak between tests. Guard: [tests/unit/test_register_tool_persistence.py](../../tests/unit/test_register_tool_persistence.py) (14 tests; the 6 core persistence guards verified to fail against the pre-fix injector). Review fold: `register_tool` now rejects a nameless object at the call site (permanent registration made an unnamed append unbounded and warn on every later `run()`), and `list_registered_tools()` exposes what will be injected. | `api.py::_inject_registered_tools`; `api.py::register_tool`; investigation doc §D18 |
| D19 | **The architecture audit is permanently red and therefore cannot detect regression.** The real audit reports 33 violations on current `main`; the unit test only checks that it returns a list, and CI does not enforce a reviewed baseline. | **FIXED 2026-08-24 (gate); 1.1.x debt burn-down stays open.** All 33 findings classified (10 typing-only under `TYPE_CHECKING`, 16 function-local (lazy) imports — none breaks an import cycle; each pulls a contract type or leaf helper that sits on the wrong side of the boundary, 7 module-level) into a reviewed accepted-debt baseline, `src/maxim/utils/architecture_baseline.json`, keyed by `file::imported_module::scope` + count + accepted symbol set (never line). The fast suite now fails on any finding outside the baseline, any stale entry (debt shrank — tighten the baseline), and any `UNREVIEWED` entry; `maxim --audit-architecture` reports against the same baseline. Verified to fail in place on `from maxim import tools`, on widening an accepted import's symbol list, on a deleted entry, and on an inflated count before landing. Row expires at the next prune; burn-down is roadmap 1.1.x item 11. | `tests/unit/test_architecture_audit.py::TestRealCodebase` (the gate) + `src/maxim/utils/audit.py::compare_to_baseline`; investigation doc §D19 |
| D20 | **The required fast suite was not offline/hermetic.** Installed optional ML dependencies could trigger model downloads; subprocess harnesses and teardown wrote under `~/.maxim`; CI ran only the narrower unit suite. | **1.1 GATE FIXED (2026-08-19, v1.0.9).** The test process and subprocesses use a unique temporary HOME/config/cache root; Maxim path caches reset per test; model hubs default offline; pretrained assets require explicit opt-in; DNS/socket/backoff host dependencies were removed; and CI now runs the documented wider command. Local exact-command evidence: 9,303 passed, 44 resource/platform skips, 41 slow deselections in 4m57s. | `tests/conftest.py`; `tests/unit/test_test_harness_hermeticity.py`; `requires_model_cache`; `.github/workflows/test.yml`; investigation doc §D20 |
| D21 | **`similarity/ec.py`'s `EntorhinalCortex` class docstring claimed "O(1) approximate nearest neighbor via LSH", but the substrate hot path (`pattern_complete_or_separate`) is a pure-Python linear scan over embedding centroids (O(Nd) per call) that never touches the LSH index.** The module docstring was already honest ("cosine-threshold clustering over embedding centroids; does NOT implement DG/CA3 attractor dynamics") — the class docstring contradicted it. | **FIXED (2026-08-19)** — the class docstring now distinguishes indexed signature queries from the exact substrate centroid scan. No runtime mechanism changed. Prune at the next ledger sweep. | `src/maxim/similarity/ec.py::EntorhinalCortex`; `::pattern_complete_or_separate` |
| D24 | **The canonical public website contradicts current release truth.** The 2026-08-19 crawl found Exp 48 still presented as `PASS/GRADUATE` with retired v1-apparatus numbers; the experiment index stops at Exp 48 and hard-codes 76; architecture is described as strictly enforced despite 33 audit findings; EC substrate lookup is described as constant-time; and fear review is claimed for every non-introspection tool although the wrapper is optional and disabled by stable `run()`. `docs.pymaxim.bio` also serves a duplicate marketing homepage instead of redirecting to the documentation entry point. | **CONTENT FIXED — 1.1 audit 2026-08-25 (maxim-web PR #7); browser pass still open.** The 2026-08-20 P0 fixes are verified live (Exp 48 retired numbers gone, derived count, D19 wording, conditional fear gate, O(N·d) EC, non-empty `goal`, `docs.pymaxim.bio` → 308 path-preserving redirect). The 1.1 audit against the exact 1.1.0 wheel added Exp 52 as the headline, superseded Exp 48, corrected the persona / `register_tool` / `--aut-mode` claims, and added the L8 / no-loudness caveats — per-route table in [pymaxim_bio_update_suggestions.md](../announcements/pymaxim_bio_update_suggestions.md) §1.1 audit. Remaining: post-deploy live re-crawl and the human visual/mobile/keyboard/accessibility pass. Original disposition: apply the cross-repo handoff, make the docs alias a path-preserving permanent redirect, rerun command/snippet/link/canonical checks, and complete the deferred visual/accessibility pass. Repo metadata now points directly at the canonical getting-started page so the wheel does not depend on the alias. | live 38-route sitemap crawl, 2026-08-19; [website handoff](../announcements/pymaxim_bio_update_suggestions.md); `AgentConfig.with_fear_gate`; `api.py::run`; D19/D21; Exp 48 apparatus-v2 disposition **2026-08-26: live re-crawl complete after 1.1.0 publish (fresh venv → 1.1.0, flag set identical); Exp 53/53b hardware card + release-notes/video pointers live (maxim-web #8, #9). Remaining: the human browser/mobile/accessibility pass → 1.1.x.** |

### Surfaced by the 1.0.9 pre-publish review (2026-08-20)

(D25/D26 — numbered after the website row D24 above, which the same release pass filed.)

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D25 | **Substrate validation tests OVERWROTE committed experiment records as a side effect of running, with no guard — so a degraded run silently replaced real evidence with artifacts.** `tests/substrate/test_p1_recognition.py`, `test_p2_reward_modulation.py`, and `test_concept_decomposition_validation.py` wrote their sweep output straight into `docs/experiments/results/*.json`, which are S4 committed raw records. Observed 2026-08-20: after the D20 hermeticity work forced `HF_HUB_OFFLINE=1`, the encoder could not load `paraphrase-mpnet-base-v2`, silently degraded, and one ordinary test run rewrote `p1_recognition_sweep.json` from `seeds_passing: 7 / mean_collapse_rate: 0.9168 / means_pass: true` to `0 / 0.0 / false`. The change was caught only because it appeared in `git status` before a commit; nothing in the harness objected. | **FIXED** (2026-08-20, PR TBD) — all three sweeps now publish through `tests/substrate/conftest.py::publish_sweep_results`, which writes to the test's `tmp_path` and prints the location. Updating the committed record requires the explicit `--write-experiment-results` flag AND passes the D25 apparatus check first, so replacing evidence is a deliberate, reviewable act instead of a side effect. No direct write to `docs/experiments/results/` remains in `tests/`. Guards: [tests/substrate/conftest.py::publish_sweep_results](../../tests/substrate/conftest.py) + [tests/substrate/test_evidence_write_policy.py](../../tests/substrate/test_evidence_write_policy.py). | the reverted `git diff docs/experiments/results/p1_recognition_sweep.json` from the 2026-08-20 offline run |
| D26 | **A missing encoder degraded into a fake scientific result instead of an error.** With the weights absent, `LinguisticEncoder` logged one WARNING and continued on `_fallback_embed` — whose own docstring says the vectors are "not semantically meaningful — paraphrase collapse will NOT work" — so `test_p1_recognition` reported `AssertionError: Substrate only beats random by -0.9%, need >30pp`, the exact shape of a genuine regression of the central claim. The three affected classes gated on sentence-transformers being *installed*, which the D20 work established is not evidence the weights are present. | **FIXED** (2026-08-20, PR TBD) — added `similarity/encoder.py::require_semantic_encoder(context=...)`, which attempts the real load and raises `ModelLoadError` naming the run, the cause, and both fixes. The three sweep classes call it in an autouse fixture BEFORE measuring, so a missing model is now a pytest ERROR on the apparatus rather than a FAILURE against the science (verified: the -0.9% assertion is replaced by the typed error). (`LinguisticEncoder.using_fallback` already existed for reporting realized state; the gap was that no measurement path consulted it.) The fallback itself is unchanged — it is correct for plumbing and wrong only for measurement, which is the distinction the assertion draws (apparatus standard S3). Guards: [tests/substrate/test_evidence_write_policy.py](../../tests/substrate/test_evidence_write_policy.py). | `similarity/encoder.py::require_semantic_encoder`; the pre-fix WARNING at `encoder.py::_get_encoder` followed by a science-shaped assertion |
| D27 | **Seven `scripts/` harnesses still overwrite committed S4 evidence unconditionally, and none asserts its apparatus.** D25/D26 were closed for `tests/substrate/`, but `fine_sweep_phase_2.py:221`, `measure_p1_at_threshold.py`, `p4_mug_test_sweep.py:288`, `p4_clip_calibration_sweep.py:345`, `p4_cross_modal_sweep.py:582` (which also rewrites a committed `.md`), `p4_vram_audit.py:383+`, and `p4_option2_measurement.py:736` write `docs/experiments/results/*.json` as a side effect with no flag, and ~18 files under `scripts/` construct an encoder without calling `require_semantic_encoder`. The defect class is identical — a degraded run publishes over real evidence — only the surface differs. Surfaced by the 1.0.9 two-lens review (cross-confirmed by both lenses). | **RESOLVED 2026-09-05 (1.2 gate 8(a)).** All seven harnesses now route every committed-tree write (the results JSONs AND their paired committed `.md` reports, which the original row under-counted) through `scripts/_provenance.py::evidence_out_paths`: without `--write-experiment-results` the write REDIRECTS to a temp dir (names preserved, paired artifacts together, loud notice); with it, a dirty `src/`+`scripts/` tree refuses (`DirtyTreeError`, exit 3) unless `--allow-dirty` — the D25 semantics on the scripts surface. `require_semantic_encoder` wired into the three LinguisticEncoder measurement entry points (`fine_sweep_phase_2`, `measure_p1_at_threshold`, `diagnose_roy_paraphrase_collapse`). **Triage of the "~18 encoder builders": 12 were `SensorEncoder` sites, whose hash-basis embedding is the DESIGN, not a degraded fallback — requiring the semantic model there would demand an apparatus the measurement doesn't use; the count conflated the two encoder classes.** Guards: `tests/substrate/test_evidence_write_policy.py::TestScriptsEvidenceWritePolicy` (results-tree scan over `scripts/` with a reasons-required allowlist + a LinguisticEncoder accounting scan that catches NEW unrequired builders; known stated limit: the scan keys on the results tree, so a new script writing ONLY a committed `docs/experiments/*.md` is reviewer-owned — a reliable .md scan would false-positive on docstring report references) + `TestEvidenceOutPathsHelper` (redirect/pass-through/refusal/allowance contract). Refusals follow the harness exit-3 policy via `evidence_out_paths_or_exit` (fail-FAST: every harness resolves its evidence paths before measuring, so a dirty-tree opt-in refuses before hours of sweep, not after). Was: OPEN — RE-FILED 2026-08-27 as 1.2 gate 8(a). | `grep -rln "experiments/results" scripts/*.py` → 7 files; [tests/substrate/test_evidence_write_policy.py](../../tests/substrate/test_evidence_write_policy.py) scans `tests/substrate/` only |
### Surfaced by the D17/D18/D2 two-lens review round (2026-08-23)

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D28 | **`create.agent()` silently restores SCN from disk despite documenting "always start fresh."** `AgentFactory._create_memory_hub` binds `scn.json` and calls `scn.load(...)` whenever the file exists — **regardless of `auto_load`** — while `create_agent`'s own docstring says `auto_load=False` means "always start fresh (`maxim.create.agent()`)". So a "fresh" agent inherits the previous session's temporal state, and only SCN, making the fresh/loaded distinction incoherent rather than merely incomplete. Pre-existing; surfaced by the D17 corruption-collection work, which made the inconsistency load-bearing (a corrupt `scn.json` is now the one file a `create.agent()` call will actually read and report). | **OPEN — 1.1.x, and a [1.2 gate 8(c)](../plans/roadmap_1_1_to_1_3.md#gates-before-12-oasis--hivemind) prerequisite (2026-08-27):** must land BEFORE the Oasis ingestion contract is drafted — that contract defines what a bundle merges *into*, which is undefinable while "fresh" is not fresh. Gate the SCN restore on `auto_load` like the other three subsystems, or change the documented contract to name SCN as an exception. Not a 1.1.x release blocker: the behaviour is long-standing and the D17 guard now makes it visible rather than silent. | `runtime/agent_factory.py::_create_memory_hub` (`scn.load` under `if scn_file.exists()`, no `auto_load` check) vs `create_agent`'s `auto_load` docstring; `tests/unit/test_load_agent_restore_contract.py::test_create_path_is_unaffected_by_corrupt_files` |
| D29 | **A committed sweep record cited as a regression guard is half synthetic.** The first `sweep_start` in `docs/experiments/data/45_doa_sweep_baseline.jsonl` (and the copy inside `45_doa_sweep_post_headfix.jsonl`) carries `"dry_run": true`; re-running `doa_sweep.py --dry-run` reproduces its numbers exactly (+0.569 / +0.571, R² 0.9785 / 0.9800). `docs/plans/behavioral_graduation_candidates.md` cites the file as a regression guard for the orient row without noting that half of it is simulated, and the point records carried no marker at all. | **OPEN — 1.1.x.** `dry_run` now rides on every record going forward ([L9](../limits/README.md)), so new files are self-describing; what remains is to annotate or re-cut the committed 45-series files and correct the graduation-row citation. Analyses on this branch already exclude `dry_run` records. | `45_doa_sweep_baseline.jsonl` `sweep_start` #1; `scripts/orient_backbone/doa_sweep.py::emit`; graduation row citation |
| D30 | **Head roll drifts under repeated body-only turns and nothing re-pins it.** Over the 2026-08-24 H1 `_big` block (16 alternating ±51.6° body turns through the production `ReachyOrientMotorBackend`, which commands `body_yaw` only), the controller's F1 early-warning fired repeatedly on recenters: achieved `head_roll` diverged from the last commanded by −5.3° → −8.7° and held there (magnitudes from the operator console — the harness did not yet record head pose per turn, so this is unarchived evidence pending the next run, which records world head yaw/roll pre/post per turn). Session 2 pinned roll/pitch explicitly for the envelope measurement and saw ≤ ±4°; the production path never pins them. Consequence: the mic array tilts during long orient sessions — a candidate for the right-turn Δaz shortfall recorded in Part C. Bounds the [Exp 45 orient row](../plans/behavioral_graduation_candidates.md) (`_big` delivered-shift evidence). | **OPEN — 1.1.x.** Decide whether the backend should ship explicit roll/pitch = 0 with each body turn (one-line change in `execute`) or whether the controller should re-pin on divergence; either way add the drift to the block's per-turn record and re-run the block. Trigger: motor-binding Phase-3 gain calibration or any change to `goto_target`'s retained-axes handling. | `hardware/reachy/controller.py::_warn_commanded_vs_achieved` warnings in the 2026-08-24 block run; `hardware/reachy/motor_backend.py::execute` (body_yaw-only `MotionTarget`); [H1 Part C follow-up](../experiments/protocols/h1_healthy_hardware_doa_preregistration.md) **Re-measured 2026-08-26 (Exp 53/53b, head pose recorded per turn, 360 turns at δ 0.55 and 0.30 rad): roll 13–19°, pitch 3–11°, sustained across the session, with the F1 divergence warning on nearly every goto; pattern completion and the direction readout were unaffected, but this is the number the D30 fix should be measured against.** |
| D31 | **The production credit path has no fold guard: a mirror-image DoA reading became a credited azimuth transition.** `ReachyOrientMotorBackend._measure_azimuth_after` takes the FIRST feed window that begins after `t_end + settle` and returns it; on 2026-08-24 (H1 `_big` block, aborted run, record `i=1`) that window read +0.289 while an independent later median-of-5 read −0.289 — an L10 sign-flip, which then landed in `measured_drive_transitions` and therefore in the SIGN of the relief credit a graduated mechanism learns from. 1 of 18 turns in one session; the sweeps put fold rates far higher at larger `|psi|`. | **OPEN — 1.1.x.** Require sign agreement across two post-settle windows (or a window and the modeled sign) before a transition is credited; on disagreement return `None` (sparsity is acceptable, a fabricated sign is not — the backend's own contract). Land the guard test with the fix. Trigger: any change to `_measure_azimuth_after` / DoAFeed windowing, or the next live orient session. Bounds the [Exp 45 orient row](../plans/behavioral_graduation_candidates.md). | `hardware/reachy/motor_backend.py::_measure_azimuth_after`; `docs/experiments/data/h1_partc_big_block.jsonl` run `20260824T213320Z-76884` `i=1` (`d_az_backend` +0.1556 vs `d_az_script` −0.4222); [L10](../limits/README.md) |
| D32 | **`CONSTITUTION.md` is a root marker, not a source: the "CORE PRINCIPLES" prompt block is a hardcoded paraphrase in `agents/llm_context.py`, gated only on the file's existence — the document and the prompt can drift silently, and `pip`-installed users (no repo root above `site-packages`) get an empty preamble.** Surfaced by the 2026-08-26 cleanup review (both lenses). | **OPEN — 1.1.x.** Either load the block from the file (and ship it as package data) or state in `CONSTITUTION.md` that the runtime text lives in code; add a test that fails when the two diverge. | `src/maxim/agents/llm_context.py::_load_foundational_context`; `tests/unit/test_llm_context.py` |
| D33 | **`maxim-diagnostics` (`utils/reachy_diagnostics.py`) duplicates the daemon probe logic that `maxim doctor`'s `check_robot_reachable` now owns, and the archived plan's disposition ("becomes a thin shim over the doctor check") never ran — two probes drift (the script still lacks the SDK/daemon era check; the doctor still lacks ping / dashboard / :8443).** Surfaced by the 2026-08-26 cleanup review. | **OPEN — 1.1.x.** Convert the console script to the shim (keep the entry point — removing it is a public-CLI break) and port ping/:8443/dashboard into the doctor check. | `pyproject.toml` `[project.scripts]`; `docs/plans/archive/doctor_robot_reachable.md` §consolidation |
| D37 | **Every diff-scoped lint was a no-op on pull requests.** The `lint` job checked out at depth 1 and each step ran `git fetch origin main --depth=1`, leaving two disjoint shallow roots: `git merge-base origin/main HEAD` failed, and the shared graceful-skip path printed `INFO: no base ref … skipping` and returned 0. Verified in CI run `33259722155` (2026-08-29): the multi-agent marker lint and `lint_no_silent_swallows.py`'s check 2 had been vacuous on EVERY pull request since they shipped, so "no NEW bare swallows" and the P4 marker rule were enforced by nothing. Surfaced by the 1.1.1 Cluster B review round (Executor lens). | **FIXED 2026-08-29 (#570).** The `lint` job checks out with `fetch-depth: 0` and the `--depth=1` fetches are removed; `scripts/_lint_git.py::must_not_skip` turns a missing base ref into a hard ERROR whenever `GITHUB_EVENT_NAME == pull_request`, so the skip can never silently return again. All four diff-scoped lints share that path. | `.github/workflows/test.yml` (`lint` job checkout); `scripts/_lint_git.py::must_not_skip`; `tests/unit/test_lint_atomic_io_ratchet.py::test_must_not_skip_is_true_only_on_a_pull_request` |

### Surfaced by the Exp 53b R1 replication (2026-08-28)

Harness gaps found by [R1](../experiments/53_cross_context_readout.md) — filed against the harness, not the result; the Exp 53/53b EARNED row in [behavioral_graduation_candidates.md](../plans/behavioral_graduation_candidates.md) rests on R1's complete run, which the D34 fix selects by construction.

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D34 | **`exp53_cross_context_readout.py verdict` pooled every trial in the file across `run_id`s** — partial starts, re-runs and `--only` debugging subsets included; there was no run filter. The Exp 53b R1 file (2026-08-28) holds five `start` records, and the printed 1.00 / 0.00 / 0.50 aggregated 219 trials from three Phase 2 starts (harmless there — the complete run alone gives the same three numbers — and not in general). Found by R1 ([53_cross_context_readout.md](../experiments/53_cross_context_readout.md) §Harness gaps). | **FIXED 2026-08-29 (1.1.1 Cluster A, PR #569).** `verdict` groups records by `run_id`, selects the ONE complete run per (phase, condition) — complete = every loaded agent has `agent_done` and `run_end.status == complete` when present; a `--only` subset is `debug`, never a result — refuses (exit 2) when two complete runs exist for one block unless `--run-id` pins it, and records `runs_used` / `runs_excluded` in the gate record. Verified: identical gate numbers on the original 53 / 53b files and on R1 (exploratory placement n 15 → 9, the complete run alone). | `scripts/orient_backbone/exp53_cross_context_readout.py::select_run`; `tests/unit/test_exp53_verdict.py` |
| D35 | **The controller's H1 F1 early warning (achieved-vs-commanded pose divergence, `ReachyMiniController`) was console-only** — R1's one `body_yaw −57.1°` divergence and the per-move `head_roll`/`head_pitch` (D30) warnings exist nowhere in the archived JSONL; the file carries `head_pre`/`head_post` but not the controller's verdict. | **FIXED 2026-08-29 (1.1.1 Cluster A, PR #569).** `cmd_run` attaches a `logging.Handler` to the `maxim.hardware.reachy` logger for the run's duration; every WARNING+ becomes a `controller_warning` record (`logger`, `level`, `message`, `ts`) in the same file, joinable to trials by `ts`. | `scripts/orient_backbone/exp53_cross_context_readout.py::_WarningsToJsonl` |
| D36 | **A partial run left no marker saying whether it was interrupted or crashed** — a Phase 2 start ended by `Motion command rejected by controller` and one stopped by Ctrl-C look identical (last agent has no `agent_done`). | **FIXED 2026-08-29 (1.1.1 Cluster A, PR #569).** Every run closes with a `run_end` record: `status` complete (rc 0) / stopped (a stop rule returned non-zero, `rc`) / interrupted (KeyboardInterrupt) / error (`error: "Type: message"`); a run with no `run_end` at all died before the harness could write one. `verdict` reads it as authoritative. | `scripts/orient_backbone/exp53_cross_context_readout.py::cmd_run`; `tests/unit/test_exp53_verdict.py` |
| D38 | **The published v1.1.0 GitHub Release notes carried 7 repo-relative links, which 404 on the Releases page** (and v1.1.0rc1 carried 1). Named by the 2026-08-27 score card; the notes were published from a source that used `../` paths — the same bug class `fix(readme)` #529 fixed for the PyPI page one release earlier. | **FIXED 2026-08-30 (1.1.2 release prep, operator-approved).** `gh release edit` re-pointed both Releases at their corrected sources; **notes body only — the wheel + sdist (sha256-matched to PyPI) and the tags were not touched, and nothing was re-published to PyPI.** Both published bodies now carry ZERO repo-relative links. The `1.1.0` and `1.1.0rc1` entries were removed from `GRANDFATHERED_RELEASES` in the same act, as that list fails on "now PASSES" — audit now reports 14 grandfathered, down from 16. **Prerequisite found while doing it:** the audit globs `release_<stem>*.md`, so `release_1_1_0rc1_backfill.md` is checked under BOTH versions — fixing only v1.1.0's body would have left 1.1.0 failing on the backfill source. That source's 2 relative links were corrected first. | `scripts/audit_release_tags.py::GRANDFATHERED_RELEASES` (entries removed); `docs/announcements/release_1_1_0.md`, `release_1_1_0rc1_backfill.md` |
| D39 | **v1.0.9, v1.0.0 and v0.9.0 GitHub Releases have ZERO assets**, and 0.6.0–0.9.1 have no Release object at all, while PyPI serves all of them — a tag says which commit, a Release hands over the artifact. | **OPEN — accepted debt.** Grandfathered by explicit list with reasons in `scripts/audit_release_tags.py`; the 1.0.9 wheel/sdist were not kept after upload, so attaching them means re-uploading files that cannot be proven identical to the published ones. Every new release must comply from 1.1.1 on. | `scripts/audit_release_tags.py::GRANDFATHERED_RELEASES`; `release-audit` CI job output |
| D40 | **`api.campaign()` documented three parameters and referenced none of them** (`npc_model`, `interactive`, `prompt_handler`) — a public argument with no observable effect (score card 2026-08-27 N1, reproduced black-box). Root cause for two of them is that `party_mode` is parsed into the campaign definition and read by NOTHING: there are no party-mode NPC agents, so `npc_model` has nothing to configure. | **PARTIALLY FIXED 2026-08-29 (1.1.1 Cluster D).** `interactive` is THREADED (`sim_logger.set_interactive_mode`, restored after the run — the orchestrator reads it when building the campaign's prompt handler); `npc_model` and `prompt_handler` now RAISE `NotImplementedError` before any side effect rather than being silently dropped. **Still open:** (a) `party_mode` remains inert, so party-mode NPC agents — and with them a real `npc_model` — do not exist; (b) `prompt_handler` COULD be threaded: `simulation/campaign_runner.py` already takes one and `bootstrap.build_tool_registry(prompt_handler=…)` consumes it — only `start_simulation_mode` lacks the parameter, so this is a missing passthrough, not an infeasibility. Both are 1.1.x+ work, not shipped here. | `src/maxim/api.py::campaign`; `tests/unit/test_api_expansion.py::TestCampaignParametersAreThreadedOrRejected` |
| D41 | **`AgentInstance.shutdown()` produced unloadable state** (score card 2026-08-27 N2): it calls `memory_hub.on_session_end()`, an atomic test-and-CLEAR that returns `{}` when no session was started — and neither `create.agent()` nor `load.agent()` called `on_session_start()`. The public create→mutate→shutdown→load cycle therefore wrote only `hippocampus.json` + `nac.json` and dropped EC, SCN, ATL and the Angular Gyrus, so every later `load.agent()` logged "Half-present NAc/EC pair" on the API's own output — the orphaned-bias state D2/D17 were fixed to DETECT, produced by the API itself. | **FIXED 2026-08-29 (1.1.1 Cluster D).** Root cause fixed at the lifecycle, not the symptom: the instance OPENS the session it later closes (`AgentFactory.create_agent`, both the create and load paths), and `MemoryHub.on_session_start()` is idempotent so the runtime's own `start_bio_session` no-ops on an adopted persistent agent instead of re-running the restore (which calls `atl.load()`, clearing before restoring). **Second review round (2026-08-29):** the first fix's `on_session_end` warning fired on EVERY clean runtime shutdown, because the agent loop's `start/end_bio_session` pair closes the session the instance opened — normal nesting, not the defect. The guard now keys on `_session_ever_started` (was a session EVER opened on this hub) rather than on `_session_active`, so it detects the defect and stays quiet on the nesting. Residual, documented: mutations made after the loop closes and before `shutdown()` are not persisted by that call. | `src/maxim/runtime/agent_factory.py::AgentInstance.start_session` (called on the FINAL hub — `create_full_agent` swaps the skeleton out, so opening inside `create_agent` opened a hub that was then discarded); `src/maxim/integration/memory_hub.py::MemoryHub.on_session_start`; `tests/unit/test_api_core.py::test_create_mutate_shutdown_load_round_trips_ec_scn_atl` |
| D42 | **`build_bio_stack` constructed a PATHLESS `SCN()`, so the runtime path never persisted temporal state.** `AgentFactory._create_memory_hub` binds `scn.json` at construction, but the bio-stack hub that `create_full_agent` swaps in (every `api.run()`, CLI, orchestrator AUT/NPC and console-handle agent) did not — so `on_session_end` had nothing to save to and every runtime agent silently lost its SCN signatures between sessions. Invisible because a pathless SCN is indistinguishable from an empty one. Found 2026-08-29 while verifying the D41 fix. | **FIXED 2026-08-29 (1.1.1 Cluster D).** `SCN(persistence_path=str(p / "scn.json"))` bound at construction (closing the same concurrent-construction race the factory closed), restored only when `load_persisted` — the orchestrator NPC's write-but-don't-read contract is preserved (that agent's `scn.json` is therefore rewritten empty each run, as its `nac.json`/`ec.json` already were). An UNREADABLE `scn.json` leaves the SCN pathless for that session — binding the path made this file writable for the first time, so a corrupt one would have been overwritten with empty state at session end; going pathless preserves it without touching it, loudly, and that session simply does not persist temporal state. | `src/maxim/runtime/bio_stack.py::build_bio_stack`; `tests/unit/test_api_core.py::test_full_agent_construction_opens_the_session_on_the_hub_it_keeps`, `::test_write_but_dont_read_agents_do_not_restore_scn` |
| D43 | **A merged foreign want reads out as exactly `0.0`, and the merge reports success.** `hivemind/merge.py::ec_merge` computes its right→left node alignment internally and **returns only the merged node set — the id map is discarded**, while `nac_merge` folds `cluster_reward_bias` on identical string keys only. Bias keys are the triple `(agent_id, cluster_id, tool_signature)` (`nac.py::NAc.get_cluster_reward_bias`), so a merged foreign want misses on **two independent axes**: the foreign `cluster_id` never matches AND the foreign `agent_id` never matches. The agent-id half also kills the prompt-annotation path (`nac.py::get_agent_tool_biases`, which filters `aid != agent_id`). No crash, no warning; `len(cluster_reward_bias)` grows, so the CLI prints success. Surfaced 2026-08-30 by the 1.2 scoping dive, confirming and extending 1.2 gate 6. | **FIXED** (2026-09-02, #590 + #591). All five barriers closed. `ec_merge_aligned` returns `ECMergeResult(nodes, id_map)`; `rekey_nac_state` rewrites the donor's cluster ids through it and normalises `agent_id` at the **ingestion boundary** rather than changing the key shape — closing both live axes without migrating a single persisted file. Barrier 4 (the one no plan document named) is closed by `SENSOR_MODALITY_THRESHOLDS`: interoception and audio align at **0.85**, not the paraphrase-mpnet text default of 0.44. Barrier 5 by restoring `cluster_reward_source` (one-way `"mixed"` promotion) and `saved_at` (younger wins). The N→1 fold is `nac_merge_many`, equal-weight in one pass. **#591 is the row's real lesson: #590 fixed nothing that ran.** It shipped the three pieces and left their COMPOSITION to call sites, of which there were zero — every shipped consumer still called bare `nac_merge`, so through every real path a merged want still read out as `0.0`. Caught only because D44's arms were `xfail(strict=True)` and did NOT flip to XPASS. `substrate_merge` is that composition (align → re-key the donor only → fold; order is load-bearing) and reports `biases_rekeyed`/`biases_dropped` because the old success indicator moved the wrong way. `EC.ingest_substrate_nodes` is the missing other half — `register_substrate_node` hardcodes `count = 1`, which would have erased the `member_count` weighting on every federation round. Measured on the shipped path: **0.0 → 1.0**, 4 of 4 merged keys reachable (was 0 of 4). | `src/maxim/hivemind/merge.py::ec_merge`, `::nac_merge`; `src/maxim/decisions/nac.py::NAc.get_cluster_reward_bias`, `::get_agent_tool_biases` |
| D44 | **No test asserts a BEHAVIORAL consequence of a merge, and the two federation experiments that pass deliberately dodge the failure mode.** `tests/unit/test_hivemind_bundle.py::test_end_to_end_hivemind_round_trip` runs the full pipeline but asserts dict equality on hand-set, already-matching keys — never `recommend_action`. The committed federation records (`scripts/orient_substrate/5_operant_creche_federation.py`, `7_graded_creche_federation.py`) pass on merged state, but every infant uses `AGENT = "infant"` with one shared encoder and EC — the script's own comment says *"SAME id for every infant so the merged cluster keys align"*, and its docstring says independent agents "need `ec_merge` alignment first". So the only green evidence for sharing is from the configuration in which D43 cannot fire. | **FIXED** (2026-09-02, #589 + #591) — `tests/unit/test_d44_merge_behavioural_delta.py`, 10 arms. Two genuinely independent agents (distinct `agent_id`, their OWN `EntorhinalCortex` + `SensorEncoder`, so `uuid4()` on separation makes their cluster ids disjoint by construction) and the assertion is a `recommend_action` delta, not dict contents. Written RED first and shipped `xfail(strict=True)` — **which is what earned the row**: after D43 merged the arms did not flip to XPASS, exposing that the fix had no callers (see D43). A non-strict marker would have stayed quietly yellow and D43 would have shipped as a green fix nobody called. Carries its own anti-vacuity guards: a no-op merge must NOT satisfy the gate; ingesting the donor's EC WITHOUT its NAc must change nothing (else the gate measures cluster relocation rather than want transfer); merged nodes must keep their `member_count`; and the NAc-only path is pinned to contribute exactly zero, since that is still what `merge-nac` does. | `tests/unit/test_hivemind_bundle.py::test_end_to_end_hivemind_round_trip`; `scripts/orient_substrate/5_operant_creche_federation.py` |
| D45 | **`SpatialMemoryBridge` restores zero, always — the cross-session spatial prior is structurally dead.** `bridges/spatial_bridge.py::SpatialMemoryBridge.on_session_start` reads `memory.perception.observations["position"]`, and **nothing anywhere writes a `position` key** into percept metadata (the only `"position"` producers are internal to `salience/salience_network.py`). The bridge is constructed and connected (`integration/memory_hub.py::MemoryHub.connect(spatial=...)`, `embodied_runtime/agentic_runtime.py`), so it looks wired. Companion finding: `memory/spatial.py::SpatialContext` (room/zone/x/y/z/heading) has **zero callers**. Surfaced 2026-08-30 by the 1.2 scoping dive. | **OPEN — 1.2 (spatial descope).** Either write `position` into percept metadata at the producing seam, or mark the bridge `Dormant since` with a resurrection trigger. `SpatialContext` is the intended carrier for the landmark-anchored symbolic location the 1.2 scoping adopted — wire it there or say it is unused. Do NOT build allocentric mapping: the Reachy Mini cannot translate. | `src/maxim/bridges/spatial_bridge.py::SpatialMemoryBridge.on_session_start`; `src/maxim/memory/spatial.py::SpatialContext` |
| D46 | **A docstring names a percept-transport module that does not exist.** `simulation/sources.py` refers to a "1.1 file `agents/remote_percept_source.py`"; the file was never written, and `mesh/message.py::MeshMessageType.PERCEPT_PUSH` (plus `EXPERIENCE_OFFER`, `CAUSAL_LINK_SHARE`) are enum slots with **zero producers** — `mesh/bus.py::LocalMessageBus` is in-process only. Live percept sharing therefore does not exist at any layer, while the docstring implies it shipped. | **OPEN — doc truth, 1.1.x.** Correct the docstring to name `deferred/mesh_perception_transport.md` as the owner, or delete the reference. The capability itself is deliberately deferred; only the claim is wrong. | `src/maxim/simulation/sources.py`; `src/maxim/mesh/message.py::MeshMessageType` |
| D47 | **A wheel built from a worktree ships ZERO Console UI files, and `twine check` PASSES it.** `src/maxim/console/ui_dist/` is `.gitignore`d with **zero tracked files** — it is copied in at release time by `scripts/vendor_console_ui.py` and exists only in the checkout that was vendored into. v1.1.0 shipped exactly 6 files under that path; a 2026-08-30 worktree build shipped 0, and `maxim serve` degrades silently to the "no UI installed" page for every pip user. Invisible on a dev box, which sets `config.json::console.ui_dist` to an external path and serves a working Console regardless of the wheel's contents. | **FIXED 2026-08-30 (1.1.2 Cluster D).** `scripts/audit_release_build.py` asserts `maxim/console/ui_dist/index.html` is in the built wheel, wired into a NEW `release-build` CI job (nothing in CI built a wheel before this) that stubs a bundle and builds — so the pyproject `console/ui_dist/**/*` package-data glob is exercised on every PR and push. The D47 branch's negative control is `tests/unit/test_audit_release_build.py::test_missing_ui_dist_fails`, which builds a synthetic wheel with the defect: the guard is shown to FAIL, not merely to pass. `scripts/vendor_console_ui.py --check` already detected this and is a documented MANUAL step in the publication guide — it had no AUTOMATED caller, which is why a build that skipped it sailed through. | `scripts/audit_release_build.py::audit_wheel`; `.github/workflows/test.yml` job `release-build`; `tests/unit/test_audit_release_build.py`  **Producer half closed 2026-08-30 by maxim-pulse v0.1.0** (operator): the bundle now ships as a checksummed release asset (`console-dist.tar.gz`, 142 KB) and its `maxim-ui.json` carries `app_version` (0.1.0, was frozen at 0.0.1 since inception), `commit_date`, `describe` and `dirty` — so a wheel can now answer *which* Console it contains. **Consumer half closed here:** `vendor_console_ui.py::validate` was shape-only and vendored a months-stale bundle silently; it now REFUSES `dirty: true` (the named commit does not describe the contents) and REPORTS age always, refusing on staleness only under `--max-age-days` — staleness is a judgment this script cannot make. Verified end to end: the real v0.1.0 asset vendors, builds, and `audit_release_build.py` reports clean with 6 Console files in the wheel. |
| D48 | **A stale-branch build produced a 1.1.0-versioned wheel and `twine check` PASSED that too.** Nothing compared the BUILT artifact's version against `pyproject.toml`. `scripts/lint_version_sync.py` compares the repo's version strings to each other — a stale branch is internally consistent, just wrong — and `scripts/audit_release_tags.py` runs POST-publication against PyPI. So the window between "build" and "upload" had no version check at all, which is the only window in which the mistake is still cheap. | **FIXED 2026-08-30 (1.1.2 Cluster D).** `scripts/audit_release_build.py` asserts wheel-filename version == `.dist-info` METADATA version == `pyproject.toml` version, in the same `release-build` CI job. Both halves are checked because the filename alone is not enough (`::test_metadata_version_drift_fails`), and the version assertion is deliberately NOT waivable by `--allow-missing-ui-dist` (`::test_version_check_is_not_waivable_by_the_ui_flag`). | `scripts/audit_release_build.py::audit_wheel`; `tests/unit/test_audit_release_build.py::test_wrong_filename_version_fails` |
| D51 | **`LSHIndex` is a linear scan over a single bucket, and its four hash tables are byte-identical — so the whole structure costs 4x and buys nothing.** Three compounding defects, all measured 2026-08-30 and reproducible: (1) `add()`/`query()` compute `bucket = signature.semantic_hash[:bits_per_table]` — **identical for every table**; the loop variable `i` is bound but never used. (2) `_hashers` is built with distinct per-table seeds (42..45) at `lsh.py::__post_init__` and again on deserialize, and **is never read anywhere** (4 source mentions, all writes) — so the multi-table probability amplification that is the entire point of multi-table LSH does not exist. (3) `semantic_hash` stays `(0,)*8` unless the EC holds a `SemanticLSH` hasher; `enable_semantic` defaults False, `build_bio_stack` and the agent factory pass only `persistence_path` and expose no passthrough, and `enable_semantic=True` STILL leaves it None (that branch builds `NeuralSemanticLSH` for the separate `EmbeddingStore` behind `find_semantic()`). Only the import-failure fallback installs one. Measured: 0.3 / 3.0 / 12.6 ms at N = 100 / 1,000 / 4,000 with one bucket holding **100%** of the corpus at every size. Verified independently here: 500 items whose structural, temporal and context hashes ALL vary produce exactly **1 bucket**, because `LSHIndex` keys on `semantic_hash` alone. That also falsifies `SituationSignature`'s own docstring, which claims that when semantic hashes collide, "similarity queries rely only on structural, temporal, and context" — `LSHIndex` never consults those. Called live from `nac.py::distribute_reward`. Surfaced by the pymaxim.bio audit 2026-08-30. | **OPEN — needs a design decision, not a patch.** Deliberately NOT fixed on discovery (no-band-aid rule): using `_hashers[i]` is not a drop-in, because the hasher hashes TEXT while `semantic_hash` is precomputed upstream, so making the tables differ means deciding what each table should hash — and whether this index should incorporate the structural/temporal/context dimensions its signature docstring already promises. Adjacent question first: is this surface load-bearing at all? The substrate results rest on `pattern_complete_or_separate` (an exact scan by design), not on this. Dormancy may be the honest disposition. **SCOPE CORRECTED 2026-09-03 (1.1.4 kickoff survey):** the roadmap's "D51 is a prerequisite for A4" names the wrong structure — `pattern_complete_or_separate` never consults `LSHIndex`, so the A4-inflated cost lands on the exact `_substrate_nodes` scan, which has NO index of any kind; fixing D51 would not shave a microsecond off it. The A4 prerequisite is the scan-cost measurement + decision rule in `scripts/ec_scan_cost.py` ([docs/plans/world_seam_1_1_4.md](../plans/world_seam_1_1_4.md) decision D4). D51 proper stays open. `find_similar`'s live callers (corrected AGAIN by the PR 0 review round — the first correction's "one live caller" repeated the wrong-caller mistake it was fixing): `NAc._predict_impl` (gated on `use_ec_similarity`, default False) AND `tools/introspection.py::SimilaritySearchTool` → `find_similar_by_memory` (UNGATED, registered live by the sim orchestrator and the embodied runtime). Not `distribute_reward`, as this row's file column previously implied. | `src/maxim/similarity/lsh.py::LSHIndex.add` / `::query` (identical bucket per table; `_hashers` unread); `src/maxim/similarity/signature.py::SituationSignature` (docstring promises a degradation path `LSHIndex` does not implement) |
| D52 | **ARCHITECTURE.md and `ec.py` described `find_similar` as "LSH-based approximate nearest neighbour" — the intent, not the behaviour.** Introduced 2026-08-30 by the 1.1.2 Cluster C EC correction, which fixed the rows' bio-attribution and their LSH-vs-exact-scan split but asserted the LSH half without measuring it. A correction written specifically about claim honesty traded one wrong claim for a subtler one, in the same commit. | **FIXED 2026-08-30**, same day, before the PR merged. Both ARCHITECTURE.md rows and the `ec.py` module docstring now state that NEITHER EC surface is sublinear in production, cite D51, and carry the measured numbers. Kept as a ledger row rather than a silent amend because the failure mode — asserting a mechanism's intent as its behaviour while auditing others for exactly that — is the one worth being able to find again. | `ARCHITECTURE.md` (both EC rows); `src/maxim/similarity/ec.py` module docstring |
| D53 | **A clamped or unreached motion is credited to the substrate as a SUCCESS.** `tools/reachy.py::focus_on_sound` returns `ToolResult(success=True, …)` even when `clamped` is true or `reached` is false — deliberately, and its *payload* is scrupulously honest (`faced_sound`, `clamped_to_head_limit`, an explanatory `note` for the LLM). But `runtime/tool_dispatch.py::record_outcome` computes `learn_success = success and not embodiment_failed`, and a clamp populates neither `embodiment_failures` nor any other channel, so the substrate books `Valence.POSITIVE` + `cluster_reward = +1.0` (`credit_source="tool_success"`) for a motion the tool itself reports did not happen. Companion finding: `ReachyMiniController.last_clamped_axes` has exactly one consumer in `src/` (`tools/reachy.py`, LLM prose) and no path to the PainBus, the NAc, or any modality channel — a refused command is invisible to the learner. Surfaced 2026-08-31 by the microduck design pass. | **FIXED 2026-08-31.** **Correction to this row's own claim linkage.** The retracted verdict read, verbatim: *"Claim linkage (rule 4): no EARNED row is bounded — but not for the obvious reason"*, on the grounds that the graduated harnesses do not route credit through `record_outcome`. It is quoted here rather than silently replaced, per D52's precedent. **It was WRONG, on two counts, both found by the pre-fix ternary sweep** ([docs/lessons/ternary-collapse-sweep-2026-08-31.md](../lessons/ternary-collapse-sweep-2026-08-31.md)). (1) `recommend_action` has a **causal component** — `score += best_pos` where `best_pos = max(link.confidence)`, UNWEIGHTED (the negative term is weighted 0.5). A link's confidence is **0.50** on creation and **0.64+** once re-observed (`min(0.99, 0.5 + 0.1*(count**0.5))`), against a `min_confidence` gate of 0.3 — it clears the gate from the FIRST observation. So the collapsed valence DID reach action selection — not via `reward_bias` (`_record_outcome_impl` writes neither bias dict) but via the causal term — and `MAXIM_OPERANT_ONLY_CREDIT` cannot suppress it, because the drive branch is evaluated FIRST and `operant_only` gates only the `cluster_reward` else-arm. Exp 42 (`--aut-mode substrate-primary`, `drive_relief_only=False`, no `OPERANT_ONLY`) ran ALL of these sites live in its credit path; Exp 52 Phase B's "the mother is the sole teacher" held for the **cluster surface only**. (2) The same collapse was hard-coded one layer BELOW `record_outcome`, in `ReachyOrientMotorBackend.execute` (`success = reached is not False`), which Exp 53/53b/54 DO attach — their claims survive only because those harnesses measure `achieved_delta_rad` independently. **The preferred fix named above was also not viable as written:** `_check_movement_failure` is reachable only from the camera path, is a one-sided PAIN detector (bad / less-bad, never neutral), and writes the `"movement"` namespace, not `"tool"`. Reviving it is a rewrite. The comparator already existed, twice, in the right namespace and already tri-valued — `FocusOnSoundTool` and `ReachyOrientMotorBackend` both compute `reached: bool \| None` and both discarded it at the `ToolOutput` boundary, because `Executor.execute` reads `side_effects` and never `metadata`. **Shipped:** a new `side_effects["outcome_ineffective"]` key (registry 1.1.3), modelled on `drive_credit_withheld` — suppress the floor WITHOUT asserting harm, so `embodiment_failures` stays rejected as this row requires. `record_outcome` now computes a three-tier `learn_valence`; a NEUTRAL outcome books `Valence.NEUTRAL` (landing in NEITHER `get_positive_outcomes` nor `get_negative_outcomes`, so it contributes zero to `recommend_action`) and mints no goal or cluster credit. Measured exactly-zero drive progress no longer falls to the `+1` floor either — the modeled path now agrees with `tool_bridge`'s measured path and with `cradle_mother::reactive_mother_tick`, which an EARNED result already depends on. Producers key on the OUTCOME, never on clamp-occurrence: a clamped turn that still moved toward the sound stays POSITIVE. Booking 0.0 vs skipping the write was settled by arithmetic, not taste — both sinks accumulate `current + alpha*reward`, so 0.0 is an exact no-op. **Re-run owed:** Exp 42 and Exp 52 re-run triggers fire on this change. | `src/maxim/tools/reachy.py::FocusOnSoundTool` (the `success=True` return under `clamped or reached is False`); `src/maxim/runtime/tool_dispatch.py::record_outcome` (`learn_success = success and not embodiment_failed`); `src/maxim/hardware/reachy/controller.py::ReachyMiniController.last_clamped_axes`; `docs/lessons/ternary-collapse-sweep-2026-08-31.md` (the full site table); guards: `tests/unit/test_tool_dispatch.py::TestNeutralOutcomeTier`, `tests/unit/test_embodiment_failures.py::TestNeutralToolCompletion`, `tests/unit/test_focus_on_sound.py::TestLearningTier`, and the INVERTED `::test_zero_progress_books_nothing` |
| D54 | **Every `PainDetector`-origin pain distributed exactly ZERO reward — no `agent_id` ever reached the bus.** `_emit_pain` hand-built its `Reaction` and pushed it straight onto `reaction_bus`, bypassing `PainBus.publish` and therefore `reactions/compat.py::pain_signal_to_reaction` — the only place `context["agent_id"]` becomes `ReactionContext.agent_id`. `compat.py`'s docstring names this exact failure mode verbatim. **The first fix claimed this was the ONE such site in `src/maxim/`; the pre-merge round falsified that** — `runtime/pain_interceptor.py` and BOTH `proprioception/perceived_pain.py` emit paths had the identical defect (no `agent_id` anywhere in either file), and four more world-origin sites remain (D57). `bio_stack::_distribute_reward_from_reaction` early-returns on `agent_id is None`, so velocity, thrashing, movement-failure, tool-failure and tool-timeout pain all credited nothing, no matter how much fired. Nothing raised, nothing logged. Root cause was deeper than the bypass: `PainDetector` had NO `agent_id` at all (zero references in the file), so routing through `compat.py` alone would still have delivered `None`. Surfaced 2026-08-31 by the D53 ternary sweep, as collateral. | **FIXED 2026-08-31.** `PainDetector.__init__` gains `agent_id`, stamped into every emitted signal at `_emit_pain` — the single dispatch point, so a future pain source cannot forget it at one of the three `PainSignal(...)` construction sites. `_emit_pain` now calls `PainBus.publish(signal)`; direct subscribers consequently get the FULL `signal.context` instead of the lossy `_bridge_reaction_to_pain_subs` reconstruction, and the bus's `(entity, failure_mode)` refractory gate now applies to this path. Wired at both construction sites (`default_network`, matching the `agent_id="default_network"` it already used for fear review; and the sim orchestrator's `_aut_agent_id`). **The three sites the round found are fixed in the same commit**: `PainInterceptorExecutor` and `PerceivedPainAssessor` take an `agent_id` and set it on their hand-built `ReactionContext` (they carry a `paths` binding `compat.py` does not map, so routing through the converter would drop it), wired from the orchestrator's `_aut_agent_id`. | `src/maxim/proprioception/pain.py::PainDetector._emit_pain`; `src/maxim/reactions/compat.py::pain_signal_to_reaction`; `src/maxim/runtime/bio_stack.py::_distribute_reward_from_reaction`. Regression guard: `tests/unit/test_pain_bus.py::TestPainDetectorAgentAttribution` — 3 of 4 verified to fail against the pre-fix detector (the 4th is the unset-`agent_id` negative control, correctly passing both ways) |
| D57 | **Four WORLD-origin pain emitters still build a `Reaction` with no `agent_id`, so their pain distributes zero reward** — `runtime/sim_adapter.py`, `simulation/sandbox.py`, `simulation/conversational_source.py`, `embodiment/backends/cerebellum_modulator.py`. Same silent mechanism as D54 (`bio_stack::_distribute_reward_from_reaction` early-returns on `agent_id is None`), found by the repo-wide guard added with D54's fix rather than by inspection. | **FIXED 2026-09-03.** Resolved by NAMING the world rather than attributing its pain to whichever agent was nearby — the latter manufactures attribution, and a reward that lands somewhere plausible and arbitrary is worse than one that lands nowhere, because it stops being visible as missing. `reactions/types.py::WORLD_AGENT_ID` is now stamped by all four emitters. **World-origin pain still distributes NOTHING** — `_distribute_reward_from_reaction`'s standing rule against a phantom bucket is unchanged, and no agent's eligibility traces should move because the sandbox fired. What changed is that the skip is explicit and traced (`pain_chain.reward_subscriber.world_origin`) instead of being indistinguishable from a MISSING `agent_id`, which is a real bug the constant must not hide. The shrink-only allowlist in `tests/unit/test_pain_bus.py` is now **empty**: every pain Reaction in the tree names its subject. | `tests/unit/test_pain_bus.py::test_no_pain_reaction_is_built_without_agent_id` (the allowlist IS the ledger of open sites; it may shrink and must never grow); `src/maxim/runtime/bio_stack.py::_distribute_reward_from_reaction`; D54 |
| D58 | **The cerebellum forward model has not trained in production since 2026-04-07 — ~5 months.** `embodiment/tool_bridge.py::ModulatorAffordanceTool.execute` calls `Cerebellum.observe_from_action(entity_path=…, actual_sensors=…)`; the signature is `entity=` / `actual=`. Every call raises `TypeError` into the enclosing `except Exception: log_swallowed_exception()`. `prompts/acting_coach.py::_compose_cerebellum_predictions` therefore rendered an EMPTY predictions section into every prompt for that whole period. A second, independent swallow hid the other half: `Cerebellum.observe`'s telemetry read `key.entity` / `key.params_hash` against a comment describing a five-field `ModelKey` that never existed (real fields `entity_path` / `param_bucket`), so `sim_cerebellum_train` had never fired. **Why no test caught it:** `tests/integration/test_affordance_transfer.py` calls `observe_from_action` correctly and directly, and passes — a unit test of an API proves the API works and says nothing about whether the one production caller agrees with it. Surfaced 2026-09-01 by the Minecraft-embodiment design dive. | **FIXED 2026-09-01.** Both keyword names corrected, both telemetry attributes corrected, and the bare `except Exception: pass` around the telemetry narrowed to `log_swallowed_exception()` (it is what let the AttributeError live). | `src/maxim/embodiment/tool_bridge.py::ModulatorAffordanceTool.execute`; `src/maxim/embodiment/cerebellum.py::Cerebellum.observe`. Regression guard: `tests/unit/test_cerebellum_wiring.py::TestCerebellumTrainsFromProductionPath` — drives the PRODUCTION path (5 of 7 verified to fail pre-fix) plus an AST check that every call-site keyword exists in the signature |
| D59 | **An affordance `requires` key that names anything but `"integrity"` or the modulator's OWN sub-sensors silently never gates.** `spec.py::SpecModulator.check_affordance_requires` tests `req_name in self.vital_metrics` (the modulator's sub-sensors) and otherwise falls through to `return True, ""`. Entity-level sensors are the common case and are unreachable: `SpecModulator` holds only `_entity_name`, a string. `items/cradle_food.yaml`'s `requires: {portions: 1}` is the ONLY non-`integrity` requires key in the 92-file bundled library, and it is a live no-op — eating is unlimited. Surfaced 2026-09-01 by the Minecraft dive, where "mine requires a pickaxe" / "eat requires food in inventory" is exactly the unexpressible shape. | **FIXED 2026-09-03.** `SpecModulator` gained `_entity_ref`, mirroring `SpecSensor`, which already had one — so `requires` resolves modulator sub-sensors first (preserving the shadowing behaviour for names that DID resolve) then the owning entity's sensors. Verified: `eat` allowed at `portions=5`, **refused at `portions=0`** with a named reason. **This did NOT re-stale Exp 52**, and the reason is worth recording: `portions` starts at 5 and *nothing decrements it* — `eat` has no `target_effect` despite the YAML description claiming "Portions decrease on consumption" — so the requirement is satisfied at every tick and the cradle apparatus is behaviourally identical. The earlier disposition assumed the gate would bite and recommended deferring; the data said otherwise. The parse warning is narrowed to names resolving to nothing at all (a warning that a working precondition never gates is worse than the original silence). The guard was **inverted, not deleted** (D53 precedent). **Filed separately: the description is false** — implementing the decrement WOULD change the apparatus, so it is a deliberate decision, not a drive-by. | `src/maxim/embodiment/spec.py::SpecModulator.check_affordance_requires`; `src/maxim/_data/components/items/cradle_food.yaml`. Regression guard: `tests/unit/test_self_effect.py::TestRequiresSilentNoOp` (warns on unresolvable; silent for `integrity` and for a modulator's own sub-sensor) |
| D60 | **The significance RPE heuristic is inert: it reads an attribute that does not exist.** `agents/exec_agent.py::_evaluate_staging` does `getattr(nac, "last_predicted_valence", 0.5)` — the only reference to that name in the repo — so `predicted` is always 0.5 and `rpe_raw` is a constant for every outcome. The `SignificanceWeightLearner` will correctly learn to zero the heuristic out, so nothing is mis-scored; what is wrong is that "surprise makes a memory worth keeping" has never run. Real RPE exists (`CausalLink.last_rpe` → `Executor.get_last_rpe`, already used by `runtime/bio_integration.py` for hippocampus salience). Surfaced 2026-09-01. | **FIXED 2026-09-01.** `NAc.last_rpe` now exposes the value `CausalLink.update_prediction_rw` already computed (`abs(R - V)`), stashed via `NAc._note_rpe` at every RW update — mirroring what `bridges/tool_pain_bridge.py` already did internally. `_evaluate_staging` reads it instead of the attribute that never existed. Verified non-constant: a valence reversal reads ~0.57 against a confirmed expectation's much lower value. **This DOES change what gets staged to long-term memory** — the heuristic now carries information the weight-learner can use, where before it correctly learned to zero a constant out. Do not pull onto a machine mid-experiment. | `src/maxim/agents/exec_agent.py::ExecAgent._evaluate_staging`; `src/maxim/decisions/significance.py::SignificanceWeightLearner` |
| D61 | **A `slow`-lane test fails on `main`.** `tests/integration/test_affordance_transfer.py::TestIT1FireTransfer::test_fire_concepts_share_ec_node` fails identically with and without the D58 fix, so it is pre-existing and unrelated. The file is `pytest.mark.slow`, so the fast suite deselects it — it runs only in the nightly `-m slow` lane that 1.1.2 added precisely because 41 marked tests had been running nowhere. The lane is now finding what it was built to find. | **FIXED 2026-09-02 — and it was not a code failure.** `tests/conftest.py` deliberately sets `HF_HUB_OFFLINE=1` / `TRANSFORMERS_OFFLINE=1` and an isolated `HF_HOME` (correct: no network in tests, no polluting the developer's cache). So under pytest `LinguisticEncoder` can NEVER load `paraphrase-mpnet-base-v2` and always uses the bag-of-words fallback, whose own docstring says *"Not semantically meaningful — paraphrase collapse will NOT work with this."* **The test was structurally impossible, not broken.** Its siblings were the mirror image: `TestIT2NoFalseTransfer`'s two negative controls assert water does NOT share a node with fire, which a hash encoder satisfies trivially — they were passing **vacuously**, and both additionally wrapped their assertion in `if water_concepts:`, so a missing concept (the likelier outcome under a degraded encoder) meant passing having checked nothing. Fixed: `_require_semantic_encoder` skips the four semantic-dependent tests naming the cause, and the two `if` guards became assertions. **A skipped test is not a passing test** — the real remedy is pre-seeding the model into the isolated `HF_HOME` so the nightly `-m slow` lane can actually run them. | `tests/integration/test_affordance_transfer.py::TestIT1FireTransfer::test_fire_concepts_share_ec_node` |
| D62 | **Exp 45's merge/fleet arm is a VACUOUS GUARD on an EARNED row — its gauntlet passes when `nac_merge` is replaced by `return left`.** Re-run on the real recorded NACs still on disk (the s1b + m1 files the row cites): real merge → correctness 1.00 / magnitude 1.00 / **PASS**; `return left` → 1.00 / 1.00 / **PASS**; `return right` → 1.00 / 0.50 / **PASS**; only `return {}` FAILS. The real merge's output is **argmax-identical to `return left` in all four bins** — it changes bias magnitudes and changes no decision. Three vacuities compound: both parents already probe correctness 1.00, so the `c_merged >= max(c_left, c_right)` clause is evaluated at ceiling and carries zero information; the gate reads correctness ONLY and never consults `magnitude_appropriateness` (which is why `return right`, argmax-wrong in both far bins, also passes); and the two parents share `agent_id` (`AGENT_ID = "reachy"`, a module constant) and a hardcoded 4-bin symbolic cluster space (`OFF_CENTER_BINS`) by construction — no `SensorEncoder`, no EC, no uuid — so the key space is a shared enum and D43 structurally cannot fire. "Independently trained" is true; "independent agents" is not. Surfaced 2026-09-01 by the D43 pre-implementation sweep. | **FIXED** (2026-09-02). The row's literal sentence was true as written; its EVIDENTIAL STATUS was not. Three vacuities, and they needed **different** fixes — the useful finding is that only one was a threshold problem. **(1) The gate read `correctness` only.** `magnitude_appropriateness` was computed and PRINTED on every run and never consulted, which is why `return right` — direction-correct everywhere, argmax-wrong in both far bins — passed. `verdict()` now gates both axes against `max(parent)` **per axis**, so a merge cannot take the best correctness from one parent and the worst magnitude from the other. Re-measured: `return right` now **fails** (magnitude 0.50 < the best parent's 1.00). **(2) The INPUTS, which no threshold can fix.** Both recorded parents already probe correctness 1.00, so `merged >= max(parents)` sat at ceiling; a parent that is already correct is by itself a passing policy, and `return left` still passes however the gate is tuned. `--complementary-split` derives two parents from one policy by splitting its bins, so each half probes **0.50/0.50** and only a real fold reaches **1.00/1.00** — all three no-ops then fail and the `Re-run on: nac_merge semantics change` trigger can fire again. **(3) The shared `agent_id` + symbolic bin space** is out of scope here by design: this arm tests the FOLD; the alignment arm is `tests/unit/test_d44_merge_behavioural_delta.py`, which needed D43 and now exists. **`--assert-noop-fails` is the guard on the guard** — it re-runs the gauntlet with `nac_merge` replaced by `return left` / `return right` / `return {}` and exits **3** if any still passes. On the recorded parents it correctly reports VACUOUS (rc 3); with `--complementary-split` it reports discriminating power (rc 0). Both measured 2026-09-02. | `scripts/orient_backbone/orient_merge_arm.py::main` (the gate); `scripts/orient_backbone/live_3_learn.py::AGENT_ID` / `::OFF_CENTER_BINS` (the shared key space); `docs/plans/behavioral_graduation_candidates.md` Exp 45 row, arm 3; `docs/plans/d43_merge_correctness.md` §7 |
| D55 | **`FocusLearner.save` hand-rolled `open()+json.dump`**, violating both the `atomic_write_json` invariant (no fsync, no tmp cleanup, non-atomic) and the `_format_version` contract (it wrote a bare payload-layer `"version": 1` and no root envelope). Surfaced 2026-08-31 by the D53 ternary sweep, as collateral. | **FIXED 2026-08-31.** Writes via `atomic_write_json(str(path), with_format_version(state))`; the loader calls `check_format_version(state, "focus_learner", log=logger)`. The payload-layer `"version": 1` is left in place per the coexist-by-design rule. Verified: new files carry `_format_version "1.0"`, pre-existing files still load behind exactly one warning, no tmp files left behind. | `src/maxim/proprioception/focus_learner.py::FocusLearner.save` / `::load`. Regression guard: `tests/integration/test_persistence_compat.py::TestBioSystemPreV1Compat::test_focus_learner_roundtrip_and_pre_v1_file` — added in the same commit because the round found the original citation named a file that mentioned FocusLearner nowhere; citing a non-covering test is worse than citing none |
| D56 | **The ternary collapse is not confined to the credit path — nine further sites survive, and the recurring shape is a CORRECT neutral-aware consumer starved by a boolean producer.** Filed from the pre-fix sweep ([docs/lessons/ternary-collapse-sweep-2026-08-31.md](../lessons/ternary-collapse-sweep-2026-08-31.md)) with the credit path fixed and the rest mapped. The notable ones: (a) `causal_link.py::_record_observation_unlocked`'s 3-way valence EMA is **structurally dead** — link ids hash `outcome_signature`, which embeds success/failure, so a link is only ever re-observed with its own valence and `blended_v` never crosses ±0.3; the one mechanism that could PRODUCE neutral from experience cannot fire. (b) `exec_agent.py::_evaluate_staging` emits `1.0 if success else 0.0` into `CycleContext.outcome_valence`, whose consumer `significance.py::_eval_valence_extremity` is `abs(v-0.5)*2` — so a heuristic designed around the midpoint is permanently saturated at 1.0 and silently steals normalised weight from `rpe_magnitude`/`novelty`. (c) `integration/memory_hub.py::_wire_sensitization::modulation_lookup` documents "neutral outcomes get modulation = 1.0 (no effect)" and delivers the inverse: an all-neutral class reads extremity 1.0, i.e. MAXIMUM sensitization. (d) `integration/bio_enrichment.py::_query_nac`'s `else: valence_str = "neutral"` is the single line where NEUTRAL and UNKNOWN merge irreversibly on the way out of the typed layer. (e) `prompts/acting_coach.py::_compose_nac_annotations` silently drops neutral rows, directly contradicting its sibling `cluster_bias_annotation.py`, which deliberately renders a `[neutral / mixed]` band and documents why. (f) `bridges/pain_bridge.py::record_action_complete` holds a CORRECT ternary whose sole production caller hardcodes `success=True`. (g) `bridges/salience_bridge.py::enrich_salience` books `Outcome.success is None` as FAILURE via a bare `else False`. Plus latent copies in never-instantiated / never-called code: `PlanningBridge` (whose `get_tool_success_rate` scores an all-neutral history 0.0 — *worse* than no history at 0.5), `Cerebellum.form_engram`, `MemoryHub.record_interaction`. | **PARTIALLY FIXED 2026-09-02 — (c), (d) and (g) closed; (a) and (b) are design calls, and THIS ROW WAS WRONG ABOUT (b).** **(g)** `record_interaction(success: bool)` now takes `bool | None` — and, **corrected in the pre-merge review round**, so does `MemoryHub.record_interaction`, which would otherwise have narrowed the tier straight back at the hub boundary. More importantly the round found the widened sink had **no reachable producer**: `record_interaction` is called only by that hub wrapper, which has no callers at all, while the path that actually fills `_interaction_history` is `on_session_start`, whose `else False` booked "this memory carries no success attribute" as a FAILURE. That `else` — an absence of evidence recorded as evidence of harm — is now a third `neutral` bucket, which is what makes `decisive < total_interactions` reachable in production and therefore what makes the decisive-denominator rate mean anything. Two further live sites kept the old denominator and were missed first time: `_calculate_history_boost` (reached via `enrich_salience`, where 5 successes among 50 neutrals boosted salience 0.03 instead of 0.30) and `stats()`. **(c)** follows from it: `modulation_lookup` and `get_success_rate` divide by DECISIVE interactions (`success + failure`), not the total. The total put neutrals in the denominator and nowhere in the numerator, so a class interacted with fifty times and learned nothing from read `success_rate` 0.0 → extremity 1.0 → **maximum** sensitization (the documented inverse), a rate *further from the truth* than the 0.5 returned for no history at all; `confidence` now scales on decisive evidence too. **(d)** `_query_nac` emits `"unknown"` for UNKNOWN rather than folding it into `"neutral"` — NEUTRAL is "observed, taught nothing directional", UNKNOWN is "not observed", and a reader that cannot tell them apart cannot tell a settled question from an open one. **(b) RE-DIAGNOSED — "the fix is producer-side only" does not survive contact.** `GoalCompleted` carries `success: bool` and nothing else, and its SOLE publisher `agentic_goal_agent._complete_goal` also takes only a bool, so there is no tier anywhere upstream to route; widening the message would trade a constant 1.0 for a constant 0.5. The defect is CONSUMER-side — feeding a genuinely binary signal to `_eval_valence_extremity` (`abs(v-0.5)*2`, which measures distance from a midpoint) is maximal by construction, and the weight-learner correctly zeroes it. Whether "a goal completed neutrally" is even meaningful is a design call: a goal was achieved or it was not, unlike a tool action, which can be ineffective without being harmful. Recorded in a comment at the site. (a) unchanged. (e)/(f) and the dead-code copies remain open. Prior status: OPEN — deliberately not fixed in the D53 pass, scope was agreed as the credit path. | See the sweep's site table for file/symbol/shape per row |

### Surfaced by the 2026-08-29 documentation-truth pass (pymaxim.bio reference gaps)

Found by writing the five reference pages that replace the retired
`dennyschaedig.com/maxim` guides, and checking every claim against the 1.1.0 wheel rather
than against the docs describing it. The doc corrections landed separately (branch
`docs/user-doc-truth-1-1`); these are the code gaps behind them, which did not.

Numbered after 1.1.1's D34–D42, which are now on `main`. **Renumbered D43/D44 -> D49/D50 on 2026-08-30:** PR #574 and PR #575 merged within an hour of each other and each independently claimed D43 and D44, so `main` briefly carried two rows under each id. The 1.1.3 ship gate cites "D44", which was therefore ambiguous between the merge-behaviour test and this section's campaign-YAML defect. The hivemind rows keep D43/D44 because every plan-doc cross-reference (`minecraft_benchmark.md`, the roadmap ladder) already points at them; these two moved.

| # | Defect | Disposition | Evidence |
|---|---|---|---|
| D49 | **The benchmark suite format promises three things the runner does not do, and two of its four advertised tiers cannot run.** (a) A suite entry's `weight` is parsed into the scenario descriptor and never read again — the composite is a flat mean, not a weighted average. (b) There is no per-scenario score for weights to compose from; `per_scenario` holds raw metrics, folded across repeat runs with a running half-mean `(old + new) / 2`, which equals the true mean only for two runs. (c) `benchmark.metrics` does not select which metrics are scored — every collected metric enters the composite. (d) `maxim --benchmark tier2` / `tier3` without `--campaign` resolve to `scenarios/benchmarks/biosystem_suite.yaml` / `embodiment_suite.yaml`, which do not exist, so the run aborts with `FileNotFoundError` (verified on the 1.1.0 wheel); `maxim.benchmark(suite="biosystem")` fails the same way. (e) `_collect_tier3_metrics` needs an `embodiment_stats()` introspector method that nothing implements, so Tier 3 is always empty. | **OPEN — 1.1.x.** Cheapest honest fix: apply `weight` in the aggregation (or delete it from the schema), replace the half-mean fold with a proper mean, and either ship the two missing suites or drop `tier2`/`tier3` from the `--benchmark` choices. Docs corrected 2026-08-29 (`docs/user/benchmarks.md`) so the file no longer over-promises in the meantime. No claim on the site or in the ledgers rests on a benchmark number, so nothing downstream is bounded. | `simulation/benchmark.py::_load_suite` (`weight` stored at the `"weight": weight` entry, no reader), `::_aggregate_runs` (`per_scenario` half-mean; `_compute_composite_score(aggregated)`), `::_compute_composite_score`, `::_collect_tier3_metrics`; `cli.py` `_tier_campaigns`; `ls scenarios/benchmarks/` (6 files, no `biosystem_suite.yaml` / `embodiment_suite.yaml`) |
| D50 | **Two campaign-YAML keys are parsed onto `CampaignDef` and then ignored, so a campaign author is told nothing.** `party_mode` is loaded from the YAML and stored, but there is no `PartyDMRuntime`, NPCs get no Hippocampus or NAc, and `dm_runtime.py` never branches on it; `choice_resolution` (default `"pc_decides"`) is parsed and has no reader anywhere. **Relationship to D40 (1.1.1):** D40 covers the *public API* half — `maxim.campaign()` documenting parameters it never referenced — and 1.1.1 fixed it by making `npc_model` and `prompt_handler` raise `NotImplementedError`. This row is the half D40 does not reach: the YAML keys themselves still load silently, and `choice_resolution` is not mentioned by either. A campaign author who sets `party_mode: true` gets no warning and no party. | **OPEN — 1.1.x.** Warn at load when `party_mode: true` is set (one line in `load_campaign`), or implement the runtime; delete `choice_resolution` from the schema until something consumes it. Documented meanwhile in `docs/user/dm-campaigns.md`. | `simulation/dm_schema.py` (fields, parse, construct), `grep -rn "party_mode\|choice_resolution" src/maxim/simulation/dm_runtime.py` → no matches; `api.py::campaign`; D40 |
| D63 | **A PR opened against a non-`main` base silently receives NO code scanning, and the absence renders identically to a pass.** CodeQL here is GitHub's *default setup* (there is no `codeql.yml` — only `test.yml`), which runs on pushes to the default branch and on PRs **targeting** it. A stacked PR based on a sibling branch therefore gets zero `Analyze`/`CodeQL` rows, and when its base merges GitHub auto-retargets it to `main` — an `edited` event, **not a push**, so CodeQL still does not run. The `main-protection` ruleset requires code scanning and is invisible to `gh pr checks`, so the PR reads `MERGEABLE` + `BLOCKED` with every VISIBLE row green and no indication of what is missing. Hit four times in one session (#595, #597, #598, #600). Same family as the CONFLICTING-suppresses-Tests variant already in CLAUDE.md, and the same shape as this release's other defects: a mechanism that does not run looks exactly like one that ran and found nothing. Surfaced 2026-09-03. | **OPEN — 1.1.x.** Workaround is known and reliable: after a retarget, push an EMPTY commit (the reviewed diff must stay the reviewed diff). The durable fix is a mechanical presence check rather than operator memory — assert that `unit-tests`, `lint`, `release-build` AND a CodeQL row are PRESENT in the PR's check list before merge, not merely that the visible ones are green. Note the manual remedy has its own failure mode, observed the same day: `git commit` can fail on a GPG timeout while the following `git push` still reports success (pushing an unchanged branch is a successful no-op), so the fix looks applied when nothing happened — **verify the branch head actually moved.** | `.github/workflows/test.yml` (the only workflow; CodeQL is default setup, not a file); `gh api repos/<o>/<r>/rulesets` -> `main-protection`; CLAUDE.md "A PR showing all-green may have run NO tests" |
| D64 | **`maxim --sim` does not exercise the substrate encoding path, so it cannot smoke-test substrate changes — and it reports clean while doing nothing.** Two runs on 2026-09-03 (one bodiless, one with `--embodiment bodies/infant_humanoid_cold`), both with `MAXIM_SUBSTRATE_PATH=1` and `MAXIM_LOG_FILE`, produced **zero** `geometry mismatch` warnings — and also zero `encode_sensors` / `substrate_node` / `interoception` log activity, no `similarity.*` logger records among 3,469 captured lines, and **no `aut_ec.json` written anywhere**. The gate under test was never reached, so the clean result was vacuous. It was nearly reported as a pass. Surfaced 2026-09-03 while verifying gates 1 and 2 before the 1.1.3 cut. | **OPEN — 1.1.x.** Two separable questions: (a) what actually enables the substrate encode path from `--sim` — `MAXIM_SUBSTRATE_PATH=1` alone was not sufficient and `--aut-mode substrate-primary` was not tried; (b) whichever it is, a sim that silently runs WITHOUT the substrate cannot be the documented way to smoke-test substrate changes. Workaround used for the 1.1.3 verification, and the better instrument regardless: drive the production plumbing in-process (`build_executor(entity_ref=...)` + `_read_drive_states`/`_read_drive_ranges` + `SensorEncoder.encode_sensors`), which is deterministic, free, and — with a counterfactual against the pre-fix tag — provably non-vacuous. | `src/maxim/simulation/`; `src/maxim/runtime/agent_loop.py::_SUBSTRATE_CHANNELS`; CLAUDE.md §"Running simulations" |
| D65 | **Four `test_affordance_transfer` tests now skip in every lane, including the nightly `-m slow` one.** `tests/conftest.py` deliberately sets `HF_HUB_OFFLINE=1` / `TRANSFORMERS_OFFLINE=1` and an isolated `HF_HOME` (correct isolation: no network in tests, no polluting the developer's cache), so `LinguisticEncoder` can never load `paraphrase-mpnet-base-v2` under pytest and always uses the bag-of-words fallback — whose own docstring says *"paraphrase collapse will NOT work with this."* D61 made that honest by skipping rather than failing, but **a skipped test is not a passing test**: the net is four mechanisms that cannot run, which is the same shape as four that ran and found nothing. Includes the two de-vacuified negative controls, so the assertions D61 added execute nowhere. | **OPEN — 1.1.x.** Remedy named at fix time and owed since: pre-seed the model into the isolated `HF_HOME` (a cached snapshot committed or fetched once in CI) so the nightly `-m slow` lane can run them for real. Until then the semantic-substrate claims in that file have no executing guard. | `tests/conftest.py` (the offline env pins); `tests/integration/test_affordance_transfer.py::_require_semantic_encoder`; `src/maxim/similarity/encoder.py::_fallback_embed` |
| D66 | **Gate 1 shipped "reject" but not "migrate", and the warning promises a remedy that does not exist.** The roadmap's gate 1 reads *"live encoder-provenance validation must reject **or migrate** incompatible state"*; what shipped (1.1.3, #597) is skip-and-warn. The warning's own text says the skipped nodes are lost *"until they are re-encoded or migrated (D1)"* — there is no such path. Consequence after a real encoder change: every percept pattern-SEPARATES, node count grows without bound, previously-learned biases are permanently uncued, and for the frozen-centroid modalities (`interoception`, `audio`) the orphaned nodes are also permanently UNCORRECTABLE — still scanned every tick, still persisted, still shipped in every hivemind bundle, with no eviction path. `docs/agents/bio-memory.md` already names an `ec invalidate` command as an open follow-up; gate 1 makes its absence load-bearing rather than latent. Surfaced by the 1.1.3 pre-merge review round (architecture + bio-fidelity lenses, independently). | **FIXED 2026-09-03 — partially, and the partiality is the honest part.** `EC.load()` now derives geometry tags for unstamped nodes from `encoder_provenance`, which persists exactly what the tag hashes. **Text nodes migrate exactly** (verified: reconstructed tag is identical to the original). **Sensor nodes written before this release cannot** — the tag hashes the DECLARED set while provenance recorded `sensor_names`, the READING, accumulated as a union across a session; a file that saw `cold` appear once has it in that union forever, and no care recovers one from the other. Those nodes are left unstamped and reported with an actionable warning, because **a wrong tag is worse than a missing one**: missing is permissive and visible, wrong silently refuses matches that should succeed. `declared_sensors` is now recorded, so files written from here on DO migrate. A session that mixed normalization modes is also refused — it is not all in one space, so no single tag is correct. Guarded incl. an ordering test: the migration must run AFTER provenance loads, since running it earlier migrates nothing while reporting that nothing could be migrated — a real bug during implementation that looks exactly like the honest refusal. **Migrate half closed 2026-09-04:** `maxim substrate invalidate` removes stale-geometry nodes by NAME (census first; unstamped untouchable), prunes the NAc biases keyed on them in lockstep, tombstones everything removed, and `_note_geometry_mismatch` now names that command — the promised remedy exists (`tests/unit/test_substrate_invalidate.py`). | `src/maxim/similarity/ec.py::pattern_complete_or_separate` (the skip + `_note_geometry_mismatch`); `::_note_geometry_mismatch` (the message that promises the missing remedy); `docs/plans/roadmap_1_1_to_1_3.md` "Gates before 1.2" item 1; `docs/agents/bio-memory.md` |
| D67 | **`items/cradle_food.yaml` describes a mechanic it does not implement.** Its description says *"Portions decrease on consumption"* and the entity declares `portions` (count, range [0,5], initial 5) — but the `eat` affordance has only `self_effect: {hunger: -0.4}` and **no `target_effect`**, and no Python decrements it. Portions sits at 5 forever. Surfaced 2026-09-03 while fixing D59: the fact that nothing decrements it is precisely why making entity-level `requires` gate did NOT change the cradle apparatus. | **RESOLVED 2026-09-04 (1.1.4 PR 3, plan decision D5): option (b) for cradle_food, option (a)'s mechanic where it belongs.** `cradle_food.yaml`'s description now states feeding is unlimited BY DESIGN (Exp 52's harness untouched), and the finite-resource mechanic ships on the world seam's own item — `items/minecraft_bread.yaml` decrements its `portions` via `target_effect` riding the existing target-resolution machinery (the `target` param defaults to the item's own name; zero new mechanism), gated by `requires: {portions: 1}`. Guard: `tests/unit/test_minecraft_seam.py` (eat decrements; empty loaf refuses). Original decision text preserved below. ~~OPEN — deliberate decision owed, not a drive-by fix.~~ Adding `target_effect: {portions: -1}` would make feeding finite in the cradle, which is **Exp 52's own harness** — a graduation row re-validated twice on 2026-09-02. So the choice was: (a) implement the decrement and accept re-staling Exp 52, (b) correct the description to match the code and keep feeding unlimited by design, or (c) leave both and record the divergence. | `src/maxim/_data/components/items/cradle_food.yaml` (the corrected description); `src/maxim/_data/components/items/minecraft_bread.yaml` (the implementing item); `tests/unit/test_minecraft_seam.py::TestMinecraftBread` (guard); `docs/experiments/52_nurture.md` (the apparatus option (a) would have changed) |
| D68 | **Gate 2 (#596) silently broke the committed bake-off harness — the instrument that selected 1.1.4's encoding mitigation could not execute.** Making `geometry` a required keyword-only parameter on `pattern_complete_or_separate` turned every `scripts/encoding_bakeoff.py` arm into an immediate `TypeError`. Nothing in CI runs `scripts/` harnesses, so the breakage rendered as nothing at all — same family as D63/D64 and the green-PR lesson: a mechanism that does not run looks exactly like one that ran and found nothing. Surfaced 2026-09-03 by the 1.1.4 PR 0 shakedown, which re-ran the harness before extending it. | **FIXED 2026-09-03 (1.1.4 PR 0)** — explicit `geometry=None` (the documented opt-out), keeping the instrument decision-identical to the run that produced `docs/experiments/data/encoding_bakeoff_2026-09-01.json` (`None` short-circuits the gate-1 filter and stamp-on-first-touch, matching pre-#596 behavior exactly). The general gap remains open as a pattern: committed measurement harnesses under `scripts/` have no smoke lane, so an API change can strand any of them; candidate remedy is a `-m slow` CI smoke that imports each and runs one trivial arm. | `scripts/encoding_bakeoff.py::_cluster_ids`; `src/maxim/similarity/ec.py::pattern_complete_or_separate` (the #596 signature change) |
| D69 | **`MAXIM_DATA_HOME` was defeated by two import-time `Path.home()` paths, and the sim orchestrator wrote its workspace relative to the CWD.** `utils/last_run.py` built `_LAST_RUNS_PATH = Path.home()/".maxim"/"last_runs.json"` as a module constant, and `models/language/cloud_dispatch.py::load_cost_config` defaulted `state_path` to `Path.home()/".maxim"/"util"/"cost_state.json"` — so `cost_tracker.py::CostTrackerConfig.__post_init__`, the resolver that already honours `MAXIM_DATA_HOME`, never ran: a second source of truth silently shadowing the first, and per-visitor cost isolation leaking through the one file that tracks spend. `simulation/orchestrator.py::start_simulation_mode` did `Path("data")/"sim_sandbox"` + mkdir + `tempfile.mkdtemp` + the `sim_agent_<ts>.jsonl` trace path relative to wherever the process was launched, plus a dead `os.makedirs("data/agents/MaximAgent/runtime")` (`atomic_write_text` creates parents), while `simulation/interactive.py` already resolved the same sandbox through `resolve_user_state`. On a read-only root with only the data home writable, no sim can start and last-run/cost bookkeeping fail invisibly (best-effort swallows). Surfaced 2026-09-02 by the pymaxim.bio sandbox audit. | **FIXED 2026-09-03** (sandbox-launch). `last_run.py::_last_runs_path()` resolves lazily through `utils/paths.resolve_user_state`; `load_cost_config` leaves `state_path` empty unless `cost_state_path` is set, so the dataclass resolver is the only default; the orchestrator block is the module-level `orchestrator.py::_prepare_sim_workspace` under `<data_home>/sim_sandbox/`, the dead mkdir removed rather than relocated. **Still CWD-relative, out of scope:** `runtime/agent_loop.py`'s `data/agents/<name>/runtime/state_<run_id>.json` (two sites; D15) and `cli.py`'s scenario-path `--home-dir` default of `data/`. | `src/maxim/utils/last_run.py::_last_runs_path`; `src/maxim/models/language/cloud_dispatch.py::load_cost_config`; `src/maxim/simulation/orchestrator.py::_prepare_sim_workspace`. Guards: `tests/unit/test_last_run.py::TestDataHome`, `tests/unit/test_cloud_dispatch.py::TestLoadCostConfig::test_default_state_path_honours_maxim_data_home`, `tests/integration/test_orchestrator_sandbox.py::TestSimWorkspaceUnderDataHome` (all verified to fail against the pre-fix source) |
| D70 | **The console handle's filesystem tools were scoped to the SERVER'S CWD, and the registry builder scaffolded `.maxim_workspace/` into that CWD on the first Talk request.** `console/handle.py` built its registry with `operational_mode="active"` and no override, so `read_file`/`write_file`/`edit_file` (and `bash`, when armed) covered wherever the operator launched `maxim serve` — the handle's own ENFORCEMENT NOTE named this and relied on the forbid set (see D72 for why that was not enough). Independently, `runtime/bootstrap.py::build_tool_registry` called `ensure_workspace_exists(cwd)` unconditionally in active mode, even when every tool was scoped elsewhere by `allowed_dirs_override` — so a read-only CWD made the handle unconstructible (the first Talk turn 500s). Surfaced 2026-09-02 by the sandbox audit. | **FIXED 2026-09-03** (sandbox-launch). `MaximHandle` passes `allowed_dirs_override=[<agent home>/workspace]` — the same root the talk loop's `FileSystemEnv` already used — and `build_tool_registry` scaffolds under the containment root (`allowed_dirs_override[0]` when given, else the CWD; the CLI path is unchanged). | `src/maxim/console/handle.py::MaximHandle.__init__`; `src/maxim/runtime/bootstrap.py::build_tool_registry`. Guard: `tests/unit/test_console_handle_workspace.py` (override root owns the scaffold; tools scoped to the agent workspace; CWD stays clean; no-override negative control) |
| D71 | **`maxim doctor` / `GET /api/diagnose` echoed inline API keys.** `doctor/checks.py::check_resolved_config` walked every `_FIELD_TO_ENV` entry and rendered the resolved value verbatim through `_format_doctor_value`. `lanes.<tier>.remote_api_key_ref` maps to `MAXIM_LANE_<TIER>_REMOTE_API_KEY`, and `config_loader._coerce_for_field` deliberately lets that env var hold an inline key (legacy semantics) — so an env-sourced key was printed in the `[source=env]` row, in the env-shadows-config row and, worst, in the copy/paste `maxim config set … {env_raw}` fix string; `check_api_key` printed a truncated tunnel key. The console serves this exact section to any caller of `/api/diagnose`, turning a diagnostics endpoint into a credential read. Surfaced 2026-09-02 by the sandbox audit. | **FIXED 2026-09-03** (sandbox-launch). One predicate, `checks.py::_is_secret_field` (leaf ends in `api_key` / `api_key_ref` / `token` / `secret`), plus `_is_secret_reference` (path or `keyring:` URI — the two shapes `_validate_api_key_ref` accepts), drive `_render_resolved_value`: references print, inline material renders as `<redacted>` (the marker `decision_log` already uses); `_shadow_fix` never echoes `env_raw` for a secret field; `check_api_key` no longer reads the key. Non-secret rows are byte-identical. `console/server.py`'s `SetupResult.detail` no longer names the secret-ref path either. Under sandbox mode `/api/diagnose` is closed outright. | `src/maxim/doctor/checks.py::_is_secret_field` / `::_render_resolved_value` / `::_shadow_fix` / `::check_api_key`; `src/maxim/console/server.py::post_setup_cloud`. Guard: `tests/unit/test_doctor_resolved_config.py::TestSecretRedaction`; `tests/unit/test_console_sandbox.py::TestClosedSurfaces::test_setup_detail_never_names_the_secret_path` |
| D72 | **`ALWAYS_ALLOWED_TOOLS` out-ranked a hard forbid, and two "read-only" tools were ungated shell primitives.** `agents/autonomy.py::AutonomyController.can_execute_action` consulted `ALWAYS_ALLOWED_TOOLS` BEFORE `SafetyConstraints.check_constraints`, so a tool in `forbidden_tools` still ran if it was also always-allowed. The console's Talk path derives its forbidden set from the live registry precisely because `SupervisionPolicy` is never consulted at AUTONOMOUS, and relied on constraints — yet `search_code` and `git_diff` executed from small talk regardless (the pin test parametrized only `bash, write_file, edit_file, git_commit`, none in the bypass set). Compounding it, `tools/git_tools.py::GitDiffTool` built `git` argv from model-supplied refs/paths with no containment (a ref of `--output=/path` is a file write outside every `allowed_dirs`), and `tools/code_tools.py::RunTestsTool` ran `command.split()` with no gate — and `runtime/fear_gate.py::_classify_action` knew only `bash`/`execute_file`/`execute_sandbox_script` as shell, so neither was reviewed even when the gate was on. Surfaced 2026-09-02 by the sandbox audit. | **FIXED 2026-09-03** (sandbox-launch). Constraints are evaluated first; the always-allowed shortcut bypasses only the PLANNING/SUPERVISED level checks. `git_diff` and `run_tests` are opt-in via `MAXIM_ALLOW_GIT_DIFF` / `MAXIM_ALLOW_RUN_TESTS` (same `env_flag` mechanism and error shape as `MAXIM_ALLOW_BASH`; the sim orchestrator arms only `bash` for the throwaway AUT via `_arm_sandboxed_aut_subprocess_tools` — its registry deregisters the other two, and neither takes a `cwd`, so arming them would be dead code; see D75); `git_diff` refuses `-`-prefixed arguments before spawn and passes `--end-of-options`; the fear gate classifies `run_tests`/`git_diff`/`git_commit` as `shell_exec`. Both new env gates carry the autouse scrub `tests/conftest.py::_isolate_maxim_tool_gate_env`. | `src/maxim/agents/autonomy.py::AutonomyController.can_execute_action`; `src/maxim/tools/git_tools.py::GitDiffTool`; `src/maxim/tools/code_tools.py::RunTestsTool`; `src/maxim/runtime/fear_gate.py::_classify_action`. Guards: `tests/unit/test_autonomy_forbid_precedence.py::TestForbidBeatsAlwaysAllowed`, `tests/unit/test_console_talk.py::TestTalkPermissions` (pin now includes `search_code`, `git_diff`), `tests/unit/test_tool_gates.py` |
| D73 | **Console agents ran with no tool gate, and `AgentConfig.tool_whitelist` was a silent no-op.** `agents/permissions.py::AgentPermissions` and the Executor's enforced check (`runtime/executor.py::Executor.execute`, alias-proof) both existed since C4, but the only builder of one was campaign YAML (`dm_schema.py`); `AgentFactory.create_full_agent` called `build_executor` without `permissions=`, so every factory-built agent (the fix wires `tools.allow` into `MaximHandle`; the CLI and `api.run` agents still pass none) had `Executor._permissions is None` and an operator had no way to keep `read_file`/`bash` away from a chat agent — a gate that exists but is never armed looks exactly like one that ran and found nothing. Alongside it, `AgentConfig.tool_whitelist` (set by `maxim.create.agent(tool_whitelist=...)` and `create_npc_agent`) was written to `ToolRegistry._tool_whitelist` and read by nothing, while `create_full_agent` replaced the registry with the caller's anyway; the `create_agent` docstring ("ToolRegistry scoped to role via tool_whitelist") was false. Surfaced 2026-09-02 by the sandbox audit. | **FIXED 2026-09-03** (sandbox-launch). `tools.allow` / `tools.deny` config (`MAXIM_TOOLS_ALLOW` / `MAXIM_TOOLS_DENY`) → `permissions.tool_permissions_from_settings` → `AgentConfig.permissions` → `build_executor(permissions=)` in the factory — the seam, as one callable thing; `tool_whitelist` deleted — `maxim.create.agent(tool_whitelist=…)` now RAISES (it builds no Executor) and `create_npc_agent` carries no permissions until Party Mode builds NPCs with one, so no builder claims an enforcement it cannot deliver; `build_executor(permissions=)` is a required keyword (the `pain_bus=` precedent) so a future builder cannot forget it silently; the allow-list is judged on the canonical name AFTER alias resolution (deny still on the source); `kind:<kind>` selectors (resolved in `Executor._permission_denial` from the registry, so `permissions.py` stays registry-free) admit per-entity SEM affordance tools as a class. Unconfigured → `None` → byte-identical to before. | `src/maxim/runtime/agent_factory.py::AgentConfig` / `::AgentFactory.create_full_agent`; `src/maxim/agents/permissions.py::AgentPermissions.can_invoke_tool` / `::tool_permissions_from_settings`; `src/maxim/runtime/executor.py::Executor._permission_denial`; `src/maxim/console/handle.py::MaximHandle.__init__`. Guards: `tests/unit/test_console_tool_allowlist.py::TestConsoleToolAllowlist::test_env_allow_list_refuses_read_file_at_the_executor`, `tests/unit/test_agent_factory.py::TestPermissionsArming`, `::TestAgentConfig::test_dead_tool_whitelist_field_is_gone`, `tests/unit/test_agent_permissions.py::TestKindSelector` |
| D74 | **The console lifespan's drain-and-stop sat OUTSIDE the shutdown `finally`, and a Talk-only session persists nothing until the handle stops.** `console/server.py`'s lifespan released the sim sink inside a `finally` (a prior review fold) but ran the bounded run-join + `handle.stop()` AFTER that block — so any exception through shutdown skipped exactly the step that persists a Talk session's substrate: the talk loop runs `run_agentic_loop(max_steps=0)` and closes its bio session only at exit, and NAc/EC/SCN/ATL persist only in `on_session_end`. `mode=rest` is not a substitute (it saves the hippocampus only), and `stop()` was reachable from nothing but the lifespan; its two waits (60 s campaign, 20 s talk join — the latter fixed inside `_stop_talk_loop`) were not a caller's to shorten, so a deployment with its own stop grace had to inherit ~110 s worst case. Surfaced 2026-09-02 by the sandbox audit. | **PARTIALLY FIXED 2026-09-03** (sandbox-launch). The drain-and-stop is the module-level `server.py::_drain_and_stop_handle` and runs INSIDE the shutdown `finally`, ahead of the sink release (so consolidation records still reach a listening client, and a raising stop cannot strand the sink); `MaximHandle.stop()` takes `campaign_wait_s` and `talk_join_s`. **Open half:** an in-band `POST /api/session/end` (a wire change) is deferred to the console contract's 0.4.0 batch — until then a hosted "end session" is a graceful SIGTERM, which reaches the same lifespan path. | `src/maxim/console/server.py::_drain_and_stop_handle` / lifespan in `::build_app`; `src/maxim/console/handle.py::MaximHandle.stop`. Guards: `tests/unit/test_console_sandbox.py::TestShutdown`, `tests/unit/test_console_handle_stop.py::TestStopWaits` |
| D75 | **Subprocess-tool gates are process-env reads, so every new gated tool is another `os.environ.setdefault` site.** `BashTool`, and since sandbox-launch `GitDiffTool` / `RunTestsTool`, read `MAXIM_ALLOW_*` at execute time (`utils/gpu_compat.env_flag`); the sim orchestrator's `_arm_sandboxed_aut_subprocess_tools` `setdefault`s the flag for the throwaway AUT, and its own docstring admits the var outlives the sim. The filesystem tools already carry a per-registry policy (`allowed_dirs_override`); the subprocess tools do not, which is why arming them for a sandboxed sim cannot be reasoned about locally and why each new gate needs another conftest scrub. Surfaced 2026-09-03 by the sandbox-launch review round. | **PENDING — design, not a patch.** Move the opt-in onto the tool/registry (a policy object passed at `build_tool_registry`, like `allowed_dirs_override`), with `cwd`/`allowed_dirs` on the two subprocess tools so a sandboxed sim can arm them honestly. Until then: `bash` only for the sim AUT; the console never arms any. | `src/maxim/tools/filesystem.py::BashTool`, `src/maxim/tools/git_tools.py::GitDiffTool`, `src/maxim/tools/code_tools.py::RunTestsTool`, `src/maxim/simulation/orchestrator.py::_arm_sandboxed_aut_subprocess_tools` |
| D76 | **Declared affordance param DEFAULTS never applied at execute time — schema decoration the tool promised and `execute` never delivered.** `spec._parse_params` has always documented `{param: {type: T, default: V}}` → `(T, V)` tuples, and `tools/base.py::_custom_to_json_schema` ADVERTISED the default to the LLM as optional-with-default — but `ModulatorAffordanceTool.execute` never injected it, so an omitted param stayed absent from kwargs, silently no-opping any machinery keyed on it. Found via `minecraft_bread`'s defaulted `target`, whose `target_effect` never fired (the D67 mechanic). Zero pre-existing YAMLs declared a default, so the hole was latent, not live. | **FIXED 2026-09-04 (1.1.4 PR 3)** — `ModulatorAffordanceTool.execute` fills declared defaults for params that are absent, `None`, or `""` (an LLM passing `target: null` must not bypass a target_effect the default routes; legitimate falsy values 0/False preserved). Structural note per the silent-no-op lesson: execute-time fill is the one-miss fix; a second miss of this class moves normalization to parse/construction. | `src/maxim/embodiment/tool_bridge.py::ModulatorAffordanceTool.execute` (the injection); `src/maxim/embodiment/spec.py::_parse_params`; guard `tests/unit/test_minecraft_seam.py::TestMinecraftBread` |
| D77 | **Entity acquisition regenerates the item's tools WITHOUT `embodiment=`, killing the replacement tools' `self_effect`.** `runtime/executor.py`'s `entity_acquired` handler calls `generate_tools_for_entity(entity, registry, entity_map=...)` with no `embodiment`, and registration replaces by name — so picking up an item replaces its sim-runner-generated, embodiment-wired tools with ones whose `self_effect` write-back is dead (`if _self_effect and self._embodiment is not None`). Latent until PR 3 shipped the first `acquirable` consumable whose self_effect matters (`minecraft_bread`): pick it up, eat — portions fall, `food` never rises, a silent no-op of the item's whole point. Pre-existing; surfaced by the PR 3 executor review lens. | **FIXED 2026-09-04 (1.1.4 PR 4)** — the acquisition regeneration threads `embodiment=self.embodiment`; guard `tests/unit/test_minecraft_harness.py::TestD77AcquisitionRegeneration` (pickup-then-eat: self_effect applies AND portions decrement; verified red pre-fix). Fixing it surfaced D78 — the pickup path had TWO holes stacked. | `src/maxim/runtime/executor.py` (the `entity_acquired` regeneration site); `src/maxim/embodiment/tool_bridge.py` (`self_effect` gate); `src/maxim/_data/components/items/minecraft_bread.yaml` (the first affected item) |
| D78 | **`EntityMap._register_locked` treated re-registration of the SAME entity as a name collision — popping the name key and stranding the entity under its full_path, silently killing every later name-based `resolve`.** The collision branch exists for two DIFFERENT entities sharing a name; registering an already-registered object hit it too, evicting the name entry. Reachable from production: acquisition regenerates tools, `generate_tools_for_entity` re-registers the entity, and from that pickup onward `resolve("minecraft_bread")` returned None — so the item's `target_effect` (D67's mechanic) silently no-opped. Found while writing D77's guard: the fixed self_effect applied and the portions decrement STILL failed, one layer down. | **FIXED 2026-09-04 (1.1.4 PR 4)** — `_register_locked` short-circuits when the stored object IS the entity being registered (idempotent re-registration). Guard: the same `TestD77AcquisitionRegeneration` pickup-then-eat asserts the decrement, which transits exactly this resolve. | `src/maxim/embodiment/entity_map.py::_register_locked`; `src/maxim/runtime/executor.py::_handle_entity_acquisition` (the re-registering caller) |
| D79 | **D77's sibling is still live at the fixed site: acquisition regeneration cannot thread `cerebellum=` because the Executor never stashes one.** `build_executor` TAKES `cerebellum` and hands it to tool generation, but the Executor object keeps no reference — so `_handle_entity_acquisition`'s regeneration (now embodiment-correct, D77) replaces cerebellum-wired tools (e.g. dm_runtime's) with ones whose `observe_from_action` forward-model feed is dead. The exact D77 skeleton one keyword over — the SECOND miss of this class at this site, which per the silent-no-op counting rule makes the structural fix due: the Executor should stash its generation-relevant collaborators (or tool regeneration should not exist as a separate weaker path). Surfaced by the PR 4 executor review lens. | **OPEN — 1.1.x.** Options: (a) stash `cerebellum` on the Executor as a declared field and thread it (the one-keyword fix, third-miss bait), (b) the structural fix — regeneration calls one shared "generate with everything this executor has" helper so a new collaborator cannot be forgotten per-site. (b) is the counting rule's answer. | `src/maxim/runtime/executor.py::_handle_entity_acquisition`; `src/maxim/runtime/bootstrap.py::build_executor` (takes-but-does-not-stash); `src/maxim/simulation/dm_runtime.py` (a cerebellum-wired generation site) |
| D80 | **The cacheable prompt prefix was invalidated at every encounter boundary.** The byte-stability invariant (docs/agents/runtime-tools.md) held — per PHASE. The scene-roster `tools` section (cacheable, unfiltered path) changes whenever the roster changes: a DM campaign's `choose` tool embeds the current options in its description, entities come and go at act boundaries. It sat SECOND in the stable prefix (after instructions/identity), ahead of `pfc_preamble`, `entity_context`, `tool_guidance_*`, `foundational`, `mode_context` — ~2.5k tokens that never change inside a session. A prefix cache (llama-server slot cache, Anthropic `cache_control`) is invalidated from the first differing byte, so every encounter change re-processed the whole prefix. Measured in Phase 0 of the sandbox plan (Maxim-web `docs/plans/sandbox.md`, 2026-09-04, Qwen2.5-32B Q4 on an M4 Pro): `cached_tokens` = 3,450 on a same-encounter call, 559 (= the head before `tools`) after an encounter change; per-call 20 s vs 65 s. Root cause is ORDER: insertion order put the most-often-changing stable section first. (The head is not immutable either — `identity` carries processing state, autonomy level and mode; and the roster tail also changes when a Wire 1/Wire 3 phrase is spliced into a description — so the ordering keeps the head cached across roster changes rather than making the tail cacheable.) | **FIXED — 2026-09-04 (P21).** `PromptSection.phase_scoped` + `PromptBudgeter.add(..., phase_scoped=True)` (requires `cacheable`, else `ValueError`); `build_segmented` emits session-stable sections first and phase-scoped ones last (`_emit_stable`); the builder tags the unfiltered `tools` manifest. Guard: `tests/unit/test_prompt_budgeter.py::TestBuildSegmented::test_phase_scoped_sections_are_emitted_after_session_stable_ones` + `::test_phase_change_keeps_the_session_stable_head_as_a_common_prefix`; `tests/integration/test_prompt_builder_audit.py::test_tools_section_is_the_last_stable_section` + `::test_encounter_change_keeps_the_session_stable_head`. | `src/maxim/agents/prompt_budgeter.py::PromptBudgeter._emit_stable`; `src/maxim/agents/prompt_builder.py::PromptBuilder._add_critical_sections` (the `tools` add site); Phase 0 table in Maxim-web `docs/plans/sandbox.md` |
| D81 | **File/glob guidance and the CWD manifest were composed for agents with no file tools.** `build_coding_context` prepends the `FILE WORKSPACE REMINDER` unconditionally and appends the `GLOB PATTERN GUIDE` when the request text contains "pattern" (among 17 indicators); the builder called it for ANY non-empty question text. An arena narration ("study his patterns") therefore bought an embodied DM AUT ~600 tokens of file guidance per call (P21 measurement: `coding_guidelines` = 614 of 6,751 estimated tokens, every call, dynamic side). The same agents also got the `CWD Context` manifest — a per-turn tree re-scan that changes when the working tree changes. Guidance about tools the roster does not carry is noise at best and a hallucination prompt at worst. | **FIXED — 2026-09-04 (P21).** `prompt_builder.has_file_tools(request)` (roster ∩ `FILE_TOOL_NAMES` — the registered names of the filesystem/code file tools, pinned to the registry by `tests/unit/test_prompt_builder.py::TestFileToolNames`; `bash` deliberately excluded) gates both the `coding_guidelines` and the `workspace_manifest` sections. Guard: `tests/integration/test_prompt_builder_audit.py::test_file_guidance_is_absent_when_the_roster_has_no_file_tool` + `::test_file_guidance_returns_with_a_file_tool_on_the_roster`. | `src/maxim/agents/prompt_builder.py::has_file_tools`, `::_add_critical_sections`, `::_add_context_sections`; `src/maxim/utils/coding_guidelines.py::build_coding_context` (unconditional reminder — unchanged, the gate is at the composition site) |
| D82 | **Tools the executor would refuse were still advertised to the model.** `tools.allow`/`tools.deny` (1.1.3) arm `AgentPermissions` on the Executor, which judges at DISPATCH; the prompt roster (`mode_info.get_available_tools` → SEM union → Wire 3) never asked. A sandboxed console agent with the plan's 8-name allow-list still saw the full registry (25 tools in the arena measurement, ~2.3k tokens), and a model that picks `similarity_search` or `write_file` gets a denial for its turn — a wasted call at 20–65 s each on the local 32B. | **FIXED — 2026-09-04 (P21).** `Executor.permits(tool_name)` (public read of `_permission_denial`) and the agent loop filters `available_tools` through it after the SEM union and the Wire 3 filter, before `tool_descriptions` are collected; the passive-mode relevance-filter path in the builder intersects the learned index's answer with the roster. No permissions configured → nothing changes. Guard: `tests/unit/test_console_tool_allowlist.py::TestRosterAdvertisesOnlyPermittedTools` (permits mirrors the dispatch gate; source pin on the loop's filter placement). | `src/maxim/runtime/executor.py::Executor.permits`; `src/maxim/runtime/agent_loop.py::run_agentic_loop` (roster filter, before `last_surfaced_tools`) |

## Pending, not yet a defect

- **The S1 annotation renderer will break PR #497's S4 parser** (its regex matches the band
  exactly; adding `— why` inside the bracket makes it report "no annotation" silently).
  Not yet a defect because neither has merged — but they must land together with the
  parser fix, or this becomes D12 on the day they don't.
